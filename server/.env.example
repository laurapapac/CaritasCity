DATABASE_URL=postgres://caritas:caritas_dev_pw@localhost:5432/caritascity
PORT=3001

# Desktop code settings
DESKTOP_CODE_LENGTH=6
DESKTOP_CODE_TTL_MINUTES=5

# QR code image generation (server/src/scripts/generate-qr-images.ts)
# Must be an address a phone camera can actually reach — swap to the real
# deployed origin before generating codes meant for production.
QR_BASE_URL=http://localhost:5173
