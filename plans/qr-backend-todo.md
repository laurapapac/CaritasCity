# QR Code Backend — Status & Next Steps

Use this to re-prompt Claude if the conversation is lost. Paste it in and say "continue from qr-backend-todo.md".

## RESOLVED: road dashed centerlines were z-fighting/flickering at intersections (2026-09-04, same session, follow-up)

User confirmed the dashed-centerline design itself was right ("the road lines are perfect") but reported it looked "very glitchy" — flickering, not a shape/design complaint.

- **Root cause**: `decor.roads` segments come from `generateCityLayout.ts`'s `leavesToRoads` — BSP-leaf boundary edges, always exactly axis-aligned (`z1===z2` or `x1===x2`, never diagonal). Adjacent leaves sharing a full edge get deduped into one segment, but at every intersection, a "horizontal" segment's `ROAD_WIDTH`-wide quad and a "vertical" segment's quad only share a corner *point* in the underlying edge data — after decor.ts turns each into a real `ROAD_WIDTH=4`-wide plane, those two quads physically overlap across the whole intersection square, both sitting at the identical `y=0.10`. Classic coplanar z-fighting. This overlap already existed before today's texture work, but was invisible against a flat matching fill color — it only became visible once the surface carried a dashed line + wheel-wear bands, since z-fighting flickers per-pixel between whichever triangle wins depth that frame.
- **Fix**: added `ROAD_LAYER_SPLIT = 0.01` and split the two orientations onto slightly different Y (`0.10` vs `0.10 + ROAD_LAYER_SPLIT`) based on each segment's own `Math.abs(dz) > Math.abs(dx)` check, in `decor.ts`'s road-building loop. A fixed per-orientation offset (not a random/per-index jitter) means the same orientation consistently wins at every overlap, every frame — no more coin-flip flicker. Stays well under the next Y-stacking layer up (plazas, `0.12`), so no new z-fighting introduced against that.
- **Verified live via `/dev/city`**: zoomed into a real cross-intersection — both dashed lines now cross cleanly, no torn/interleaved pattern where they meet (previously would have shown the z-fight artifact even in a single frame, not just as temporal flicker). No console errors. `pnpm exec vite build` clean.
- **Not exhaustively re-verified**: checked one intersection closely, not all of the city's 335 road segments' crossing points — the fix is a structural one (any axis-aligned crossing gets the same treatment), so it should generalize, but worth a look if a flicker gets reported at a specific spot again.

## RESOLVED: roads and the ground disc textured — were the last two flat-color surfaces (2026-09-04, same session, follow-up)

User asked for roads and "the floor" to get a texture instead of a plain color, "if possible... different textures to make it more realistic." Parks/beaches already had procedural CanvasTextures (`buildGrassTexture`/`buildStoneTexture`, added 2026-08-12) — roads (`ROAD_COLOR` flat fill) and the city's grey ground disc (`GROUND_GREY` flat fill, in `buildGroundGroup`) were the only two decor surfaces still untextured.

- **Added two new procedural CanvasTextures in `decor.ts`**, same technique/pattern as the existing ones (seeded `mulberry32` PRNG, drawn once at startup, `THREE.RepeatWrapping`):
  - `buildAsphaltTexture()` — dark speckle grain, two faint darker "wheel-wear" bands, and a dashed centerline. The centerline is a single dash centered in the tile (gap split evenly at the tile edges) so it repeats seamlessly across segments of any length.
  - `buildConcreteTexture()` — light mottled speckle + a joint line drawn only along the tile's own top/left edge, so repeating it reads as one continuous slab grid instead of doubled lines at each seam.
- **Road UV**: each road segment now gets `scaleUV(geo, length / TEXTURE_TILE_UNITS.road, 1)` — only the length axis is scaled. `PlaneGeometry`'s default V already spans exactly `[0,1]` across `decor.roadWidth`, so leaving V unscaled is what keeps the dashed centerline sitting on the true center of every segment regardless of that segment's own length or the pre-existing `ROAD_WIDTH=4` value (unchanged).
- **Ground disc UV**: `buildGroundGroup`'s inner `CircleGeometry` isn't a plane, but its own default UV already maps the full diameter to `[0,1]` on both axes, so the same `scaleUV` helper works unchanged with `repeat = (2*discRadius) / TEXTURE_TILE_UNITS.floor`.
- **`ROAD_COLOR` constant removed** — dead once the road material switched from a flat `color` to a `map`; left in place, its unused-const would have failed the build lint. Base tones are otherwise unchanged (asphalt/concrete canvas fills match the old flat hex colors), this only adds visible texture on top.
- **Verified live** via `/dev/city` (whole-city preview, 467 buildings/335 road segments/3 parks/2 lakes/2 plazas at this zoom) — zoomed screenshots confirm the dashed centerline + wheel-wear bands render correctly on real road segments at multiple angles/lengths, and the ground disc shows the mottled slab-grid texture instead of flat grey. Also checked `/kiosk` (blurred background city, code-entry card unaffected) — no console errors on either page (checked via `read_console_messages`, both a first load and a hard reload). `pnpm exec vite build` clean.
- **Not done**: plazas (`PLAZA_COLOR`) are still a flat fill — out of scope, user asked specifically about roads and "the floor," not plazas; flag if they should get the same treatment later. Not committed yet — this file's standing convention is to confirm with the user before committing.

## RESOLVED: documentation gap backfilled — 5 real commits from 2026-09-03 were never logged here (2026-09-04)

Picked up cold via this file's resume prompt. `git status` showed a clean working tree, but `git log` showed **5 commits landed 2026-09-03** (a separate session, `session_01MMMAH2k7kELvwtYTsREw7g`) after this file's last-documented entry (`879c26a`, kiosk autofocus) — none of them mentioned here. Same pattern this file has flagged before (2026-08-25, 2026-09-01): work landing and shipping without the todo file being updated in the same session. Confirmed via `git show --stat` on each rather than assuming from the subject line alone.

- **`ea8aae0` — Redesign kiosk code-entry card + mobile code screen.** `/kiosk` entry step rebuilt to match a client-provided mockup: GRADiMIR logo, Croatian thank-you/donation copy, restyled OTP input. `/s/:token` mobile screen gets the same logo header, a bordered card, "visit gradimir.caritas.hr" instructions, and a larger letter-spaced code. Bundles the Caritas/GRADiMIR logo as a real frontend asset (previously only lived under `server/qr-exports`). Touches `Kiosk.tsx`, `ScanLanding.tsx`, adds `src/assets/logo-caritas-crvena.png`.
- **`ce52c21` — Shrink the mobile code screen's letter code, 6xl → 3xl.** User feedback: the code didn't need to be huge. One-line change in `ScanLanding.tsx`.
- **`7872b75` — Redesign school-selection step, translate kiosk UI to Croatian, swap in slogan logo.** School-selection card now matches the code-entry card's width/layout (logo on top, no repeated donation explainer), copy translated to Croatian to match the rest of the flow, kiosk error messages (invalid code, unknown school, etc.) also translated. Swaps in a newer logo variant with the "Craftaj bolji svijet!" slogan baked into the graphic (`logo-caritas-crvena-slogan.png`, replacing the plain one added the commit before), used consistently across kiosk + mobile screen per the user's explicit choice.
- **`1e09f8d` — School step now names the specific building being built, not just the category.** "Koja škola?" → "Koja ste škola?", and the generic "Postavljate {category} kockicu" line is replaced with "Stavljate kockicu koja gradi {building}." using the actual active variant (house/apartment/school/restaurant/food bank/hospital) rather than the coarser category label. Read straight out of the city data `Kiosk()` already has loaded locally (the active building per category is already fixed before school selection) — no backend change needed.

**Not independently re-verified this session** — backfilling from `git show`/commit messages only, no live re-test of the Croatian copy or the redesigned cards. Environment was cold (Docker down, no dev servers) at the start of this session; see the next entry for bringing it back up before any live verification.

## RESOLVED: kiosk code-entry field now autofocuses on load — real dropped-ref bug, not an environment quirk (2026-09-02)

User asked for the `/kiosk` code input to be focused on load with no click needed. First attempt: added a `useRef`/`useEffect(() => inputRef.current?.focus(), [])` in `EntryStep` (`Kiosk.tsx`) and passed `ref={inputRef}` to the local `<InputOTP>` wrapper. Verified live via the automated browser tab at the time — but that tab is backgrounded (`document.hasFocus()` false, `visibilityState: "hidden"`), so the failure to observe focus landing was wrongly attributed to that known limitation. User then tested for real (hard refresh on the actual kiosk machine) and reported it still didn't work — click was still required.

- **Real root cause**: `InputOTP` in `src/app/components/ui/input-otp.tsx` was a plain function component, not wrapped in `React.forwardRef`. This project is React 18, where a `ref` passed to a plain function component is silently dropped (no error, no warning surfaced here) — so `inputRef.current` stayed `null` forever and `inputRef.current?.focus()` was a no-op every render. Not a timing/CSS/environment issue at all.
- **Fix**: wrapped `InputOTP` in `React.forwardRef<React.ElementRef<typeof OTPInput>, ...>` and passed `ref` through to the underlying `<OTPInput>` from the `input-otp` library (itself already `forwardRef<HTMLInputElement>`, confirmed by reading its `.d.ts`). No change needed to `Kiosk.tsx`'s `EntryStep` — its ref/effect were already correct once the wrapper actually forwarded the ref.
- **Verified live correctly this time**: distinguished "does .focus() get called" from "does the tab have OS focus" — confirmed a manual `.focus()` call on the raw DOM node does move `document.activeElement` even in this backgrounded automation tab (`hasFocus:false`), proving focus-calls aren't suppressed here. Then reloaded `/kiosk` fresh and polled `document.activeElement`: it was the OTP input at `t=0`, immediately on mount — confirming the ref now actually attaches and the effect fires, not just that it theoretically should. `vite build` clean.
- **Committed and pushed** — see `git log` for the hash. User confirmed to commit without an additional live re-test round, having reviewed the fix.

## Status: dust burst raised again, 10→20 — user flagged 10 was still "way too mild" (2026-09-01, same session, follow-up)

Immediate follow-up after the 5→10 round below shipped: user live-tested it and said 10 still looks too mild, asked for at least 20, with more possible if 20 is still mild.

- **Fix**: `DUST_PARTICLES_PER_BURST` 10→20, `DUST_POOL_SIZE` 128→256 (`utils.ts`) — same doubling-together pattern as the 5→10 round, for the same reason (keep the ring-buffer pool's headroom at ~12-13 concurrent bursts rather than letting it shrink as per-burst count rises).
- **Verified live via `/dev/preview`**, same temporary-hook pattern as every round in this file: re-added `window.__cityDustDebug`, confirmed the hot-reloaded constants read back as `perBurst: 20, poolSize: 256`, called `spawnDustBurst` directly and confirmed exactly 20 active pool slots — then removed the hook (confirmed via `grep`, zero matches; `git diff --stat` shows only `utils.ts` changed, not `cityScene.ts`). `vite build` clean. As with every prior round, the actual "does 20 look right now" visual judgment needs the user's own live look — this tab still has no running `requestAnimationFrame`.
- **Open**: user explicitly flagged this may need another round if 20 still isn't enough — no cap has been set on how far this can go, just keep scaling `DUST_POOL_SIZE` in lockstep.
- **Committed and pushed together with this same todo.md update** — see `git log` for the hash.

## RESOLVED: dust burst still read as too mild after the visibility fix — doubled particle count 5→10 (2026-09-01)

Picked up cold via this file's resume prompt. Found on arrival that every "not committed yet" note in this file's then-top three entries (dust visibility tuning, dust burst addition, hospital camera-framing fix) was stale — `git log`/`git show` confirmed all three were already committed and pushed (`c980aa3`, `e919ef2`); corrected those notes below rather than leaving them wrong. Brought the dev environment up from a full cold stop (Docker Desktop, backend, frontend, same access pattern as every prior session — LAN IP unchanged at `192.168.1.163`). Then user reported the dust burst itself (see the visibility fix two entries below) still reads too mild — not a "can't see it at all" complaint like before, just "not enough of it."

- **Fix**: `DUST_PARTICLES_PER_BURST` 5→10 (`utils.ts`). `DUST_POOL_SIZE` scaled 64→128 in the same commit, doubled alongside it rather than left as-is — the pool is a fixed-size ring buffer shared by every animated landing (real placement + the ambient loop's up-to-180-concurrent-sites cadence), so leaving it at 64 while doubling per-burst spawn count would have halved how many concurrent bursts the pool could hold before recycling still-visible particles early. Scaling both by the same factor keeps the pool's headroom, in bursts-worth of capacity, identical to before (~12-13 concurrent bursts either way) rather than guessing a new number.
- **Verified live via `/dev/preview`**: added a temporary `window.__cityDustDebug` hook exposing `spawnDustBurst`/`dustSlots`/the two tuned constants (`cityScene.ts`, fully removed before finishing — confirmed via `grep` returning no matches and `git diff --stat` showing zero diff in that file). Read the constants back live (`perBurst: 10, poolSize: 128`, confirming the hot-reloaded dev server actually picked up the change), then called `spawnDustBurst` directly and read the pool back: exactly 10 active slots, each with a correctly randomized velocity and the right spawn point. Same as every prior animation-verification note in this file, this automated browser tab has no live `requestAnimationFrame`, so the actual "reads as more dust now" visual judgment still needs a look on a real kiosk/phone — this confirms the mechanism is correct and wired, not the felt density. `vite build` clean.
- **Committed and pushed together with this same todo.md update** — see `git log` for the hash (user confirmed directly this time, avoiding the stale-note problem the entry below describes).

## RESOLVED: three stale "not committed yet" notes below corrected — all three were already committed (2026-09-01, same session)

On resuming, `git status` showed a clean working tree and `git log` showed the dust-burst/tuning commit (`c980aa3`) and the camera-framing commit (`e919ef2`) both already present — matching, commit-for-commit, what the entries below described as still uncommitted. Diffed `c980aa3` directly against the visibility-tuning entry's described constants (`DUST_SIZE`, `DUST_LIFETIME`, `DUST_HEX`) to confirm it wasn't a partial/different commit before editing anything. Updated each entry's closing line from "Not committed yet" to the real commit hash rather than leaving the file internally contradicting `git log`.

## RESOLVED: landing dust was firing but was too small/brief/low-contrast to actually see (2026-08-31, same session, follow-up)

User reported not seeing any particles on `/dev/kiosk-progress` after the dust feature below landed, even after a hard refresh (ruling out stale HMR). Root-caused rather than guessed: reproduced live, confirmed via debug hooks that the particle pool WAS populating correctly on real "Add next block" landings (`activeCount:5`, correct randomized values) — so the spawn/wiring wasn't broken. Then isolated rendering itself from the timing question by writing a deliberately oversized (1.5-unit) debris cube directly into an inactive pool slot, bypassing the whole spawn/lifetime system — it rendered exactly where expected, correct colour, confirming the `InstancedMesh`/material/scene wiring all work. That left only the tuning: `DUST_SIZE=0.16` shrinking to 0 over `DUST_LIFETIME=0.35s`, in a tan-grey close to the terrain's own ground colour, was small/brief/low-contrast enough to be genuinely imperceptible at the camera distances a real placement happens from — not a rendering bug, a visibility one.

- **Fix**: `DUST_SIZE` 0.16→0.32, `DUST_LIFETIME` 0.35→0.5s (widened together — bigger alone without more time on screen wouldn't have helped), `DUST_HEX` 0xb0a890→0xd4c4a0 (lightened so it doesn't blend into the ground). All in `utils.ts` alongside the other `DUST_*` constants, same file/pattern as everything else in this feature.
- **A genuine tooling gap hit while debugging this, worth remembering**: this automated browser tab has no free-running render loop at all — not even the previously-documented "throttled" rAF, but literally none — confirmed by polling the drop/dust pool state for a full 2 seconds after a real click with no forced repaint in between and seeing zero progress (the drop that should land in 0.722s never did). A single forced repaint (e.g. a screenshot call) makes exactly one `animate()` tick fire, which correctly fast-forwards any time-based state to what it should be at that instant (since the math is wall-clock-elapsed-based, not frame-count-based) — but that means the *dust itself* only stays "fresh" for whatever's left of its whole 0.5s lifetime after that one catch-up tick, so unless a screenshot happens to land inside that narrow post-landing window, it'll only ever show either the pre-landing fall or the post-dust settled state, never the dust itself. Tried narrowing the window (a calibrated ~1s wait before the screenshot) without success — round-trip overhead alone seems to exceed the ~1.2s combined fall+dust window consistently. **Not resolved as a verification method** — same underlying limitation as every other animation feature in this file's history (`revealBlockAt`, `DROP_FALL_DUR`, the easing curve), just sharper here because the visible window is shorter. Confirmed correct via the isolated forced-render test and direct pool-state reads instead, not a screenshot.
- **Committed and pushed together with the dust-burst addition below** as `c980aa3` (confirmed via `git log`/`git show` on 2026-09-01 — this note previously said "not committed yet," which was stale).

## RESOLVED: landing-impact dust added — the hard-stop drop read as bland (2026-08-31, same session, follow-up)

User feedback: the drop's hard-stop landing (no squash/bounce, deliberately — see the entry below, a squash was tried earlier and explicitly rejected as unrealistic for a solid block) reads as bland on its own. Discussed options first (dust/debris burst, impact flash, small non-squash positional rebound, camera shake) before implementing anything — user picked the dust/debris burst.

- **Design**: a small burst of debris cubes kicks up from the landed block's own base at the exact moment it lands — real solid objects throw off dust/chips on impact without deforming or bouncing, so this doesn't touch the block itself at all, staying consistent with the "no squash" call. One shared particle system for the whole city (`dustMesh`, a single `InstancedMesh`, not per-building) — a fixed-size ring buffer pool (`DUST_POOL_SIZE=64`, `DUST_PARTICLES_PER_BURST=5` per landing) bounds total particle count regardless of how many buildings are landing blocks at once, since this fires on every animated landing — both a real user's own placement and the ambient loop's roughly-one-per-second-per-active-site cadence across up to 180 concurrent sites — same as the fall itself, nothing asked for these to differ. Each particle gets a randomized outward+upward velocity (`DUST_SPEED_MIN/MAX`, `DUST_UP_SPEED_MIN/MAX`), falls under `DUST_GRAVITY`, and shrinks to nothing over `DUST_LIFETIME=0.35s` rather than popping out. All new constants documented in `utils.ts` alongside the existing `DROP_*` ones.
- **Wiring**: `spawnDustBurst(x,y,z)` is called from the animate loop's existing drop-entry "elapsed >= duration" branch (`cityScene.ts`) — the exact point a block transitions from falling to landed, for both solid and glass, real and ambient — so no new state tracking was needed to know when a landing happens. A second small per-frame loop (64 fixed iterations, cheap) advances/expires the dust pool the same way the block-drop loop advances `node.drops`.
- **Verified live via `/dev/preview`** (temporary `window.__cityDust`/`__cityNodes` debug hooks, removed before finishing — confirmed via `grep`): called `spawnDustBurst` directly and read back the resulting pool state — exactly 5 new active slots, each with a randomized angle/speed within the configured ranges and the correct spawn point. Confirmed by direct code reading (not just belief) that the per-frame update math is correct projectile motion matching the pool spawn parameters. Same as every other animation change in this file: this automated browser tab has no live `requestAnimationFrame` ticking in the background, so the actual visual motion couldn't be watched frame-by-frame here — needs a look on a real kiosk/phone to confirm it reads right. `vite build` clean, no console errors on `/kiosk`.
- **Committed and pushed together with the visibility tuning above** as `c980aa3` (confirmed via `git log`/`git show` on 2026-09-01 — this note previously said "not committed yet," which was stale).

## RESOLVED: house windows rendered as opaque colored blocks, never transparent glass (2026-08-31, same session, follow-up)

Follow-up to the glass drop-animation fix below — flagged there as a side note, user asked to fix it. House windows carry a real per-style tint from `WINDOW_PALETTES` in `houseGenerator.ts` (10 house styles, each with its own window color, e.g. `0x99ddbb` for `cottage`/`adobe`) that never matched `GLASS_HEX` (`0x90c8d8`) exactly — and `isGlass()` (`utils.ts`) classified blocks purely by exact color match. So every house window failed that check and rendered through the SOLID mesh as an opaque, flat-colored block instead of a transparent pane — every other building type's windows already worked because `buildingGenerators.ts`'s `makeBlocks` force-writes `color: GLASS_HEX` on every window-type block regardless of source palette, a normalization step `houseGenerator.ts` never had.

- **Fix**: `isGlass()` now also checks `b.type === "glass" || b.type === "window"` (the two labels used across the generators) before falling back to the exact-color check — `export const isGlass = (b: Block): boolean => b.type === "glass" || b.type === "window" || resolveColor(b) === GLASS_HEX`. Confirmed this doesn't lose anything: the glass material itself is one shared, uniformly-tinted transparent material for the whole city (`glassMesh` never receives a per-instance colour, in any building type — see `revealBlockAt`), so `WINDOW_PALETTES`' per-style tint was never actually visible in the rendered scene even before this fix; it only existed in source data and this one now-fixed classification check. Nothing else reads a glass block's own color for rendering, so no other behavior changes.
- **Verified live via `/dev/preview`** (temporary debug hooks, removed before finishing): a real house (60 window-type blocks) — before the fix, `glassMesh.count` was 0 and all 60 rendered through `solidMesh`; after, `glassMesh.count === 60` exactly, and a zoomed screenshot confirms the windows now show as visibly distinct light-blue transparent panes instead of blending into the brick facade as flat opaque squares. Re-checked the drop-animation fix together with this one: house window index 296 (the same block instance flagged in the prior entry, which used to resolve `isGlass:false`) now resolves `isGlass:true` and gets the animated fall. `vite build` clean, no console errors on `/kiosk`.
- **Committed and pushed together with the glass drop-animation fix below** — see `git log` for the hash.

## RESOLVED: glass blocks now animate the drop-in fall — a gap inherited from the old highlight system (2026-08-31, same session, follow-up)

User noticed glass (window) blocks never animated — they always popped straight to their resting position, while solid blocks got the full drop-in fall. Root cause: `revealBlockAt`'s glass branch (`cityScene.ts`) never looked at `opts.animate`/height/duration at all — it always set the final position immediately, a behavior carried over unexamined from the old yellow-highlight system (which, per its own now-removed comment, "also only ever applied to solid blocks") when the drop-in animation replaced it on 2026-08-28.

- **Fix**: `DropEntry` gained an `isGlass` flag. Glass and solid blocks live in separate `InstancedMesh`es with independent index spaces (`glassUpTo` vs `solidUpTo`), so the per-frame drop loop now picks the right mesh/index array per entry instead of always assuming solid. `revealBlockAt`'s glass branch now spawns elevated and registers a drop the same way solid blocks do when `animate` is true (no instance colour to set — glass never carried one) — same height/duration/easing, so a real user's own glass blocks fall from `DROP_HEIGHT_OWN`/`DROP_FALL_DUR_OWN` and ambient/replay glass blocks behave exactly like their solid counterparts already did.
- **Side note, resolved above**: while testing, found the house blueprint's own "glass" blocks carry a custom color (`0x99ddbb`, a mint tint) that doesn't match `GLASS_HEX` (`0x90c8d8`) exactly, so `isGlass()`'s exact-color check resolves them as solid, opaque blocks (through `solidMesh`, not `glassMesh`) rather than transparent — see the entry above, user asked for this to be fixed too.
- **Verified live via `/dev/preview`** (temporary `window.__cityScene`/`window.__cityNodes` debug hooks, both fully removed before finishing — confirmed via `grep`): forced `addBlock` to land exactly on a real `GLASS_HEX` window block in the small hospital (index 405) and read back the resulting drop entry directly — `{isGlass:true, height:9.5, duration:0.722}`, and the glass instance's spawn matrix confirmed at `y=12` (`2.5 + 9.5`, matching the exact same spawn formula solid blocks use). This tab still has no live `requestAnimationFrame` running in the background (re-confirmed: the entry was still pending 2s after triggering, well past its 0.722s duration), consistent with every prior animation-verification note in this file — the landing snap itself reuses the identical, already-proven `b.y + 0.5` logic solid blocks use, just via a picked mesh/index. `vite build` clean, no console errors on `/kiosk`.
- **Committed and pushed together with the house-windows fix above** — see `git log` for the hash.

## RESOLVED: camera framing used the finished blueprint's occupancy instead of construction-in-progress state — badly wrong on hospitals and other thick-walled buildings (2026-08-31, same session, follow-up)

User report, from real usage: on hospital buildings specifically, blocks placed while filling a floor's wall ring horizontally sometimes framed from the side with the shot blocked by the building's own geometry, instead of from above. Confirmed on hospitals; flagged that other buildings could have it too.

- **Root cause**: `findExposedFaceNormal` (`cityScene.ts`) decides which way to point the camera by checking which of a block's 6 neighbor cells are empty — but it checked against the **entire finished blueprint** (`node.blocks`, unfiltered), not what's actually built at the moment the block is revealed. Block visibility everywhere else in this codebase is a prefix of the blueprint array (`visibleCount`/`solidUpTo`), but this one function ignored that and looked at the final state. Hospitals (and per the generator comments, apartments/school/food_bank — anything hollowed with a 2-thick wall ring, `generateSmallHospital`/`generateMediumHospital`/`generateLargeHospital`) merge a solid full-footprint floor slab into the wall ring at each floor's floor level. Against the *finished* building, the ring's inner sub-layer at that exact layer has every one of its 6 neighbors occupied (wall continues above, previous floor's ring below, that floor's own slab inward) — so it hit `exposedLateral.length === 0`, the "fully enclosed (shouldn't happen)" fallback, which returns a **fixed, unchecked direction** regardless of whether it's actually clear. But at reveal time neither "above" nor "inward" exists yet (blueprint insertion order: a floor's full-height wall ring is written before that floor's slab, and row-by-row before the layer above it), so the block was genuinely visible from several directions right then — the check just never looked at construction-order state to find that out.
- **Verified empirically before touching any code**: wrote a standalone script running the exact old (full-blueprint) vs. a new (prefix/construction-order) occupancy check against the real generators (`generateSmallHospital`/`generateMediumHospital`/`generateLargeHospital`, stubbing `document`/`ImageData` so `buildingGenerators.ts` could load outside a browser). Old logic hit the broken fallback on **340/4,000 (8.5%) small hospital blocks, 1,140/6,100 (18.7%) medium, 1,356/8,000 (17%) large** — not a rare edge case, a large fraction of every hospital variant. New (prefix) logic: **zero** fully-enclosed cases across all three, with thousands of blocks correctly reclassified to the top-down framing branch.
- **Fix**: `findExposedFaceNormal(blocks, index)` now builds its `occupied` set from `blocks[0..index]` only, not the full array — one-line change to the set construction, single call site (`focusOnBlock`), no other callers affected. `wallSpanLength`'s corner tie-breaking (fed the same `occupied` set) inherits the same construction-order semantics automatically, consistent with the same reasoning.
- **Verified live via `/dev/preview`'s "Camera framing test" panel** (not just the standalone script): jumped straight to small-hospital block index 211 — the exact block the script flagged as old-broken (`x=1,y=1,z=1`) — with "Blocks placed" set to 212 so the ring is actually rendered. Before this fix this would have hit the arbitrary fallback; after the fix it frames as a clean top-down shot with the target block clearly visible (highlighted white), no obstruction. Also re-checked the roof/default case (index 3999, the red-cross roof slab) still frames correctly from above — no regression on the cases that already worked. `vite build` clean, no console errors on `/dev/preview`.
- **Committed and pushed** as `e919ef2` (confirmed via `git log` on 2026-09-01 — this note previously said "not committed yet," which was stale).

## RESOLVED: real-placement drop duration given its own constant, scaled to the new height (2026-08-31)

Picked up cold via this file's top TODO. After the 2026-08-28 session raised `DROP_HEIGHT_OWN` 5.0→9.5 (so a real user's own block spawns off-camera), `DROP_FALL_DUR` (0.38s) stayed shared with the ambient loop's shorter `DROP_HEIGHT` (3.0, untouched) — same duration suddenly covering ~1.9x the distance, reading as too fast.

- **Fix**: new `DROP_FALL_DUR_OWN` in `utils.ts`, `0.38 * (DROP_HEIGHT_OWN / 5.0) ≈ 0.722s` — scaled by the same 5.0→9.5 ratio the height itself was raised by. The fall's t³ easing curve is height-independent in shape (fraction of height fallen is always `t³` regardless of height), so only landing speed changes with height/duration — this reproduces the exact landing speed the fall already had back when `DROP_HEIGHT_OWN` was still 5.0, before it started reading as too fast, rather than guessing a new feel from scratch.
- **Threaded through as its own constant, not a shared one** (matching the TODO's explicit ask): `DropEntry` (`cityScene.ts`) gained a `duration` field alongside `height`, `revealBlockAt` gained a `duration` opt (defaults to the ambient loop's `DROP_FALL_DUR`), and the per-frame drop animation loop reads `duration` off each entry instead of the module-level `DROP_FALL_DUR` constant. `addBlock` (real placement) now passes `{ height: DROP_HEIGHT_OWN, duration: DROP_FALL_DUR_OWN }`; `playAmbientCycle`'s reveal call site was left untouched (still calls `revealBlockAt(node, i)` with no overrides, so it keeps the original height/duration) — confirmed by reading that call site directly rather than assuming.
- **Verified live**, not just by reading the code back: brought the dev environment up from a full cold stop (Docker Desktop, `docker compose up -d`, backend, frontend — all were down, consistent with every prior "session ended here" note in this file; LAN IP unchanged at `192.168.1.163`). On `/dev/preview`, real-time animation still can't be watched frame-by-frame in this automated browser tab (confirmed again this session: a bare `requestAnimationFrame` polling loop hung the tab for the tool's 45s timeout), and round-trip latency between a UI click and the next screenshot already exceeds the new 0.722s duration, so a screenshot-based mid-flight catch isn't reliable either. Used the same direct-verification pattern as prior animation sessions instead: added temporary `window.__cityScene`/`window.__cityNodes` debug hooks (`CityRenderer.tsx`, `cityScene.ts`, both fully removed before finishing — confirmed via `grep` after removal, and `git diff --stat` shows no residual diff in `CityRenderer.tsx`), called `addBlock` directly through the hook, and read the resulting `drops` Map entry back synchronously in the same script (no dependency on a render frame actually occurring): `{"height":9.5,"duration":0.722}` — confirms the new constant is real, wired to the right call site, and not shadowed by some other default. Also confirmed `/kiosk` loads clean (fresh reload, no console errors, code-entry screen renders normally over the blurred city).
- **Not committed yet** — confirm with the user before committing, per this file's standing convention.

## RESOLVED: drop easing bumped cubic→quartic — landing itself still read as too slow (2026-08-31, same session, follow-up)

User tested the `DROP_FALL_DUR_OWN` fix above live and reported the fall overall was fine now but the very end, right at landing, still read as too slow.

- **Fix**: the shared drop-animation curve (`cityScene.ts`'s animate loop, `y = height*(1 - t^n)`) went from `t³` to `t⁴` — `t * t * t` → `t * t * t * t`. Reasoned rather than guessed: fraction-fallen is `t^n` at every point in time, so raising `n` leaves the slow start slower still (t⁴ < t³ for all t<1 — e.g. at 90% of the fall's duration only 66% has fallen vs. 73% at n=3) while landing-instant velocity (∝ n·height/duration) rises by a third — more of the fall's motion is now compressed into a visible speed-up right before impact instead of a steady coast into the ground. Shared by both the real-placement drop and the ambient loop's (same code path, only `height`/`duration` differ per entry) — user didn't ask to split it further, and both use the same fall "language" by design (see `DROP_HEIGHT`/`DROP_FALL_DUR`'s own history above).
- **Verified**: `vite build` clean. Curve math reasoned through directly (see above) rather than guessed — this environment still can't watch the animation play frame-by-frame (no live `requestAnimationFrame` in this automated browser tab, confirmed again this session), so the actual "feels right" judgment came from the user testing live on their own end, not from anything observed here.
- **Committed and pushed** — see `git log` for the hash (bundled with the `DROP_FALL_DUR_OWN` fix above in the same commit, since the user approved both together after live-testing the combination).

## Status: newly-placed-block animation overhaul — drop-in physics + a persistent "your own block" marker, replacing the old yellow highlight (2026-08-28)

Long, iterative session, all live-verified and committed/pushed as 3 separate commits (`019f8dd`, `d6b6707`, `d052ddb` — see `git log`). Picked up cold via this file; dev environment was already up from a prior session (see the old resume prompt further down) and stayed up the whole time.

**1. Replaced the old "pop in yellow, fade to final colour" highlight with a real drop-in animation** (`addBlock`/`playAmbientCycle`, both route through `revealBlockAt` in `cityScene.ts`). Iterated through several rounds of live user feedback, each verified live (mostly via `/dev/preview`'s "Simulate placing next block" and direct instance-matrix/colour readback through a temporary `window.__cityScene` debug hook, always removed before finishing — this automation environment's tab has no live `requestAnimationFrame`, confirmed by hanging a bare rAF chain for 45s, so frame-by-frame animation had to be verified by forcing single paints via screenshots and reading the GPU instance buffers directly, not by watching it animate):
   - Added squash-on-landing + colour flash → user: squash read as "too bouncy" → removed, kept a plain fall.
   - Found a real bug: the fall looked like it "dropped too high, then blinked into place." Root cause was ease-in (`1-t²`) paired with too-short a duration/too-tall a drop, compressing nearly all visible motion into the last sliver of time — NOT the curve direction. First attempt wrongly flipped to ease-out; user's later "make it more realistic, start slower, hit the ground" feedback confirmed ease-in (accelerating gravity) was right all along — fixed by lengthening `DROP_FALL_DUR` and switching to cubic (`1-t³`), which keeps the same early-motion pace while landing sooner (verified: at 20% of the time only ~4% has fallen).
   - Removed the colour flash entirely per request; tried a transient post-landing "shine" (HSL lightness boost) → user: wanted a *persistent* marker instead, on both real placements AND re-scanned "existing" blocks, but invisible to other kiosks/users ("this is your block").
   - Built it as a separate camera-facing glow sprite (radial-gradient canvas texture, additive blending) → user disliked the "circular sprite" look, wanted the block ITSELF lit up.
   - Rebuilt as a persistent per-instance colour blend on the block's own colour (`markOwnBlock`, only one marked block in the whole scene at a time — marking a new one restores the previous block's true colour first, so it's purely client-side/local and a shared kiosk cycling between users always shows just the current one's block). First version blended toward warm gold at up to 50% — user: "didn't mean make it yellow, I liked the white, just wanted it less opaque" (a genuine misunderstanding on my part, corrected) → now blends toward white, capped at 30% at the pulse's peak (`OWN_BLOCK_LIT_HEX`/`OWN_BLOCK_BLEND_MIN`/`MAX`/`PULSE_PERIOD` in `utils.ts`).
   - Split `DROP_HEIGHT` in two: ambient decoration loop keeps the original (3.0), a real user's own placement (`addBlock`) got its own `DROP_HEIGHT_OWN`, raised 5.0 → 9.5 so it spawns fully outside camera view. The 9.5 wasn't guessed — projected test points through the real `focusOnBlock` camera (both a lateral wall shot and a steep top-view roof shot) and found the frame's top edge sits at height ≈7 in both, independent of shot type.
   - **Open item from this round**: see the TODO at the top of this file — duration wasn't re-tuned for the new taller height, user flagged it now reads as too fast.

**2. Roadside trees now keep clear of real buildings' own corners** (separate ask, mid-session) — `src/scripts/generateCityLayout.ts`'s `generateRoadTrees` already kept trees off road-*segment* endpoints (a block's own corners) but that never lined up with a building's actual footprint corner (buildings are inset from the block edge, several share one block). Added a direct check against every real building's own corner points (`buildingCorners`), 6-unit clearance (`BUILDING_CORNER_TREE_CLEARANCE`). Decoration buildings excluded on purpose — never the target of a real placement shot. Regenerated: 431 of 2,088 roadside trees removed (~20%); every other generated dataset (buildings, decoration buildings, parks, furniture, terrain) came back byte-identical per the generator's own log counts — nothing else moved.

**Session ended here at the user's request** — dev environment (Docker, backend, frontend) shut down cleanly so the machine could log off. See "Environment notes" further below for how to bring it back up.

**Resume prompt** — paste this to pick up exactly where this session left off:
> Continue from plans/qr-backend-todo.md. Working tree is clean, everything committed and pushed to `origin/master` (`d052ddb` is the tip). One explicit open item at the very top of this file: the real-placement block drop (now falling from `DROP_HEIGHT_OWN=9.5`, see `src/components/city/utils.ts`) needs its fall duration slowed back down — it was tuned for the old, shorter height and now reads as too fast. Give it its own duration constant rather than touching the ambient loop's. Verify live via `/dev/preview`.
>
> Dev environment (Docker, backend, frontend) was shut down cleanly at the end of this session — see "Environment notes" further below for how to bring it back up. Same access pattern as every prior session once it's back: Docker Desktop isn't on PATH (`C:\Users\mm\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe`, ~10s to come up), neither is node/pnpm (`C:\Tools\node-v24.19.0-win-x64\`), LAN IP was `192.168.1.163` as of this session (re-check via `ipconfig`). Frontend must be `pnpm exec vite --host` (not plain `pnpm dev`) for the QR scanner to work from a real phone.

## Status: real concurrency bug found and fixed — two kiosks placing into the same building visually landed on the same spot (2026-08-27, same day, follow-up)

User report from real usage (2 phones, 2 desktop kiosks, 2 QR codes in the same category): both kiosks visually placed their new block in the same spot, but revisiting later showed the blocks in their correct, distinct positions — meaning the DB was right and this was a client-only rendering bug.

- **Root cause, traced through the full call chain**: `Kiosk.tsx`'s `handleConfirm` (the live single-placement flow, fires right after a successful `placeBlock` call) drives `focusOnBlock`/`playConstructionMontage`/`addBlock` — but `playConstructionMontage` and `addBlock` (`cityScene.ts`) both compute which block to reveal from the CLIENT's own local `node.visibleCount`, not from the server-confirmed `res.block.blockIndex` the response already contains. `focusOnBlock` (camera framing only) was already correct — it takes `blockIndex` directly. Two kiosks that hadn't polled since each other's last placement both still had the same stale local `visibleCount`, so both montages played out ending at that same (no-longer-current) index — the DB had already correctly serialized two different `block_index` rows the whole time (confirmed directly: `server/src/routes/placeBlock.ts` takes a `FOR UPDATE` row lock on the active building per category, computes `blockIndex = completed_blocks` before incrementing, and the insert has a unique constraint — this side was never the problem).
- **Fix**: one line — `cityRef.current?.setVisibleCount(res.block.buildingId, res.block.blockIndex)` added right before `focusOnBlock`/`playConstructionMontage` in `handleConfirm`'s `.then`. This silently, instantly catches the local scene up to the true pre-placement count (revealing, with no animation, any block another kiosk placed that this one didn't know about) before the existing montage/highlight logic runs — which then, operating on now-correct data, lands on the right block. `addBlock`'s own `onProgress` callback (which dispatches `completedBlocks` into React state) also derives from `node.visibleCount`, so this one change keeps the Three.js scene, the local `state.buildings`, and the server in sync together — no other call sites needed the same fix (the boot-reveal effect only runs once right after a fresh fetch, so it was never stale; the "existing code" re-scan path only calls `focusOnBlock`, already correct).
- **Verified live end-to-end, not just by code reading**: loaded a real kiosk tab (building id 1, house, `completed_blocks=5`), then placed a block into the *same* building via a raw API call outside the browser (`scan` → `enter` → `place-block` with a second real unused QR token, simulating "the other kiosk") — this pushed the DB to `completed_blocks=6` with the browser tab still unaware. Then drove a real placement through that same tab's UI (third real token) — the kiosk correctly reported **"7/1080 placed"** (not the stale "6" the bug would have shown), matching the DB exactly (`completed_blocks=7`, `block_index` rows 0-6 all present). Zoomed in on the actual rendered voxels: exactly 7 foundation blocks visible, no gap, no duplicate. `vite build` clean, no console errors.
- **Committed and pushed** as `e9a5630`. Test data note: the verification above consumed 2 more real dev QR tokens (now used, same as every prior test-placement note in this file) and left building id 1 (house) at 7/1080 in the dev DB — not reverted, consistent with this file's established practice of leaving dev-DB test placements in place rather than resetting.

## Status: ambient decoration pool raised to 180 concurrent + a second "near-bottom" start mode (2026-08-27, same day, follow-up)

User feedback, two rounds in one sitting: 90 concurrent sites (the prior session's own raise) still wasn't noticeable enough, and every active site starting "near the top" (15-35% missing) read as too uniform across a pool that size.

- **Checked geometric feasibility before picking a number**, same discipline as every prior pool-size tuning in this file: only 305 decoration buildings exist in the whole city. A standalone script running `pickPool`'s exact greedy algorithm found a sharp cliff in the real `STATIC_DECOR_BUILDINGS` position data right at the buildings' own median nearest-neighbor spacing (~22 units): `MIN_SEPARATION=23` reliably reached 209-219 candidates over 200 trials, but `22` jumped straight to 286+ — meaning 22 and below barely enforces any separation at all (most of the city already qualifies). User had asked about 200 concurrent first; told them the real ceiling for genuine spread was closer to ~180, and they went with that. `CONCURRENT_SITES` 90→180, `POOL_SIZE` 110→200, `MIN_SEPARATION` 35→23 — verified live via a one-off debug log: pool of exactly 200, 180 active.
- **New `useNearBottomStart` mode in `playAmbientCycle`** (`cityScene.ts`): a ~50/50 coin flip per cycle between the existing "near-top" behavior and a new "near-bottom" one that starts around block ~200 regardless of building size (every real building type has ≥1,080 blocks, so 200 always leaves genuine room below it) — rebuilding nearly the entire building from scratch. Both modes still always truncate through to the true roof, so the 2026-08-27 visibility fix (gap must reach the top, never an isolated window) isn't reintroduced. Verified live via temporary debug logging: an exact 45/45 split at `CONCURRENT_SITES=90` before raising further to 180.
- **Accepted trade-off, flagged not asked about**: a near-bottom start on the largest buildings (8,000 blocks) means a ~2-hour cycle at the established true-1-block/sec pacing, since it's rebuilding nearly the whole thing. Consistent with the standing "duration doesn't matter, only perceived activity does" call from earlier this session.
- **Committed and pushed** as `adf8566`.

## Status: tree canopy voxels get the same block texture as buildings (2026-08-27, same day, follow-up)

User request: treetop voxels read as flat solid color next to the buildings' own textured blocks, wanted texture added without changing any colors.

- Added `map: BLOCK_TEX` (the same procedural 64×64 canvas texture — per-face edge bevel + grain, multiplied with the material's base color — buildings already use via `utils.ts`'s `makeBlockTexture`) to all 5 tree canopy `foliageMat` definitions in `decor.ts`'s `buildTreeTypes()`. Colors themselves untouched. Hill/buffer trees share the same materials (merged into the same `buildVariedTreesMesh` call as park/road trees), so they're covered too with no separate change needed. Trunks (smooth `CylinderGeometry`, not a voxel shape) deliberately left plain.
- **Verified live** on `/dev/city`: zoomed into a tree cluster and confirmed the canopy cubes show the grain/bevel pattern, no longer flat green. Clean build, no console errors.
- **Committed and pushed** as `f4ac720`.

## Status: fountain moved north (z 148->163) — narrower retry after the first attempt was reverted, capped below the requested +8 after a live-swept side effect (2026-08-27, same day, follow-up)

First attempt at "move the fountain north" also resized the church-block plaza (rebased its sizing logic to fix a real bug it hit along the way) and added lamps/benches around it, committed as `f08a837`. User reviewed live and said "this is not what i wanted so we will try again" and had it reverted (`git revert f08a837` → `5bd9d02`, clean — diff against the pre-change commit came back empty). Confirmed this wasn't "the fix was wrong," but scope: the user's actual ask this time was narrower — **only** move the fountain, explicitly leave the plaza's size/position and the church alone, no lamps/benches this time.

- Asked the user how far north (the first attempt's target, z=154, was one option; **z=160 (+12 from the original 148) is what they picked**).
- `fountain_zone` (`generateCityLayout.ts` RESERVED_ZONES) and `FOUNTAIN_POSITION` (`staticCityData.ts`) both moved 148→160, x unchanged (25) — same "only the z axis moves" pattern as every prior fountain move.
- **The real risk this time**: `churchBlockPlaza`'s sizing was still (before this session's edit) derived from `fountainLeaf`, itself looked up from the fountain's *live* position — harmless historically because the fountain and the plaza always moved together, but a real coupling bug for a fountain-only move, since pushing the fountain far enough north can cross it into a different BSP leaf (confirmed exactly this in the reverted attempt at z=154/z=150+, see the archived entries below) and silently resize/reposition the plaza as a side effect. Fixed by introducing `FOUNTAIN_ANCHOR = { x: 25, z: 148 }` — the fountain's pre-move position, frozen, not a live reference to `fountainZone` — and pointing the plaza's `fountainLeaf` lookup at that instead. The plaza's geometry can now never change no matter where `fountain_zone` itself moves, short of someone deliberately editing `FOUNTAIN_ANCHOR` by hand.
- **Verified at z=160**: re-ran the generator; only `generateCityLayout.ts` and `staticCityData.ts` show real diffs — `cityFurniture.ts` (which holds the plaza/lamps/benches) came back byte-identical (git flagged it as modified only due to this repo's known CRLF/LF false-positive, already documented elsewhere in this file; `git diff` showed zero actual content changes), and `cityLayout.ts`/`cityDecorBuildings.ts`/`cityRoads.ts`/`cityDecor.ts`/`cityTerrain.ts` didn't even get touched. Furniture log line matched the pre-change baseline exactly: `196 lamp posts (5 along the church's block, 6 along lake_east's), 67 benches, 2 plazas`. `vite build` clean, live on `/dev/city` looked right.
- **User then asked for +8 more (z=168, same session)**. This time the generator's own log lines changed: BSP buildable leaves 93→94, decoration buildings (never backend-linked filler) 305→309, road segments 335→338 — real 160 QR-linked buildings still unaffected and still zero overlaps, but a genuine ripple this specific move introduced that z=160 hadn't. Mechanism: `fountain_zone` is an exclusion *circle* (radius 15) used during BSP leaf pruning, not just a point — moving it 8 units crossed a leaf boundary somewhere along that circle's edge (not necessarily near the church itself) and flipped one previously-excluded leaf to buildable, which the decoration/fill pass then populated. User initially thought this must be about proximity to the church (misreading `CHURCH_POSITION_BASE_Z=170`, a frozen historical pre-shift reference, as the church's real position — corrected: the church's real current position is `CHURCH_POSITION.z=182`) — clarified the actual mechanism (circle-vs-leaf-boundary, unrelated to proximity) before asking how to proceed.
- **User chose to find the largest ripple-free z.** Live-swept every integer z from 160 to 168 (not guessed): 160-163 reproduce the exact pre-move counts (93 buildable, 305 decoration buildings, 335 roads); 164 is the first z where the ripple appears (94/308/338), and it stays flipped through 168 (94/309/338). Set the fountain to **z=163** — the largest move that changes nothing except the fountain's own position, +15 from the original 148 rather than the requested +20 (to 168).
- **Verified at z=163**: re-ran the generator — `BSP: 93 buildable`, `Decoration buildings: 305 total`, `Furniture: 196 lamp posts..., 67 benches, 2 plazas`, `Roads: 335 segments` all match the untouched baseline exactly; `git status` shows real diffs only in `generateCityLayout.ts`/`staticCityData.ts` (`cityFurniture.ts`'s flag is the same CRLF false-positive, zero real diff). `vite build` clean. Live on `/dev/city`: whole-city building count back to the original 467 (was 471 at the z=168 test), plaza unchanged, fountain visibly closer to the church than the z=160 shot, no overlap with the church itself, no console errors.
- **Committed and pushed** as `50b5802`.

## Archived (reverted, see the entry above): fountain next to the church + plaza lamps/benches — user reverted, scope was wrong (2026-08-27)

Moved the fountain to z=154 ("next to the church"), found and fixed a real plaza-sizing bug the move exposed (`churchBlockPlaza` silently returning empty once the fountain crossed into the church's own BSP leaf), fixed a second real bug (plaza x-center didn't track the fountain), and added lamps+benches around the plaza per the same request. All verified live and committed as `f08a837`. **User reverted it** ("this is not what i wanted so we will try again") — reverted cleanly via `git revert --no-edit f08a837` (`5bd9d02`), diff against the pre-change commit (`fc422d8`) confirmed empty. The two real bugs found during that attempt (leaf-identity coupling, x-centering) are still real and worth remembering if the plaza is ever touched again, even though the fix itself was reverted.

## RESOLVED: ambient decoration construction pacing — was batching several blocks per tick, now strictly one (2026-08-27, same day, follow-up)

Committed the gap fix below as `fa2cfbc`, then user reviewed live and reported the gap itself was fixed but the reveal now placed "a handful of blocks per second" instead of one — a regression introduced by that same fix, not a separate bug.

**Cause**: `playAmbientCycle`'s truncated span grew ~3-4x (5-9% → 15-35% of a building's blocks, needed so the now-top-anchored gap reads as a real gap rather than a sliver) but the caller-supplied tick count controlling duration (`REVEAL_COUNT_MIN/MAX`, 20-45) didn't change — so `perTick = ceil(span / ticks)` started batching several blocks into every `staggerMs` step to keep the old ~20-45 second duration.

**Fix**: removed tick-count-driven batching entirely. `playAmbientCycle` now always reveals exactly one block per `staggerMs` step; duration is simply `span` seconds, so bigger buildings legitimately take longer to finish rather than being sped up to fit a fixed window (a house's cycle is now roughly 2.5-6 minutes at 15-35% of 1,080 blocks; a big 8,000-block hospital's could run tens of minutes). Since duration is no longer a caller-supplied input, the `ticks`/`count` parameter was dropped from `playAmbientCycle`'s signature end-to-end (`cityScene.ts`, its pass-throughs in `CityRenderer.tsx` and `City.tsx`, and the one call site in `useAmbientDecorConstruction.ts`, which also lost its now-unused `REVEAL_COUNT_MIN/MAX` constants) rather than left as a vestigial unused argument.

**Verified**: `vite build` clean, no console errors on a fresh `/kiosk` load. Confirmed via temporary debug instrumentation (removed before finishing) that each reveal step increments exactly one block index, no batching — checked the actual index sequence (`733,734,735,...`), not just eyeballing the screen. Could **not** cleanly verify the real-world 1-second cadence via wall-clock timing in this automated browser tab: `document.visibilityState` reported `"hidden"` throughout (a background/inert tab in this environment), and Chrome throttles/clamps `setTimeout` in backgrounded tabs — measured deltas drifted to ~2s instead of ~1s, consistent with known throttling rather than a real bug (this project's terrain-work sessions hit the same rAF-throttling artifact before). The block-index-sequence check is unaffected by tab-visibility throttling and is the part that actually mattered for the user's report (block *count* per tick, not wall-clock precision) — a real focused kiosk display doesn't have this throttling.

**Committed and pushed** as `b78f05f`.

**Flag for the user, resolved**: raised whether size-scaled duration (houses a few minutes, biggest hospitals tens of minutes per cycle) was actually wanted. User confirmed directly: "user doesn't need to see the completion of the building. it just needs to make it seem like city is actively being built" — duration scaling is fine as-is, not a problem to solve. No further change needed here.

## RESOLVED: ambient decoration construction gap — root cause was viewing angle, not the hide mechanism (2026-08-27)

Picked this up cold via this file's resume prompt. Brought the dev environment back up first (Docker Desktop was closed, `caritascity-postgres` container gone but the named volume survived — `docker compose up -d` recreated the container clean; backend `pnpm run dev` on :3001, frontend `pnpm exec vite --host` on :5173; confirmed both reachable over LAN at `192.168.1.163`, unchanged). Then picked up the "MUST FIX" entry directly below, which two prior rounds of fixes (highlight added, then proportional window sizing) had left open.

**Root cause, found by direct live A/B testing in the browser (not just re-reading the code)**: the previous session's own GPU-instance-buffer verification was correct as far as it went — `hideBlockAt`'s zero-scale matrix genuinely was landing at the right slot — but nobody had actually compared a hidden-vs-revealed screenshot at the *same camera position* to check whether that correct data produced a visible pixel change. Added temporary debug globals (`window.__debugCam`/`__debugMatrix`/`__debugHide`/`__debugShow`/etc., all removed before finishing) to jump the live `/kiosk` camera directly onto one specific decoration building (`deco_house_0`) and one specific hidden block index, confirmed via `getMatrixAt().decompose()` that the instance really was scale-`[0,0,0]` at that exact moment, then screenshotted hidden vs. revealed at the identical camera position: **pixel-identical**, even though the block was confirmed to have exposed (non-occluded) faces and no duplicate co-located block existed at that position. Tried the todo's other proposed alternative (translate-far-away instead of scale-zero) — same result, pixel-identical. Then, as a sanity check on the test methodology itself, truncated the same building via the already-proven `rebuildNode`/`mesh.count` mechanism (what real building placement uses) — the building visibly vanished immediately, proving the camera/screenshot test *could* detect a real gap when one exists.

The actual mechanism: the 2026-08-26 fix deliberately placed the hidden window in the **middle** of the blueprint's height (user request, so it wouldn't always be "just the last few roof blocks") while leaving the roof and upper floors above it fully intact. From the kiosk's normal elevated/establishing camera angle (looking down at the city, not eye-level), the intact roof's own silhouette visually overhangs and bridges across the gap below it — the roof and the foundation connect in screen space even though there's a real, correctly-rendered void between them in world space. Confirmed directly: from a close, near-eye-level angle looking straight at the hidden band, the gap is glaringly obvious (see-through to buildings behind, floating roof); from the kiosk's actual default angle, identical pixels hidden vs. revealed. This is exactly the failure mode the user reported ("fully built the entire time, only the highlight visible") — the highlight pop was real and visible (it's a small flash right at the surface), but the gap it should have been revealing never was, from the angle anyone actually looks at the kiosk from.

**Fix**: `playAmbientCycle` (`cityScene.ts`) no longer uses an isolated mid-building window at all — `hideBlockAt` is deleted (was dead code the moment this shipped, its only caller). Instead it reuses the exact same `rebuildNode` prefix-truncation + `revealBlockAt` sequence that real building placement and `playConstructionMontage` already use (both long since proven to render correctly from any angle, since a truncated building has no floating disconnected geometry above the gap): truncates the building down to a randomized point partway up (`start = total - span`), then reveals sequentially from there back up to the true top. `start` still varies cycle to cycle (span is randomized, same as before) so the effect doesn't always begin from 0 or always redo the same tiny roof sliver — but the reveal always finishes at the real roof, guaranteeing the silhouette itself is genuinely incomplete while hidden, visible from any angle. Compensated by raising `AMBIENT_REVEAL_PCT_MIN/MAX` from 5-9% to 15-35% (`AMBIENT_REVEAL_MIN_BLOCKS` 24→40): a suffix anchored at the roof needs to be a real fraction of the building to read as "still under construction" rather than "the last couple of shingles," which was the original 2026-08-26 complaint this whole redesign is fixing at the root instead of patching around.

**Verified live via `/kiosk`** (fresh reload, code-entry blur overlay hidden via a one-off `style.display='none'` for screenshot clarity only, no form interaction): the default elevated background-city view now shows real, visible construction — yellow highlight flashes appearing and fading at multiple buildings' rooflines as they're actively rebuilt, matching the intended "the city feels alive" effect. `vite build` clean (1,733 modules, no errors). No console errors on a fresh `/kiosk` load. All temporary `window.__debug*` diagnostic globals removed — `hideBlockAt` and its doc comment fully deleted rather than left dead. Updated `useAmbientDecorConstruction.ts`'s own doc comments (module header + the `REVEAL_COUNT`/window-position comment), which had described the now-removed mid-window design.

**Committed and pushed** as `fa2cfbc` (bundled with the previous session's still-uncommitted pool-scatter/reveal-window-position changes — pool 5→50, `MIN_SEPARATION` 120→60, scatter shuffled — since those landed in the same working tree and were never about this bug).

**Not exhaustively re-verified**: watched the default city view for ~15-20 seconds across a handful of buildings, not a long unattended session across the full pool of 50 concurrent sites. The 15-35% span range is a reasoned choice (matches the "needs real size to read as active construction" logic above) but not tuned against every building size/variant — worth another look if a specific building type (e.g. the largest 8,000-block hospitals) still reads as subtle.

**Resume prompt (2026-08-27, most current)** — paste this to pick up exactly where this session left off:
> Continue from plans/qr-backend-todo.md. Working tree is fully clean — everything from this session is committed and pushed to `origin/master` (`e9a5630` is the tip: concurrent-kiosk block-placement fix, ambient pool raised to 180 with a near-bottom start mode, tree canopy texture, the fountain moved to z=163, plus the ambient gap/pacing fixes further down). Nothing outstanding from this session. Check `git status`/`git log` for anything newer before assuming this is still the most current state.
>
> Dev environment (Docker, backend, frontend) was shut down cleanly at the user's request at the end of this session, same as every prior "session ended here" note in this file — see "Environment notes" further below for how to bring it back up. Same access pattern as every prior session once it's back: Docker Desktop isn't on PATH (`C:\Users\mm\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe`, ~10s to come up), neither is node/pnpm (`C:\Tools\node-v24.19.0-win-x64\`), LAN IP was `192.168.1.163` as of this session (re-check via `ipconfig`). Frontend must be `pnpm exec vite --host` (not plain `pnpm dev`) for the QR scanner to work from a real phone.

## Archived (superseded by the RESOLVED entry above): ambient decoration construction — hidden-block gap still not visually appearing after 2 rounds of fixes (2026-08-26)

**Open bug, unresolved at end of session.** The ambient decoration-construction loop (`aad8301`, 2026-08-25 — a cosmetic effect that cycles a handful of never-backend-linked decoration buildings through fake "construction" so the kiosk feels alive) is supposed to hide a window of a building's blocks, then reveal them one at a time with a yellow highlight flash. User reports: buildings look **fully built the entire time** — only the yellow highlight is visible, traveling across what's already solid geometry, never an actual missing-then-appearing block. Confirmed live twice, after two different attempted fixes, same session:

1. **First fix (highlighting)**: the very first version had no highlight at all (`revealBlockAt(node, i, { highlight: false })`), so there was no visual cue at all — added the highlight. User then reported the "fully built from the start" symptom for the first time.
2. **Second fix (proportional window size)**: hypothesized the hidden window (fixed 20-45 blocks) was just proportionally too small to notice on a big building — ~2-4% of a 1,080-block house but only ~0.3-0.5% of an 8,000-block hospital. Changed to a percentage of each building's own size (5-9%, floored at 24 blocks), verified via live debug trace that spans now scale correctly (e.g. 65-96 blocks on a house, ~544 on a 6,100-block hospital). **User re-tested and reported the exact same symptom persists** — ruling out "too small to notice" as the (sole, at least) explanation.

**What's been directly verified NOT the cause** (not just reasoned about — actually inspected live):
- `hideBlockAt`'s GPU instance-buffer write was read back directly (not just code-reviewed): confirmed a mathematically correct zero-scale matrix (`[0,0,0,...,0,tx,ty,tz,1]`) lands at the exact right slot/position. Zero-scale InstancedMesh instances are a standard, normally bulletproof WebGL hide technique (degenerate zero-area triangles, which rasterizers universally skip) — `voxelGeo` is a plain `THREE.BoxGeometry`, `solidMat` a plain `THREE.MeshLambertMaterial`, no custom shader/`onBeforeCompile` found that could bypass instance scale.
- Traced every caller of `rebuildNode` (the only other function that writes these instance matrices) — `syncBuilding`'s guard (`target === node.visibleCount`) always skips it for decoration buildings mid-cycle, since neither `hideBlockAt` nor `revealBlockAt` ever move `node.visibleCount` away from the building's already-full total. No code path was found that could be silently un-hiding blocks before the user looks.

**Real candidates for next session** (none tried yet):
- No one has actually screen-recorded/screenshotted a real gap — every check so far was either console-logged data verification (confirmed correct) or the user's live impression (confirmed broken). Log the exact buildingId + start/end block indices for one active cycle, then have the user zoom in on specifically that building and watch, to remove all ambiguity about which of the 50 concurrent sites they're looking at or when in the cycle they're looking.
- Try the alternative "translate far away" hide technique (e.g. move to y=-5000) instead of scale-to-zero, as an A/B — different rendering code path, would help rule the specific technique in or out even though it should theoretically be equivalent.
- With 50 sites scattered across the whole city, it's possible the user keeps looking at buildings that aren't currently mid-cycle (which correctly show 100% built) rather than the ones that are. Consider a temporary on-screen/console marker of which buildings are actively cycling right now.
- Consider a `/dev`-only shortcut (matching this project's established pattern for hard-to-verify camera/animation work — see `/dev/preview`'s "Camera framing test" panel, 2026-08-18) that triggers one isolated ambient cycle on one chosen building, no other buildings cycling, so it's trivial to watch a single clean repeatable case instead of hunting through a full live city.

**Also requested this session, done and verified but blocked on the above**: pool raised from 5→50 concurrent (`POOL_SIZE=65`), scattered across the whole city instead of anchored to one point (`MIN_SEPARATION` 120→60, selection shuffled instead of sorted-by-distance), and the reveal window moved to the middle third of each building's blueprint instead of the tail. All independently verified via live debug trace (pool size, scatter distances, window %, proportional span — see git history for the exact numbers). These are real, working changes — just **not committed**, since they were built to help diagnose/fix the core bug above and that bug is still open. Do not commit until the actual visibility issue is confirmed fixed live.

**Session ended here** — dev environment (Docker, backend, frontend) was shut down cleanly at the user's request so the machine could be powered off. See "Environment notes" further below for how to bring it back up.

## Status: camera zoom/tilt relaxed per user feedback + ambient decoration-construction loop added (2026-08-25, same day, fourth follow-up — documented late, see note)

**Documentation gap**: these 3 commits (12:35–13:29) landed after the QR-minting entry below was written but were never added to this file at the time — caught and backfilled at the start of the next session (2026-08-26) when `git log` showed them undocumented. All 3 are already committed and pushed; nothing here is at-risk, this entry just catches the file up. Each commit's own message (see `git log`) has the full reasoning — summarized below.

- **`2901320` — Tighten placement-camera zoom ~50% and relax top-view tilt away from bird's-eye.** `FOCUS_OFFSET` 22→14.7 (shared by lateral + top-view branches, so tree-clearance math downstream stays consistent). `TOP_VIEW_TILT_DEG` 22→40, `TOP_VIEW_TILT_LOW_DEG` 55→68 — user feedback after the `PAN_LIMIT` fix was "framing correct but too far/too steep." Verified live via `/dev/kiosk-progress`.
- **`c482688` — Relax top-view tilt further toward eye-level per user feedback.** First pass still read as too bird's-eye; pushed further: `TOP_VIEW_TILT_DEG` 40→55 (35° above horizon), `TOP_VIEW_TILT_LOW_DEG` 68→78 (12° above horizon, near eye-level for low blocks). Verified live via `/dev/kiosk-progress`.
- **`aad8301` — Add ambient decoration-building construction loop for a livelier kiosk.** New purely-cosmetic, client-side-only effect (user request, unrelated to the two tuning commits above): a rotating pool of 7 decoration buildings (never backend-linked, `deco_`-prefixed ids) near the city center cycles through construction forever, one block/sec, 5 concurrent at a time, so the kiosk feels alive between real placements — never touches the DB or the 160 real QR-linked buildings. New `playAmbientCycle`/`stopAmbientCycle` on `CitySceneHandle` (`cityScene.ts`, tracks each building's timer independently via a `Map`, doesn't conflict with a real user's own placement montage), passed through `CityRenderer.tsx`/`City.tsx`, new `useAmbientDecorConstruction.ts` (greedy nearest-first pool selection with 120-unit min separation, hands off to the farthest reserve member on completion to avoid jarring neighbor jumps, skips to a random near-the-end blueprint point so cycle length is size-independent), wired into `Kiosk.tsx` only (gated on scene mount — `/dev/kiosk-progress` and `/dev/city` unaffected). Verified live via `/kiosk`: correct pool separation, 5 concurrent cycles, correct rotation on completion, no console errors anywhere, `vite build` clean.
- **Not verified this session (2026-08-26)**: brought the dev environment back up (see resume prompt below) but did not re-test the ambient loop or new tilt values live yet — only confirmed the servers/DB came up clean. Worth a quick `/kiosk` look before further camera/decoration work in this area.

## Status: fresh QR codes minted — old images were pointing at tokens wiped by the 2026-08-19 dev DB reset (2026-08-25, same day, third follow-up)

User tried scanning an existing QR image and it failed. Root cause, confirmed directly against the DB (`docker exec caritascity-postgres psql -U caritas -d caritascity`): the `qr_codes` table only had **5 rows total** (the dev tokens minted by the 2026-08-19 reseed's `pnpm run seed`), one already consumed by that session's own end-to-end test placement. Every PNG under `server/qr-exports/images/qr-codes-2026-08-10T...` (the original ~5,080-token batch) encodes tokens that no longer exist in the database at all — that whole batch was wiped when the dev DB got `docker compose down -v`'d as part of the 2026-08-19 rebalance work. The LAN IP baked into those old images (`192.168.1.163`) is still correct and unchanged — it was specifically the *tokens* that were stale, not the network address.

- **New non-destructive script**: `server/src/scripts/generate-qr-batch.ts` — mints additional `qr_codes` rows only (no wipe of `blocks`/`buildings`/existing `qr_codes`, unlike `reset-and-generate-qr.ts` which resets the entire city's progress back to 0). Same CSV output shape as `reset-and-generate-qr.ts` so it feeds straight into the existing `generate-qr-images.ts`. Usage: `pnpm exec tsx src/scripts/generate-qr-batch.ts residential:20 hospital:20 food:20 school:20` (category:count pairs).
- **Minted 20 fresh tokens per category (80 total)**, then ran `pnpm exec tsx src/scripts/generate-qr-images.ts "" http://192.168.1.163:5173` against them → `server/qr-exports/images/qr-codes-2026-08-25T11-51-44-169Z/{residential,hospital,food,school}/<token>.png`.
- **Verified one real end-to-end**: `POST /api/scan/:token` → `POST /api/enter` → `POST /api/place-block` (school id 1, "Dev Test School") against one fresh residential token — placed a real block (house building id 1, block 0/1080). Confirms the new batch's tokens are valid and the encoded URLs are correctly reachable. That one specific residential token/image is now "used" (scanning it again will show `existing`, not place a new block) — the other 19 residential + all 20 each of hospital/food/school are untouched/fresh.
- **Old 2026-08-10 CSV/image batch left in place, not deleted** — still on disk at `server/qr-exports/images/qr-codes-2026-08-10T...` but every token in it is now permanently invalid (no matching DB row); don't hand those out again. Not deleted since removing files wasn't asked for.
- **Not done**: no code committed for this (only the new `generate-qr-batch.ts` script is new source; the PNG exports and CSV are gitignored build artifacts, same as prior QR batches — confirm this is the convention before committing anything).

## Next steps & resume prompt (2026-08-26, most current)

**Environment confirmed up and healthy as of 2026-08-26** (start of this session, picking up cold via this file): Docker Desktop was closed, `caritascity-postgres` container gone (not just stopped — `docker compose up -d` had to re-`Create` the network/container from scratch), backend/frontend not running. Brought it all back up: Docker Desktop launched (~10s to ready), `docker compose up -d` (**data survived** — named volume persisted despite the container itself being recreated; `qr_codes` count = 85, `buildings` count = 160, matching the last documented state exactly, so the 2026-08-25 QR-minting work above is still intact and valid), backend (`pnpm run dev` in `server/`) on :3001, frontend (`pnpm exec vite --host` from repo root) on :5173. Confirmed `curl http://192.168.1.163:3001/health` → `{"ok":true}` and `http://192.168.1.163:5173` → 200, both over LAN. Also caught and backfilled a documentation gap while doing this — see the new top entry above (3 real, already-committed-and-pushed commits from later on 2026-08-25 that this file never mentioned).

**Still outstanding / uncommitted**: `server/src/scripts/generate-qr-batch.ts` (new script, untracked) and this todo file's own edits (modified, not yet committed) — same as the "Not done" note in the QR-minting entry below: confirm with the user before committing, per this file's standing convention.

**Resume prompt** — paste this to pick up exactly where we left off:
> Continue from plans/qr-backend-todo.md. Check dev servers are up first (docker ps, curl <LAN-IP>:3001/health, curl <LAN-IP>:5173) and restart them if needed — see "Environment notes" below. **User wants servers reachable over LAN, not just loopback** (so the QR scanner works from a real phone) — start the frontend as `pnpm exec vite --host` (never plain `pnpm dev`, which binds loopback only) and use the machine's actual LAN IPv4 (check via `ipconfig`, was `192.168.1.163` as of 2026-08-26 — re-check, it can change) instead of `localhost` everywhere: browser URLs, curl checks, and if QR images ever need regenerating (`server/src/scripts/generate-qr-images.ts`, see the 2026-08-10 entries below). The live `/kiosk` renders the real full **160**-building city — any older entry in this file referencing "158 buildings"/"500,000 across 158" is stale on the exact count, though the block-budget total (500,000) is unchanged.
>
> **Environment note (2026-08-25):** on this machine `node`/`pnpm` are NOT on PATH by default in either the Bash or PowerShell tool — node lives at `C:\Tools\node-v24.19.0-win-x64\` (add it to PATH first: bash `export PATH="/c/Tools/node-v24.19.0-win-x64:$PATH"`). Docker Desktop is also not on PATH/Program Files — its real install is `C:\Users\mm\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe` (launch that .exe directly if `docker ps`/`docker info` fails to connect; takes ~10-30s to come up, poll `docker info`). Neither was running at the start of this session (Docker Desktop closed, backend/frontend dev servers not started) — check both before assuming servers are just unreachable for some other reason.
>
> **A pending, uncommitted generator fix from the church/fountain/plaza work (2026-08-21) was found sitting un-regenerated at the start of this session** (2026-08-25) — `src/scripts/generateCityLayout.ts` already had a real bug fix (`church_zone` exclusion radius 35→25, because the old radius reached into the *next* BSP leaf north once the church moved far enough north, silently deleting a real road/losing the road at z=207.057) plus a shift-constant refactor (`CHURCH_POSITION_BASE_Z`/`CHURCH_FOUNTAIN_SHIFT`/`CHURCH_ONLY_SHIFT`) written but never re-run, and the todo file itself hadn't been updated past 2026-08-19 despite 5 real commits landing 2026-08-20/21 (`git log` — park/lake spread, lake rotation, stray-tree cleanup, church/fountain/plaza moves). This session ran the regenerator (`node --experimental-strip-types src/scripts/generateCityLayout.ts`), which applied cleanly with no assertion failures. **Committed and pushed as `c44e7a2`.**
>
> **Same day, same session, user then live-tested `/dev/kiosk-progress` and reported the placement camera pointing at "seriously wrong" places.** First pass found and fixed two real, pre-existing bugs in `cityScene.ts`'s `focusOnBlock`/`pickTopViewOffset` (see "Status: two real camera-framing bugs found and fixed via /dev/kiosk-progress" below) — committed as `1c315aa`, **but this did NOT fix the user's actual reported symptom**: the user re-tested and confirmed the camera was *still* pointing at empty floor, not near the building. **The real root cause, found on the second pass**: `PAN_LIMIT` (a hand-maintained camera-pan boundary constant, separate from the generator) was still `280`, left over from before the 2026-08-19 160-building rebalance expanded `CITY_RADIUS` 280→400 — since this soft-boundary clamp runs unconditionally every animation frame (not just during a user pan gesture), it was silently dragging `focusOnBlock`'s correctly-computed `controls.target` back inside a 280-unit circle for any building placed beyond it, which by 2026-08-19 was most of the city. Fixed by raising `PAN_LIMIT` to `450` (matching `CITY_EDGE`, not `CITY_RADIUS`, for safety margin). See "Status: real root cause found — stale PAN_LIMIT constant, not the camera-framing logic" below. **Committed and pushed** — check `git log` for the exact hash if picking this up mid-session. The two `1c315aa` framing fixes are still real, legitimate improvements (verified independently before the `PAN_LIMIT` bug was found) — just not what the user was actually seeing.

## Status: real root cause found — stale PAN_LIMIT constant, not the camera-framing logic (2026-08-25, same day, second follow-up)

User re-tested `/dev/kiosk-progress` after the `1c315aa` camera-framing fixes below and reported the camera was **still** pointing at "the empty floor, not even near the building" — meaning the framing-logic fixes (real bugs, but not this one) hadn't actually addressed what was reported. Went back to first principles instead of assuming the fix landed: re-added temporary debug logging to `focusOnBlock`, reproduced live (`hospital_small_0`'s first two blocks, `house_0`, `food_bank_0`), and this time looked past the framing math (`ox,oy,oz`) at the actual `camPos`/target values.

- **Root cause**: `cityScene.ts`'s `PAN_LIMIT` constant (a hand-maintained "how far `controls.target` may be panned from the origin" soft boundary, unrelated to `generateCityLayout.ts`) was still `280` — set 2026-08-13 to match `CITY_RADIUS` *as it stood then*. `CITY_RADIUS` was expanded 280→400 during the 2026-08-19 160-building rebalance (see that entry further below), but `PAN_LIMIT` was never updated to match — a second instance of exactly the kind of hand-synced-constant drift this file has already flagged once before (`BUILDING_SPECS`/`BUILDINGS`). The critical detail that made this a real bug and not just cosmetic: the clamp runs **unconditionally inside `animate()`, every single frame** — not only in response to a user drag gesture — so any `controls.target` sitting beyond radius 280 gets silently dragged back toward the origin one frame after being set, regardless of who/what set it. Confirmed live: `house_0` sits at world `(-388,-92)` (radius ≈399), `hospital_small_0` at `(-349.5,-165.5)` (radius ≈387) — both comfortably outside the stale 280 limit, both real buildings placed as part of the *same* 2026-08-19 rebalance that moved `CITY_RADIUS` and never touched `PAN_LIMIT`. Because nobody live-tested block placement between 2026-08-19 and this session, this bug shipped silently and sat undetected through 5 more real commits.
- **Fix**: raised `PAN_LIMIT` 280→450 — deliberately matching `CITY_EDGE` (the generator's own terrain boundary, sized with margin to fully contain every real building/road/tree) rather than `CITY_RADIUS` itself, since `CITY_RADIUS` is only the BSP leaf-*center* filter, not the city's true measured extent (the same center-vs-envelope gap the terrain code had already measured and accounted for once before, 2026-08-13 — real building/road/tree geometry can extend somewhat past the nominal radius).
- **Verified live via `/dev/kiosk-progress`** across all 4 categories (Residential/Food/School/Hospital, each freshly clicked from 0): every single one now shows a clean, correctly-centered shot with the newly-placed block clearly visible (bright yellow highlight) and the rest of the building/nearby trees in frame — a dramatic, obvious difference from the "blank grey floor, tiny unrelated speck in a corner" shots seen before this fix. Also checked `/dev/city` (wide establishing view, unaffected — still renders correctly, no exposed void or broken boundary from the raised pan limit) and `/kiosk` (loads clean, code-entry screen unaffected). No console errors anywhere. `pnpm exec vite build` clean. All temporary debug logging removed before finishing.
- **Why this was missed on the first pass**: the two `1c315aa` framing-logic bugs (tree-clearance math, neighbor clamp) were real and independently reproducible/verifiable via debug logging + screenshots, and fixing them produced visibly different (and individually correct-looking) camera math — easy to mistake for "the fix" without checking whether the reported symptom specifically was gone. Lesson applied here: re-test the *exact* user-reported symptom after a fix, not just "the bugs I found," before declaring it resolved.
- **Not exhaustively re-verified**: walked a handful of blocks per category, not all 160 buildings across the full 0-to-complete range. `PAN_LIMIT=450` is a reasoned choice (matches `CITY_EDGE`'s own established safety margin) but not derived from an exhaustive scan of every real building's exact radius — worth a quick sanity check if a building near the very edge of the city still shows anomalies.

## Status: two real camera-framing bugs found and fixed via /dev/kiosk-progress (2026-08-25, same day, follow-up)

User tested `/dev/kiosk-progress` right after the church-zone fix above landed and reported the camera "pointing to the wrong place" on block placement — described as seriously broken, not a minor framing nitpick. Reproduced live immediately: `house_0`'s very first foundation block (0/1080) framed dead-center on two roadside tree canopies, no building geometry visible at all; `food_bank_0`'s second block (2/4000) framed as an extreme close-up clipped *inside* a neighboring building's solid wall. Traced both via temporary debug logging in `focusOnBlock` (removed before shipping, same pattern as every prior camera-bug session in this file) plus a standalone script (`scratchpad/check-trees.mjs`, not part of the repo) cross-referencing `house_0`'s real position against `cityRoads.ts`/`cityDecor.ts`/`cityTerrain.ts`'s tree arrays.

- **Confirmed both bugs are pre-existing, not caused by the church-zone fix** — `cityLayout.ts`/`cityRoads.ts`/`cityDecorBuildings.ts` came back byte-identical when regenerated earlier the same session (see the entry above), so no building/road/tree position actually changed. The real cause: 5 layout-changing commits landed 2026-08-20/21 (park/lake spread, lake rotation, tree cleanup) without anyone re-testing live block placement afterward — the last time this camera code was verified live was 2026-08-19, before all of that.
- **Bug 1 — `pickTopViewOffset`'s tree-clearance formula never credited camera height.** Unlike `pickLateralOffset` (which combines XZ distance with `camY - TREE_CANOPY_TOP_Y` via `Math.hypot`, added 2026-08-18 for exactly this reason), the top-view/vertical branch's `clearanceOf` only ever measured horizontal (XZ) distance from a tree to the camera→target sightline. Every candidate's sightline starts at the same (cx,cz) point, so when a tree sits within `TREE_CLEARANCE_RADIUS` (14) of the **target itself** — confirmed live: a road tree only ~1.58 units from `house_0`'s first block — no azimuth or tilt combination could mathematically ever cross the threshold, since the nearest-point-on-segment calculation caps out at roughly the tree-to-target distance regardless of camera position. The code's own comments already described the *intent* ("steeper pulls the camera toward directly overhead... which is what actually gets it out from among the trees") but the formula never implemented it. Fixed by adding the same `camY`/`TREE_CANOPY_TOP_Y` vertical-clearance term `pickLateralOffset` already uses, combined via `Math.hypot` with the existing XZ distance — a steep, elevated candidate now correctly scores much higher clearance than a low one against the same target-adjacent tree, matching the design intent that was already written down. This is a genuinely tight case (tighter than the ~2.9-unit worst case that prompted `LATERAL_LIFT_DEG`'s 2026-08-18/19 tuning) — the fix measurably improves it (steep, elevated, legible establishing shot instead of a tree filling the entire frame) but doesn't make the tree invisible; not exhaustively re-tuned against every building's real tree spacing, consistent with this file's usual "tune by feel" caveat.
- **Bug 2 — the neighbor-building distance clamp was skipped entirely for vertical/top-view shots.** That exemption (added 2026-08-18, "Vertical shots skip this: their horizontal displacement is only sin(TOP_VIEW_TILT_DEG)*FOCUS_OFFSET (~8 units), comfortably inside typical building gaps") was reasoned around the *original single fixed* 22° tilt. It silently broke when `TOP_VIEW_TILT_LOW_DEG=55°` was added the same day for low blocks: `sin(55°)*22 ≈ 18` units of horizontal reach — more than double `ROAD_GAP=8` — nobody revisited the exemption once the low tilt existed. This is exactly what caused `food_bank_0`'s second block to fly the camera into a neighboring building. Fixed by removing the `isVertical` exemption entirely — the clamp now runs for both branches unconditionally (it already floors safely at `controls.minDistance` and only engages when a real neighbor is found within `FOCUS_OFFSET`, so there's no downside to always running it).
- **Verified live via `/dev/kiosk-progress`** (fresh tab, `pnpm exec vite build` clean beforehand and after): `food_bank_0` block 2 now frames a clean steep-overhead shot with the placed blocks clearly visible/highlighted, no more wall-clipping. `house_0` block 4 frames a legible establishing shot (road, trees off to the side, building context visible) — confirmed the fix generalizes past the one specific tight-tree case. `school_0` blocks 1-4 all produced sane, non-degenerate camera math (reasonable `camPos`/`dist` values via debug logging) even though the target's own small foundation block wasn't always visually distinguishable against grey road at screenshot resolution — a separate, pre-existing, minor cosmetic highlight-visibility question, not the reported bug (camera position/target math itself was correct in every case checked). No console errors on `/dev/kiosk-progress` (fresh reload) or `/kiosk`. All temporary debug logging removed before finishing.
- **Not exhaustively re-verified**: same caveat as every prior camera-framing entry in this file — walked a handful of blocks across all 4 categories, not all 160 buildings' real tree/neighbor spacing. Worth another look if a similarly dramatic camera break gets reported again, but the two specific mechanisms found here (target-adjacent tree in the vertical branch; neighbor clamp skipped for low-tilt vertical shots) are now fixed at the root, not just patched for the one reported case.

## Status: pending church-zone radius fix regenerated + dev environment recovered (2026-08-25)

Picked this up cold via the resume prompt above. `git status` showed 6 modified files already sitting in the working tree from a previous session that was never closed out: `generateCityLayout.ts` had a real, already-written bug fix (see the resume prompt above for the mechanism) that had never been re-run, so the data files hadn't caught up to it, and this todo file's own "most current" entry was 6 days stale (last dated 2026-08-19; `git log` showed 5 more real commits after that, through 2026-08-21, none reflected here).

- **Dev environment was fully down** — Docker Desktop not running (`docker ps` failed to connect to the npipe), backend (`server`) and frontend not started, `node`/`pnpm` not even on PATH in a fresh shell. Found the real install paths (`C:\Tools\node-v24.19.0-win-x64\`, `C:\Users\mm\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe` — see the resume prompt above, now documented so this doesn't need re-discovering) and brought everything up: Docker Desktop launched and ready in ~10s, `docker compose up -d` (postgres container, previously exited 3 days ago) started clean, backend (`pnpm run dev` in `server/`) came up on :3001, frontend (`pnpm exec vite --host` from repo root) came up on :5173 bound to all interfaces. Confirmed `curl http://192.168.1.163:3001/health` → `{"ok":true}` and `http://192.168.1.163:5173` → 200, both over the real LAN IP (unchanged from 2026-08-19: `192.168.1.163`).
- **Regenerated the city layout** (`node --experimental-strip-types src/scripts/generateCityLayout.ts` from repo root) to apply the pending fix. Ran clean, no thrown assertions (`Placed 160 buildings, no overlaps`, terrain/mountain-gap checks all passed). **Real diff was small and confined to the landmark files, as expected** — `git diff --stat` after regenerating shows only `staticCityData.ts` (21 lines — `CHURCH_POSITION`/`FOUNTAIN_POSITION` z-values updated to match the new shift constants) and `cityFurniture.ts` (2 lines — `plaza_church_block`'s z nudged +8 to match `CHURCH_FOUNTAIN_SHIFT`) actually changed content; `cityLayout.ts`, `cityDecorBuildings.ts`, and `cityRoads.ts` all came back byte-identical to what was already committed (git still flags them as "modified" — pure CRLF line-ending noise, the same false-positive this file's terrain entry already documented once before) — confirming the church-only shift and the radius bug fix, while real, didn't end up flipping any leaf's buildable/road-contributing status on this particular run; only the two landmark-position files needed to change.
- **Verified live via `/dev/city`** (screenshot, zoomed on the church): church sits with its back wall close to (not overlapping) the road/tree line behind it — the "deliberately tight ~1.6 unit clearance" the fix's own code comment describes — fountain and the tan `plaza_church_block` disc sit together directly in front of the church entrance, no overlaps, no gaps that read as broken. No console errors on `/dev/city` (fresh reload) or `/kiosk` (loads to the code-entry screen cleanly, city renders blurred behind it as usual). `pnpm exec vite build` clean.
- **Backend not reseeded** — this fix only touches landmark (church/fountain/plaza) position data, never the 160 real QR-linked buildings' identity/counts, so the existing dev DB/QR tokens from the 2026-08-19 reseed are still valid and were left untouched. `GET /api/buildings` spot-checked or reachable, real building rows unaffected.
- **Not committed yet** — small diff (`staticCityData.ts` + `cityFurniture.ts`, plus the already-real-but-unapplied `generateCityLayout.ts` changes from the prior session). Confirm with the user before committing/pushing, per this file's standing convention.

## Status: city rebalanced to 160 buildings + decoration buildings + city expansion (2026-08-19, same day, second follow-up)

Large, multi-file change. User wants a bigger building-count rebalance (`plans/qr-backend-todo.md`'s block-budget invariant — 500,000 total, split evenly across the 4 categories — stays exactly the same, just redistributed differently across variants/counts) AND a brand new concept: the city should no longer start completely empty of buildings. Researched via 3 parallel Explore agents (BSP/terrain mechanics, decoration-building safety, backend seed sync) + 1 Plan agent before implementing — see the approved plan for the full reasoning; this entry covers what actually shipped and what was verified.

- **New real (QR-linked) building table — 160 buildings, still 500,000 blocks, each of the 4 categories summing to exactly 125,000**: house 75×1,080 (unchanged), short_apartment 7×4,000 (was 23), tall_apartment 2×8,000 (was 12), food_bank 10×4,000 (was 11), restaurant 17×5,000 (was 14), school 25×5,000 (was 13), hospital_small 12×4,000 (was 5), hospital_medium 10×6,100 (was 4×6,000 — see next bullet), hospital_large 2×8,000 (was 1). Updated in `src/scripts/generateCityLayout.ts`'s `BUILDING_SPECS` and `server/src/scripts/seed.ts`'s `BUILDINGS` (still hand-synced duplicates, no automated check exists — kept identical on variant/count/totalBlocks/order).
- **Medium hospital +100 blocks (6,000→6,100)**, independently verified before touching the real file: a standalone reimplementation of `generateMediumHospital()`'s exact `ifill`/`ishell`/`islab` logic confirmed a solid 5×4×5 rooftop mechanical/AC enclosure at the roof's clear top-left corner (x=1-5, z=1-5 — clear of the central tower at x=6-11/z=4-13, the roof cross at x=8-9/z=8-9, and the antenna at x=9,z=9) adds exactly 100 voxels with zero coordinate collisions. Applied to the real `generateMediumHospital()` in `src/components/city/buildingGenerators.ts`, then verified live via `/dev/preview` (Medium Hospital variant now shows "6,100 blocks", placed to 6100/6100, rooftop enclosure renders correctly). **A second hardcoded duplicate was found and fixed while verifying**: `src/app/pages/DevPreview.tsx`'s own `VARIANT_OPTIONS`-style list had its own independent `totalBlocks: 6000` for the dropdown/slider (unrelated to `blueprintForVariant`, which correctly ignores this param for hand-authored generators) — the dev tool's label/slider max was silently stale until this was caught and fixed too.
- **New decoration-only buildings** (`src/data/cityDecorBuildings.ts`, generated by a new second, lenient placement pass in `generateCityLayout.ts` right after real buildings are placed and BEFORE bushes/lamps/benches/plazas generation — placed before furniture specifically because furniture reads each block's leftover shelf-cursor state, so furniture generated afterward correctly treats decoration buildings' claimed space as no-longer-available instead of scattering into it). Reuses the exact same proven `completedBlocks === totalBlocks` + never-backend-linked-id pattern the church/fountain landmarks already use (`STATIC_LANDMARKS` in `staticCityData.ts`), just generated in bulk (138 total) instead of 2 hand-picked coordinates, prefixed `deco_` so ids can never collide with a real `buildingId`. 138/138 placed exactly on the first successful run (`assignAndPlace`'s new `strict: false` mode logs+skips instead of throwing on an unfit job — real buildings' call site is untouched/still hard-throws). Mix: house 50, short_apartment 20, tall_apartment 10, food_bank 8, restaurant 12, school 10, hospital_small 10, hospital_medium 12, hospital_large 6 — chosen so tall_apartment's real+decoration total (12) matches the *pre-rebalance* real-only total exactly (skyline density preserved despite real tall_apartment dropping to 2), residential's decoration share (58%) runs above real buildings' residential share (52.5%) per "focus on residential slightly more," and total city fill lands at ~46% "already built" (138/(160+138)) — deliberately just under half per "don't fully populate." Wired into the 3 existing `STATIC_LANDMARKS` consumer sites (`Kiosk.tsx:250`, `DevKioskProgress.tsx:116`, `DevCityPreview.tsx:29`) via a new `STATIC_DECOR_BUILDINGS` export from `staticCityData.ts`.
- **City physically expanded to fit the ~doubled total building load** (160 real + 138 decoration ≈ 298 vs. the old 158): `CITY_RADIUS`/`ROOT_HALF_SIZE` 280→400 (≈√2×, doubling buildable area), `MAX_SPLIT_DEPTH` 6→7 (keeps average BSP leaf size/grain close to the original instead of leaves silently doubling in size against the unchanged `MAX_BUILDINGS_PER_BLOCK=6` per-leaf cap). First run: 91 buildable leaves (up from 46), buildings-per-block max 5/median 2 (comfortably under the cap of 6) — real buildings placed exactly 160/160, decoration 138/138, `verifyNoOverlaps` clean on both passes combined.
- **New `ReservedZone` kind `"landmark"`** added for the church and fountain (`church_zone`/`fountain_zone`, radius 35/15 at their exact `CHURCH_POSITION`/`FOUNTAIN_POSITION` coordinates) — a real risk flagged during planning and fixed proactively rather than reactively: church/fountain are hand-placed entirely outside `generateCityLayout.ts` and were never excluded from the buildable-leaf pool before, so growing the BSP grid could plausibly have let a real or decoration building land on top of one. `rectIntersectsAnyZone`/`leavesToRoads` don't filter by zone kind (so exclusion + road-boundary behavior works identically to park/lake zones), but `parkZones`/`lakeZones`'s kind-specific filters correctly exclude `"landmark"` zones from park/lake rendering — confirmed live afterward (zoomed screenshots on `/dev/kiosk-progress`) that nothing overlaps either landmark.
- **Terrain bands re-tuned** using the documented "run, read the thrown assertion's measured envelope, scale, rerun" process (not pre-guessed): first run with the old `CITY_EDGE=350` threw immediately with a measured envelope of 438.1 (also fixed `measureCityEnvelope()` itself while doing this — it was only walking `placed`, the real buildings, not `decoPlaced`; now walks both). New `CITY_EDGE=450` (preserving the original's ~2% headroom ratio), every other band (grass buffer, rolling hills, distant mountains, fog, ground rim) scaled by the same 450/350=1.2857× ratio to preserve today's relative band widths exactly. Second run passed clean: `Terrain clears city: min terrain radius 450.3 (CITY_EDGE 450)` — the "0.3 over the threshold" margin matches the *original* (pre-change) run's own "350.3 over 350" pattern exactly, confirming this is how the terrain sampler always lands relative to `CITY_EDGE` by design, not a new razor-thin-margin problem.
- **Verified end-to-end**: `vite build` clean throughout. Generator's own build-time assertions all passed (`verifyNoOverlaps`, exact-count check for the 160 real buildings, `verifyTerrainClearsCity`, mountain-horizon-gap check — still 3 gaps ≥35°, 30.9% coverage). Live on `/dev/city`: whole-city screenshot shows all 300 buildings (160 real + 138 decoration + 2 landmarks) rendered fully-built, organic/non-broken layout, church and fountain both clearly visible with nothing overlapping either. Live on `/dev/kiosk-progress` at 0/500,000: decoration buildings (multiple hospitals, apartment towers, houses, a school) render fully-built even though every category shows 0/125,000 — confirms the actual "city doesn't start empty" effect the user asked for. Real building placement re-tested through the dev tool (hospital_small_0 → 1/4000) with no console errors and no visible effect on any nearby decoration building. **Backend reseeded** (`docker compose down -v && up -d`, fresh `migrate`+`seed`) — `/api/buildings` confirmed 160 rows matching the new per-variant counts exactly, zero progress. **Real end-to-end QR flow re-verified** post-reseed: `POST /api/scan/:token` → `POST /api/enter` → `POST /api/place-block` against a real seeded token placed a real block (hospital_small building id 137, 1/4000) — confirmed live on `/kiosk` (loads cleanly, code-entry screen renders over the new expanded city, no console errors).
- **Comment/doc-string cleanup**: every meaningful (non-coincidental-coordinate) `"158"` reference across `src/` and `server/src/` updated to `"160"` — `generateCityLayout.ts`'s module header, `buildingGenerators.ts`'s 3 church/fountain "not one of the 158" comments, `staticCityData.ts`'s header, `Kiosk.tsx`, `DevCityPreview.tsx`, `DevKioskProgress.tsx` (both comments and the user-facing card description string), `server/src/routes/buildings.ts`.
- **Not committed yet** — this is a large diff (`generateCityLayout.ts` + all 5 regenerated `src/data/city*.ts` files + `cityDecorBuildings.ts` new + `buildingGenerators.ts` + `DevPreview.tsx` + `staticCityData.ts` + 3 consumer files + `server/src/scripts/seed.ts` + `server/src/routes/buildings.ts`). Confirm with the user before committing/pushing.
- **Not independently re-verified**: an automated FPS/perf measurement (attempted via a `requestAnimationFrame` loop through the browser automation tool — timed out, likely Chrome throttling `rAF` in a background/non-focused tab, not a real app problem) wasn't obtained; screenshots/interactions throughout this session's testing were visually smooth with no lag observed, but this is not a numeric confirmation. The decoration count/mix (138 total, the per-variant table above) and the `CITY_RADIUS=400`/`MAX_SPLIT_DEPTH=7` starting values are treated as "tune by feel" starting points per the approved plan, not derived/exhaustive — worth a closer look/adjustment once seen across more live sessions, same as most of this file's other placement constants.

## Status: lateral tree-dodge still failed for a target-adjacent tree — extended LATERAL_LIFT_DEG (2026-08-19)

User reported a tree still covering a freshly-placed block at `hospital_small_0` block 204/4000, despite the lateral tree-dodge (2026-08-18 entries below) having been verified live already. Reproduced directly via `/dev/kiosk-progress` (set Hospital to 203, "Add next block") — confirmed the report: the camera landed with tree canopy filling almost the entire frame.

- **First traced which branch actually fires for this block**, since it wasn't obvious from the report alone: `findExposedFaceNormal` checks occupancy against the block's *full blueprint* position (not construction-so-far) — block 204 (`hospital_small_0` block index 203, 0-indexed) sits at local (x=0, y=1, z=3), the bottom row of the west wall ring. Both its vertical neighbors (y=0: foundation slab, y=2: same wall ring one layer up) are occupied in the full blueprint, so `isVertical` is false — this is a **lateral (wall)** shot, not the top-view/vertical branch the two most-recent entries below had just finished tuning. (Also made an unrelated but real improvement to `pickTopViewOffset` first, before tracing this — added a steeper-tilt fallback candidate set mirroring `LATERAL_LIFT_DEG`'s escape hatch, since a low block's shallow tilt has the same "azimuth alone can't clear a tree-ringed building" limitation the lateral branch already had a fix for. Didn't cause or fix this particular report, but is a real gap closed while in the area.)
- **Added temporary debug logging** (`window.__lastLateralDebug`, stashing each candidate's measured clearance) directly in `pickLateralOffset`, reproduced live again, and read it via the browser console — found the real cause: a tree sits only **~2.9 units from the target's own center** (`nearby` list showed `{x:-229,z:-55,dist:2.92}` against a target at `(-227.5,-57.5)`), closer than any case the 2026-08-18 lateral-dodge tuning had actually been verified against. At that distance, `LATERAL_LIFT_DEG`'s existing 35° step measured clearance 0.04-0.95 across all 5 azimuths (nowhere close to `TREE_CLEARANCE_RADIUS=14`) — swinging azimuth alone can't do anything when the tree is nearly co-located with the target itself, matching the exact limitation the 35° lift step was originally added for, just further along the same curve.
- **Extended `LATERAL_LIFT_DEG` from `[0, 35]` to `[0, 35, 60, 80]`**, tuned against the live debug numbers rather than guessing: 60° only just crossed the threshold (~14.6 of the required 14, chosen by `pickClearestOffset` but still visually mostly hidden in a screenshot — the clearance formula's xz/vertical combination can cross a numeric threshold while still reading as occluded up close). 80° (`cos(80°)·22≈3.8` horizontal / `sin(80°)·22≈21.7` vertical) pushed clearance to ~17 and, verified with a fresh screenshot, the block was genuinely visible at the frame edge instead of fully buried.
- **Verified live via `/dev/kiosk-progress`**: re-ran the exact reported case (Hospital → 203 → Add next block) after the fix — the newly-placed (yellow-highlighted) block is now clearly visible beside the tree rather than hidden behind it. Walked ~40 more blocks across all four categories (hospital block 204→224, residential house_0 up to block 20, food_bank_0 to block 10, school_0 to block 10) checking for regressions — every case showed a clean, unoccluded shot (several with trees clearly visible off to the side rather than blocking), no console errors on `/dev/kiosk-progress` throughout, `/kiosk` reloaded fresh with no console errors either. `vite build` clean.
- **Removed the temporary debug logging** (`window.__lastLateralDebug`) before finishing — was diagnostic-only, not left in the shipped code.
- **Not exhaustively re-verified**: the 60°/80° steps were tuned against this one specific close-tree case plus the ~40-block regression sweep above — not walked against all 158 buildings' real tree spacing. Also unconfirmed: whether an even closer tree (sub-2-unit) exists anywhere in the real layout that 80° still wouldn't clear — none was found in this session's sweep, but the sweep wasn't exhaustive.

## Status: top-view tilt now height-dependent — closer to the ground until a block surpasses tree height (2026-08-18, same day, fourth follow-up)

User liked the steep top-view tilt (`TOP_VIEW_TILT_DEG=22°`) in general, but asked for low blocks specifically to frame closer to the ground/more horizontal instead of the same steep near-overhead angle used for a tall rooftop shot, transitioning to the current steep angle once the building actually surpasses tree height — the same "reads better once it's tall enough" idea already driving the tree-dodge work above, just applied to the tilt itself rather than only azimuth/lift.

- **`pickTopViewOffset` (`cityScene.ts`) now takes `cy`** and computes its tilt per-call via a new `topViewTiltComponents(cy)`: smoothstep-interpolates from a new `TOP_VIEW_TILT_LOW_DEG=55` (shallower/more horizontal, starting point for live tuning) at `cy=0` up to the existing `TOP_VIEW_TILT_DEG=22` once `cy` passes `TREE_CANOPY_TOP_Y` — reusing the exact same "surpasses the tree" threshold `pickLateralOffset`'s lift dodge already uses, so both pickers agree on what "tall enough" means.
- **Refactor to support this**: `TOP_VIEW_VERTICAL`/`TOP_VIEW_HORIZONTAL` (previously fixed module-level constants baking a single tilt into `TOP_VIEW_DIRECTIONS`) are gone — `TOP_VIEW_DIRECTIONS` renamed `TOP_VIEW_AZIMUTHS` and reworked to store pure unit-circle directions (steepness no longer baked in, since it now varies by height), with the actual per-candidate offset (`[ax·horizontal, vertical, az·horizontal]`) built at call time from `topViewTiltComponents`. `pickClearestOffset`'s "prefer default" convention (candidates[0]) is untouched — index 0 is still due south at whatever tilt applies for that call.
- **Verified live via `/dev/kiosk-progress`**: `school_0` blocks 1-16 (all still below `TREE_CANOPY_TOP_Y=6`) now frame at a clearly more horizontal, ground-level angle — trees visible at roughly the same screen height as the block instead of looking down from well above them. Set School's counter directly to 4991/5000 (a roof-level block) via the input field and confirmed the framing returns to the original steep near-overhead angle, showing the flat roof surface clearly — the transition works at both ends. No console errors on `/dev/kiosk-progress` or a fresh `/kiosk` reload. `vite build` clean.
- **Not yet tuned**: `TOP_VIEW_TILT_LOW_DEG=55` is a reasoned starting point, not derived or exhaustively checked against all 158 buildings' real tree spacing — may need adjustment once seen across more variety.

## Status: tree-dodge fix didn't actually dodge — wrong clearance metric, fixed for both branches (2026-08-18, same day, third follow-up)

User re-tested the lateral tree-dodge below and reported it hadn't actually helped — trees still blocked part or half the screen. Root cause, found by instrumenting `focusOnBlock` with temporary debug logging (removed before shipping) and reproducing the exact broken case live:

- **Real bug #1 — ground-floor blocks never even reach the lateral branch.** `findExposedFaceNormal` checks `VERTICAL_DIRECTIONS` (nothing above / nothing below) *before* lateral — and since no building has blocks at `y < 0`, every y=0 foundation block has "nothing below" and gets classified `isVertical`, routing to `pickTopViewOffset`, not the newly-added `pickLateralOffset` at all. Confirmed directly: `school_0` block 0 logged `normal: 0 -1 0, isVertical: true`. So the user's reported occlusion was happening in the **vertical branch** (the one from the *first* follow-up below, believed already "tested/working") — the lateral fix's own live testing had never actually exercised `pickLateralOffset` in the sessions before this one; every "successful" verification screenshot so far was coincidentally a vertical shot.
- **Real bug #2 — the clearance formula itself was wrong, in both branches.** Both pickers measured distance from a candidate *camera* position to each tree — but a tree standing right next to the *target block* (the common case: buildings sit only a few units from the nearest road tree) reads as `~FOCUS_OFFSET` away from the camera regardless, i.e. always "clear," even though it's squarely in front of what's being framed. This is why the dodge silently never triggered for the actual failure case. Fixed by adding `pointToSegmentDistanceXZ` (standard point-to-segment projection) and measuring each tree's distance to the *whole camera→target sightline*, not just the camera endpoint — applied to **both** `pickTopViewOffset` and `pickLateralOffset`. Strictly a superset of the old check (a tree exactly at the old-detected camera position still measures the same distance), so no risk of un-detecting a previously-caught case.
- **Real bug #3 (lateral only) — azimuth swinging alone can't clear a target-adjacent tree.** Live math check: rotating the lateral camera ±40° around a tree sitting close to the target barely moved its measured clearance, since the sightline's near-target end stays pinned close to the tree regardless of the camera end's angle — a geometric ceiling azimuth can't swing past. Added a second candidate dimension, `LATERAL_LIFT_DEG = [0, 35]`: each azimuth is now also tried with a 35° upward tilt, genuinely raising the camera above canopy height (~13 units up at `FOCUS_OFFSET=22`) rather than waiting for the building itself to grow past it — deliberately reproducing the mechanism the user identified ("stops mattering once the building outgrows nearby trees") instead of only benefiting from it passively. `pickLateralOffset`'s clearance formula now computes `camY` per-candidate (`cy + oy·FOCUS_OFFSET`) instead of reading `cy` once, since candidates can now carry a real vertical component.
- **Verified live, re-testing the exact previously-broken case**: `school_0` block 0 (the one that logged `isVertical: true` and rendered fully hidden behind a tree) now frames cleanly, tree visibly off to the side. Walked `school_0` to block 34/5000 (~30 more placements across Residential/Food/School/Hospital) with dense tree cover in-frame in most shots — every one framed clean, several showing a long unobstructed row of foundation blocks with the dodged tree clearly beside rather than in front of the target. No console errors on `/dev/kiosk-progress` throughout, or on a fresh `/kiosk` reload afterward. `vite build` clean. All temporary debug logging removed before this was considered done.
- **Still not independently re-confirmed**: an actual live trigger of Fix 2 below (the neighboring-building distance clamp) — testing so far kept landing on tree-dodge cases, not a fully-built neighbor close enough to visibly engage that clamp. Also unconfirmed: whether `LATERAL_LIFT_DEG`'s single 35° step is sufficient for every real tree/building combination in the city, or whether it needs more steps/a wider range — shipped as a reasoned starting point per this project's established "tune by feel" pattern, not derived or exhaustively tested against all 158 buildings.

## Status: lateral (wall) camera framing — tree dodge + neighboring-building distance clamp (2026-08-18, same day, second follow-up)

After the top-view (floor/ceiling) tree-dodge below was verified, the user found two more camera bugs by walking real construction forward via `/dev/kiosk-progress`'s "Add next block" buttons (see the `/dev/kiosk-progress` entry further below) — both specific to the **lateral (wall-facing)** framing branch of `focusOnBlock` (`cityScene.ts`), which had zero dodge/collision logic of any kind before this: (1) trees blocking the view of a low block, clearing up once the building outgrows nearby canopy height; (2) the camera sometimes landing "on the wrong side," with an already-built block covering the new one.

- **Investigated before writing code** (two background agents — a research pass, then a design-review pass): checked every hand-authored building shape's protrusions (canopies, awnings, porches) against `FOCUS_OFFSET=22` — all are 2-8 units deep, well inside any building's own footprint, so **self-occlusion by the same building was ruled out**. The real cause of bug 2: buildings are packed only `ROAD_GAP=8` apart (`generateCityLayout.ts:105`), far less than `FOCUS_OFFSET=22` — the lateral camera can fly straight past the street into a *neighboring* building's footprint, whose already-built geometry then covers the shot. For bug 1: `TREE_SPACING=20` and buildings' road-facing walls can sit as little as a few units from the nearest road tree; canopy tops measure ~5-6 world Y (`decor.ts`), matching the user's own observation that the problem clears up after the building's first 1-3 floors (`FH=2-6` depending on variant).
- **Fix 1 (tree dodge):** generalized the existing vertical branch's `pickTopViewOffset` pattern rather than inventing something new. Refactored its "prefer default, else best-clearance candidate" selection policy out into a shared `pickClearestOffset` helper (behavior-preserving — vertical branch's output is unchanged), then added a new `pickLateralOffset` with its own small azimuth fan (`LATERAL_AZIMUTH_FAN_DEG = [0,20,-20,40,-40]`, deliberately much narrower than the vertical branch's full 360° so a lateral shot still clearly reads as "viewing this wall") and its **own** clearance formula rather than sharing the vertical branch's — a real trap found during design: the vertical branch's camera Y is always ~20 units above target regardless of block height, so folding a height term into a *shared* formula would make every vertical shot look permanently "above the canopy" and silently break its already-tuned dodge. The lateral formula's height term (`TREE_CANOPY_TOP_Y = 6`) only works because lateral camera Y always equals the block's own Y — when `cy` is at/below canopy height (the actual bug), it's byte-identical to a plain XZ check; once `cy` climbs past it, clearance grows and the dodge naturally stops triggering — exactly the "stops mattering once the building outgrows nearby trees" behavior being restored, without a special case for it.
- **Fix 2 (neighbor distance clamp):** each `BuildingNode` gained a `bboxXZ` (footprint, ±0.5 padded since block x/z are cube centers) computed once at node creation from the full blueprint. A new closed-form ray/AABB slab test (`rayEntersBBoxXZ`, exact O(1), not step-walking — works for the rotated azimuths Fix 1 produces too) finds the nearest occupied neighbor along the chosen direction; `focusOnBlock`'s lateral branch clamps its distance to stop `NEIGHBOR_CLEARANCE_MARGIN=2` short of it, floored at the scene's existing `controls.minDistance` (1.5) rather than a separate cosmetic minimum (which could push the camera back into the very neighbor it's avoiding in a tight-packed edge case). **Gated on `visibleCount > 0`** — an empty/unbuilt neighbor lot has nothing to occlude, and clamping against it unconditionally would needlessly pull `FOCUS_OFFSET`'s wide 22-unit establishing shot down to ~8 units for most ordinary placements. `STATIC_LANDMARKS` (church/fountain) are correctly *included* in the check, not excluded — they're ordinary `nodes` entries and always fully built, so always a real occlusion risk. Vertical shots were left untouched — their horizontal displacement is only `sin(22°)×22 ≈ 8.24` units, comfortably inside typical building gaps.
- **Verified live via `/dev/kiosk-progress`:** filled Food/School/Hospital to 100% (`Full`, then reset Residential back down) so most of the city sat fully built around a still-empty `house_0`; walked `house_0`'s foundation forward one block at a time via Residential's "Add next block" — every low block (down to block 1) framed cleanly with no tree occlusion despite dense tree cover immediately adjacent in several shots (visually confirmed via screenshots, not just "no crash"). No console errors on `/dev/kiosk-progress`, `/kiosk`, or a fresh reload throughout. `vite build` clean.
- **Not independently re-confirmed with a literal before/after screenshot pair**: the neighbor-distance-clamp fix (bug 2) didn't get a case where a fully-built neighbor happened to sit close enough to `house_0`'s tested blocks to visibly trigger the clamp during this session's testing — the code path was verified by direct review against the exact file state (registration lifecycle, `controls.minDistance`, `STATIC_LANDMARKS`' registration path all confirmed via a dedicated design-review pass before implementation) rather than a live "camera pulled in" screenshot. Worth a closer look if the wrong-side bug resurfaces.

## Status: top-view camera framing — tree-dodge verified live via new dev-only shortcut (2026-08-18)

Closed out the previous entry's open item (below): the steepened tilt + tree-dodge camera code (`focusOnBlock`'s vertical branch, `cityScene.ts`) had only been verified by a standalone math script, never live, because reaching a real roof/floor block through the actual kiosk needs hundreds of real placements. Built the "faster" option that entry suggested instead of grinding out real placements.

- **Added a dev-only shortcut to `/dev/preview`** (`src/app/pages/DevPreview.tsx` only — no production/kiosk code touched): a second panel, "Camera framing test", with a block-index input (defaults to the last blueprint index — the roof/cap block for most variants) and a "Focus camera on block" button that calls `focusOnBlock` directly, bypassing the montage/placement flow entirely. Also a "Mock tree" dropdown (None / South-at-the-default-candidate-spot) that builds a minimal `CityDecor` with one `roadTrees` entry placed at exactly the world position the default south top-view camera candidate would land (computed from the same `TOP_VIEW_TILT_DEG`/`FOCUS_OFFSET` constants `cityScene.ts` uses, so it's guaranteed inside `TREE_CLEARANCE_RADIUS`) — forces `pickTopViewOffset`'s dodge path deterministically instead of hoping a real tree happens to be nearby. Changing the tree option remounts `<City>` (its `key` includes it) since `cityScene.ts` builds the tree census once at scene-creation time.
- **Verified live via `/dev/preview` (House variant, block 1079/1079 — the chimney cap):**
  - **No mock tree:** camera framed the chimney cap from a clearly tilted angle (steeper than the first pass, side faces of the chimney visible, not a flat top-down silhouette) — confirms the 22° steepened tilt renders correctly, matching the earlier math-only verification.
  - **Mock tree at the default south candidate spot:** camera swung to a different azimuth entirely (framing changed, mock tree canopy visible off to the side rather than blocking the shot) — confirms `pickTopViewOffset` actually dodges a real nearby tree when exercised live, not just in the standalone script.
- **No regressions:** `vite build` clean. No console errors on `/dev/preview` (either tree state) or `/kiosk` (reloaded fresh, code-entry screen renders normally — the new dev-only decor/City-key changes don't affect it, since `/kiosk` never touches `DevPreview.tsx`).
- **Not tested:** the lateral (wall) framing path — explicitly out of scope, per the original ask ("side/lateral framing was explicitly left untouched"). Only the top-view/vertical branch needed closing out.

## Status: top-view camera framing steepened + tree-dodging (verified live 2026-08-18, see entry above) (2026-08-17)

User reported the top-down camera focus (used when a freshly-placed block completes a floor/ceiling layer) was hard to notice once decor existed — from straight overhead it blends into the flat rooftop/floor plane and surrounding terrain. Two follow-up asks in the same session: steepen the angle further, and make it dodge nearby tree canopies so a tree never sits right in front of the shot. **Side/lateral (wall) framing was explicitly left untouched both times** — only the floor/ceiling ("top view") branch of `focusOnBlock` in `src/components/city/cityScene.ts` changed.

- **First pass:** replaced the old dead-straight-overhead offset (`[0,1,0]`) with a fixed ~35°-off-vertical tilt (`TOP_VIEW_TILT`), always leaning south (-z). Verified visually via `/dev/preview`'s "simulate placing next block" on a house run to 1080/1080 — the chimney cap (a genuine roof-surface block) rendered from a clearly tilted angle showing the block's side faces, not a flat top-down silhouette.
- **Second pass (same day, user asked for "more steep" + tree-avoidance):** steepened to 22° off vertical (`TOP_VIEW_TILT_DEG`, up from 35°) and replaced the single fixed direction with `pickTopViewOffset()` — 8 candidate azimuths at that same steepness; stays on the default south direction unless a tree sits within `TREE_CLEARANCE_RADIUS` (14 units) of where the camera would land, in which case it swings to whichever azimuth is clearest of trees (checked against `decor.roadTrees`/`parkTrees`/`terrain.hillTrees`/`terrain.bufferTrees`, gathered once per scene into `topViewTrees`).
- **Verified so far:** a standalone script reproducing the exact shipped constants (`scratchpad/test-top-view.mjs`, not part of the repo) confirmed the math — no trees nearby keeps the original south tilt, a tree placed due south correctly triggers a swing to the clearest opposite direction, a far-away tree is correctly ignored, trees crowding 3 sides correctly leave the one open gap chosen. `vite build` clean throughout both passes.
- **NOT yet verified live/visually with real decor.** Tried via the real kiosk (3 real blocks placed this session: house_0 → 6/1080, hospital_small_0 → 16/4000) — both landed in genuinely tree-dense intersections, but both were still early foundation-course blocks, which use the *lateral* (wall) framing path, not the vertical one — confirmed that path is visually unchanged, but never actually exercised the new steeper-tilt/tree-dodging code, since `focusOnBlock`'s vertical branch only fires for a block on the building's *actual finished roof/floor surface* (checked against the full blueprint's real occupancy, not "nothing placed above it yet" during construction) — early-course blocks essentially never qualify. Getting a live rooftop-block hit through the real kiosk would take hundreds more real placements per building, which wasn't done.
- **Next time:** either keep placing real blocks toward a roof (slow, but the most real test) or — faster — build a small dev-only shortcut for jumping straight to a rooftop block near a tree (e.g. extend `/dev/preview` to accept an explicit block index/decor, or add a debug hook) if this needs closing out properly. Don't assume it's broken OR working from the current evidence — it's genuinely unconfirmed, not failing.

## Status: kiosk wired to the real 158-building city (2026-08-13, same day)

The live `/kiosk` previously showed only 4 hardcoded plots (`PLOTS`, one fixed position per category) — the real 158-building layout with parks/lakes/roads/trees/terrain only existed in the dev-only `/dev/city` preview. User asked to wire the real thing into the kiosk, with two explicit questions: (1) will future layout changes be easy to re-apply to the kiosk, (2) can the exact current layout/decor be kept, just unbuilt.

**Key finding, verified directly in code before building anything:** `generateCityLayout.ts` already assigns `buildingId: \`${variant}_${i}\`` (0-indexed within variant), and its own doc comment says this is a "stable key... matches server/src/scripts/seed.ts's (variant, order_index) natural key." The backend's `buildings` table already had `variant`/`order_index` columns populated in exactly that order by `seed.ts`. **The identity link between a frontend layout entry and a backend DB row already existed by design — it just was never wired through the API.** This meant no schema migration was needed; position still never touches the database (same as parks/lakes/trees/terrain always have) — it's joined client-side.

- **Backend (`server/src/routes/buildings.ts`)**: `GET /api/buildings` widened from "only the 4 `in_progress` rows" to all 158, with `order_index`/`status` added to the response shape (both already columns, just not previously selected). No migration.
- **`src/lib/api.ts`**: `BuildingState` gains `orderIndex`/`status`.
- **New `src/components/city/staticCityData.ts`**: extracted the full decor/terrain assembly (parks/lakes/beaches/roads/trees/bushes/lamps/benches/plazas/hill-forest-mountain terrain) plus the two hand-placed landmarks (church, fountain) out of `DevCityPreview.tsx` into one shared module, so both the dev preview and the kiosk render identically and automatically stay in sync with any future generator re-run — **this is what answers question 1**: for pure layout/decor/terrain changes (moving buildings, reshaping parks, retuning hills — everything this session has been doing), regenerating the static data files and redeploying the frontend is enough, zero backend involvement. The one real gap, stated plainly rather than glossed over: if a *future* change adds/removes buildings (changes counts per variant), `BUILDING_SPECS` (`generateCityLayout.ts`) and `BUILDINGS` (`server/src/scripts/seed.ts`) are still two independently hand-maintained arrays needing manual sync — pre-existing, not solved here.
- **`DevCityPreview.tsx`**: refactored to import from the new shared module — no behavior change, just dedup.
- **`Kiosk.tsx`** — the real rewrite, replacing `PLOTS`:
  - Builds `CITY_LAYOUT_BY_ID` (a `Map` keyed by `buildingId`) once, looks up each backend row via `\`${row.variant}_${row.orderIndex}\`` for its real position.
  - **Two correctness bugs caught and fixed while translating the naive "map over all rows" approach**, before they ever shipped: (1) the old `queue[row.category] = row.id` loop iterated ALL rows unconditionally — with only 4 rows (all `in_progress`) that was harmless, but with all 158 loaded it would've set `queue[category]` to whichever row of that category came last in the array, not necessarily the actual active one. Fixed by filtering to `status === 'in_progress'`. (2) the boot-reveal "-1, then replay to catch up" trick previously applied to every row with `completedBlocks > 0` — but a `completed` building also has `completedBlocks > 0`, and nothing would ever add back that missing last block (only the active building per category gets `addBlock` calls), so every finished building would have rendered permanently one block short forever. Fixed by scoping the `-1` trick and the boot-reveal loop to `status === 'in_progress'` only; `queued` buildings pass through as genuinely 0, `completed` ones show their real final count outright.
  - `City.tsx`'s existing reducer (`BLOCK_ADDED` advancing `queue[category]` to the next incomplete building already present in `state.buildings`) needed **no changes** — confirmed by reading it directly. Since all 158 buildings' static data is now loaded upfront, a newly-activated building is already in local state with its real position; only its `completedBlocks` ticks up from 0.
- **Verified end-to-end against the real dev DB** (not just visually): confirmed `GET /api/buildings` returns 158 rows with `orderIndex`; confirmed the join keys (`house_0`, `hospital_small_0`, etc.) match `CITY_LAYOUT` exactly with matching `totalBlocks`; loaded `/kiosk` and saw the real city (lake, roads, parks, church landmark) in the background, no console warnings (meaning every one of the 158 backend rows matched a `CITY_LAYOUT` entry — the warn-and-skip fallback path never triggered); **ran a full real scan → enter-code → school-select → place-block round trip through the actual UI** (scanned an unused QR token via curl to mint a real desktop code, typed it into the kiosk's on-screen input, picked the school, submitted) — the block landed on `house_0` at its real city position (camera correctly focused there, not the old fixed plot) and the UI showed the true DB count (4/1080), confirming the whole chain works, not just the loading step.
- **Answering the user's two questions directly**: (1) yes, easy for the common case (position/decor/terrain regeneration) — zero backend involvement; adding/removing buildings still needs a manual two-array sync, unchanged from before. (2) yes — exact same 158 positions, same parks/lakes/roads/hills/mountains/church/fountain, each building's block count now reflects real DB progress (0 for the 154 not yet started) instead of being hardcoded to fully-built.

## Status: world terrain — grass buffer, rolling hills, distant mountains (2026-08-13, same day, second follow-up)

Follow-up to the reverted mountain ring below. User explained the actual problem: the ring felt "closed off and claustrophobic" — too close, too tall, too discrete. User then specified their own layered redesign (ASCII diagram): city (unchanged) → wide grass/park buffer → low rolling voxel hills → fog → faint distant mountains near the sky, plus a *generous* camera boundary as a backstop, not the main mechanism. Explored via 2 background agents (current rendering budget/pan-limit options, then a full implementation plan) before writing any code.

- **Measured correction, found during planning, load-bearing for everything else:** `CITY_RADIUS` (280) is only the BSP leaf-*center* keep filter — real city geometry (building footprint corners, road-tree positions, road segment endpoints) actually extends out to **~344** (confirmed by measuring the generated data directly: road vertices 343.6, bushes 325.7, road trees 334.6, building footprint corners ~318.6). Every terrain band below is built from **`CITY_EDGE = 350`**, not 280 — using the old 280 would have put grass right on top of the outermost roads.
- **Bands** (`generateCityLayout.ts`'s new "World terrain" section, `SEED+20..25`, never the shared module-level `rng`): city 0-350 (untouched) → grass/park buffer 350-640 → low rolling hills 640-1150 → distant mountains 1350-1550 → ground disc rim at 2000. `scene.fog` widened 300/900 → **800/2400**; `controls.maxDistance` **raised** 1200 → 1600 (deliberately the opposite of the rejected ring's tightening to 800 — openness comes from scale/atmosphere now, not a tight clamp); new hand-rolled camera pan boundary at radius 600 (`PAN_LIMIT` in `cityScene.ts`'s `animate()` — `OrbitControls` has no built-in pan-distance clamp, confirmed by checking its source).
- **Low rolling hills**: a Simplex-noise heightfield (`three/addons/math/SimplexNoise.js`, already bundled with the installed `three` package, zero new dependency; seeded off the existing `RNG`), quantized to 5 height levels (max height 15) over a ~200-unit-wide noise lobe — a ~13:1 width:height ratio, which is what actually reads as "rolling" instead of "peaked" (the rejected ring was close to 1:1). Rendered as one flat-topped voxel "column" `InstancedMesh` per 12-unit grid cell (non-uniform-scaled shared box, not stacked layers) — 12,369 columns this run, 1 draw call. Forest scattered on top (815 trees, reusing `buildVariedTreesMesh` unchanged in structure — `TreeMarker` gained an optional `y`, snapped to each tree's *quantized* column height so trunks sit flush on a terraced step instead of floating/sinking).
- **Grass/park buffer**: flat ground using the existing `buildGrassTexture()`, ~25x sparser vegetation than an actual park (227 trees, 258 bushes — this is buffer space, not another park), plus 8 organic meadow color-variation patches reusing the existing `sampleBlobPolygon` (already used for lakes).
- **Distant mountains — same stepped-pyramid cube technique the rejected ring used (that was never the problem), fixed on what actually was:** placed ~4x further out (1350-1550 vs the ring's 280-390) and generated as genuinely sparse clusters instead of a continuous ring. **Real bug caught by the generator's own build-time assertion on the first run**: an initial version randomly skipped ~30% of 9 evenly-spaced cluster slots and only produced 1 real gap ≥35° (79% angular coverage) — looked plausible but would have read as almost as solid a wall as the rejected ring, just further away. Root cause: peak footprint angular width wasn't accounted for in the slot-spacing math. Fixed by reserving 3 mandatory gaps (45-70°, evenly spaced with jitter) *before* placing any cluster geometry, so "at least 3 real gaps" is true by construction, not by hoping a random skip lands right — re-run confirmed 3 gaps, 32.7% coverage, 5.0° max angular height (comfortably under the ~6° design target, versus the rejected ring's ~16° — the same angular size as the buildings themselves, which is the actual reason it read as a wall). Colors are absolute per-instance values (material left white) lerped toward the fog color at generation time, so peaks stay atmospheric-looking regardless of camera angle or fog state — same trick used for the hills' outer fade and the new ground rim.
- **Ground geometry replaced**: the old flat `PlaneGeometry(800,800)` + square `GridHelper` are gone — a grey inner disc (radius 360, identical color/material to the old ground — the city's own ground doesn't change at all) plus a large gradient `RingGeometry` (355→2000) with a per-vertex color fade (grey → grass → haze → fog-color) and the grass texture. The gradient is what actually removes the visible edge at any camera angle or fog state, not fog alone. **`GridHelper` removed entirely, not just resized** — confirmed with the user first (its own square boundary was part of what read as "the rectangular edge"). Ground/fog/camera changes are unconditional in `cityScene.ts`, so `/kiosk` gets them too even though it passes no `decor` (confirmed via screenshot: blurred kiosk backdrop shows the new grey→grass gradient).
- **New file**: `src/data/cityTerrain.ts` (mirrors the existing per-concern-file pattern — `cityLayout.ts`/`cityDecor.ts`/`cityRoads.ts`/`cityFurniture.ts`). Hill columns are stored as a **flat `[gx,gz,level,...]` number array**, not pretty-printed objects like every other export in this project — at ~12k entries, objects would have made the file enormous; a comment in the generated file explains why.
- **Build-time safety assertions** (in the generator, not just eyeballed): city envelope re-measured from live data and asserted ≤350; every terrain element asserted clear of `CITY_EDGE` (caught a real bug on the first run too — meadow polygon vertices, not just their centers, can extend inward via `sampleBlobPolygon`'s wobble; a meadow center scattered too close to `CITY_EDGE` produced a vertex at 330.9, inside the city floor — fixed by computing the center-scatter margin from the blob's own worst-case wobble radius instead of a guessed constant); mountain horizon gap count (≥3 gaps ≥35°) is a hard throw, not a suggestion.
- **Verified**: regenerated cleanly (`Mountains: 3 horizon clusters, 32.7% angular coverage, 3 gaps >= 35°` / `Terrain clears city: min terrain radius 350.3` / `Terrain: 12369 hill columns, 815 hill trees, 227 buffer trees, 258 buffer bushes, 8 meadows, 21 mountain peaks` all logged), zero diff on `cityLayout.ts`/`cityRoads.ts`/`cityFurniture.ts` (only `cityDecor.ts` flagged by `git status`, confirmed byte-identical to HEAD — pure line-ending noise, not content). `node node_modules/vite/bin/vite.js build` clean. `/dev/city` screenshots at the default camera and after partial zoom-out, plus 3 different rotation angles, all show: city clearly dominant, a spacious curved (non-rectangular) grass boundary with no visible edge anywhere, a smooth color gradient toward the horizon. No console errors on `/dev/city` or `/kiosk`.
- **Not independently confirmed**: a true full-zoom-out (`maxDistance=1600`) screenshot showing the mountains themselves — scroll-to-zoom was unreliable via browser automation this session (a known limitation from the earlier mountain-ring session too), and rotation-drag was used for angle verification instead. The generator's own numeric checks (angular height, gap count/coverage, radius clearance) are solid, but worth a real manual zoom-out pass to confirm the mountains actually read as intended (faint, atmospheric, not a wall) rather than relying on the math alone.

## Status: mountain ring — tried and reverted (2026-08-13, same day follow-up)

User raised a bigger-picture issue: the city sits on a flat 800×800 rectangle against a flat blue sky — sharp terrain edges visible, camera could zoom out (`maxDistance: 1200`) far enough to see the whole thing floating in a void. User's own idea was mountains around the city; built a ring of 40 terraced voxel-cube peaks (same stepped-cube-layer technique as the tree canopies below, building-scale) filling the buffer zone between `CITY_RADIUS` (280) and the ground plane's edge (400), plus tightened `controls.maxDistance` (1200→800) and matched fog color to the sky. Fully implemented and verified working as designed (continuous ring, no coverage gaps, zero impact on building/road layout) — **but the user didn't like how it looked** and asked for it to be removed. Reverted in full: `cityScene.ts`, `types.ts`, `DevCityPreview.tsx`, `generateCityLayout.ts`, and `src/data/cityDecor.ts` are all back to their exact pre-mountain (committed) state; `decor.ts` had the mountain-specific additions (`buildMountainsMesh` and its wiring) manually stripped out while keeping the tree-canopy changes below intact, since both landed in the same uncommitted working tree. Verified via a fresh build + `/dev/city` screenshot: mountains gone, tree canopies still present, no console errors.

**Still unsolved**: the original flat-plane-edge/blue-void problem is back. Before proposing another fix, ask the user what specifically they didn't like (too blocky/chunky against the rest of the scene? wrong color/scale? the idea of mountains at all vs. a different approach like fog/gradient sky/something else entirely?) rather than re-attempting the same visual direction.

## Status: tree canopies rebuilt as voxel-cube blocks (2026-08-13)

User found the tree canopies (spheres/a cone) "bland" and wanted them "made out of voxel blocks... give them dimension," matching the buildings' own blocky look — positions unchanged, trunk untouched, foliage only. Explored via a background agent first (confirmed `decor.ts`'s exact current geometry, tree data shape, instance counts — 390 `PARK_TREES` + 1012 `ROAD_TREES` = 1,402 trees — and that buildings already use the exact reusable pattern needed: one shared `BoxGeometry(1,1,1)` instanced many times, `cityScene.ts:276`). User picked "Minecraft-style chunky" (~8-14 big cubes/tree) over a finer ~20-30-cube "voxel-sphere" option when asked.

- **`src/components/city/decor.ts` only changed** — `TreeType`'s `foliageGeo`/`foliageY`/`foliageScale` fields replaced by a `CanopyShape` (`{cubeSize, baseY, offsets: {dx,dy,dz}[]}`): a small stepped stack of cube-grid offsets per type, hand-authored to roughly match each type's old silhouette/height. Round deciduous and blossom: an 11-cube cross-stack (2 cross layers + 1-cube cap). Bushy: the same cross-stack but only 2 layers, no cap (shorter). Birch: narrow/tall via the offset list itself (not the old non-uniform `foliageScale` stretch, which would've read as slabs rather than blocks on a blocky shape) — 4 layers tapering from a cross down to a single cube. Conifer: a genuine tiered pyramid (3×3 base → cross → single cube), replacing `ConeGeometry`.
- **`buildVariedTreesMesh`**: each type's foliage `InstancedMesh` now holds `bucket.length * canopy.offsets.length` instances (one shared `canopyCubeGeo` reused across all 5 types, same shared-geometry discipline as `cityScene.ts`'s building voxels) instead of one instance per tree. Total added instances ≈1,402 trees × ~10-15 cubes/canopy ≈ 14-18k — trivial next to what buildings already push through identical instancing (up to 500,000 voxels), so draw-call count is unchanged (still ≤10: one trunk + one canopy mesh per type in use) and there's no meaningful perf cost.
- **Added a cheap two-tone shade per cube layer** (`shadeForLayer`, via `InstancedMesh.setColorAt` — no material flags needed, three.js multiplies a Lambert material's base color by instance color automatically): top layer ~12% lighter, bottom layer ~12% darker, middle layers unshaded. Built-in ambient-occlusion-like depth cue on top of the blocky shape itself.
- **Nothing else touched**: `TreeMarker`/`cityDecor.ts`/`generateCityLayout.ts` (tree positions/data), `pickTreeType`/`TREE_TYPE_WEIGHTS`/`hash2D`, trunk geometry, `disposeDecorGroup` (already generically disposes any `Mesh`/`InstancedMesh` it finds via `group.traverse`, needed no changes).
- **Verified:** `node node_modules/vite/bin/vite.js build` clean. `/dev/city` screenshots (wide + zoomed) confirm canopies now read as clearly faceted/blocky cross-shaped clusters (not smooth spheres/cone) with the top/bottom shading visible, all 5 type colors still distinguishable (green/pink/yellow-green/pale-birch/dark-conifer), lake/park/road layout and tree positions unchanged from the pre-change screenshot. `/kiosk` still loads cleanly to the code-entry screen, no console errors on either route.

## Status: ground textures, per-building color variety, and hollow-per-floor conversions (2026-08-12, same day, 7 follow-up commits not previously logged here)

Seven commits landed after the lake_east/beach rework below without a status entry being written at the time — reconstructed from `git log`/`git show` for the record. All confirmed `git diff`-clean against `cityLayout.ts`/`cityRoads.ts` (none of this touches `RESERVED_ZONES` or building positions — it's all per-building rendering/geometry, see [[feedback_building_layout_stability]]).

- **Procedural ground textures (`bf19298`):** parks and beaches switched from flat `MeshLambertMaterial` fill colors to small seeded `CanvasTexture`s — a grass texture (blade strokes, `buildGrassTexture`) and a beach/stone texture (pebble speckle, `buildStoneTexture`), both 128×128 canvases drawn once at scene-build time with a `mulberry32` seeded PRNG (stable across reloads), tiled via `THREE.RepeatWrapping` at a real-world density (`TEXTURE_TILE_UNITS`: 6 units/tile grass, 5 units/tile beach). New `scaleUV()` helper scales each tile's UVs *before* `mergeGeometries` runs (has to happen pre-merge or the repeat count would be wrong once tiles are flattened into one buffer). Added `disposeMaterial()` to `disposeDecorGroup` since `Material.dispose()` doesn't dispose an assigned `.map` texture on its own — would've leaked a texture every scene rebuild otherwise. Same commit also folded in an uncommitted todo-doc update from the prior lake_east session.
- **Per-building color variety, added incrementally across 5 commits, all using the same pattern:** a small fixed palette array indexed by `variantIndex % palettes.length`, where `variantIndex = hashSeed(buildingId)` (new `djb2`-style string hash in `blueprintForVariant.ts`) — so a given building's color is deterministic and stable across sessions/re-renders without needing a stored DB column. `blueprintForVariant()` and `HAND_AUTHORED_DESIGNS` generators' signatures both grew a `variantIndex` parameter to carry this through; `Kiosk.tsx`/`DevCityPreview.tsx` now pass the real `buildingId` in.
  - **Houses (`ab0669b`):** `WALL_PALETTES`/`ROOF_PALETTES`/`TRIM_PALETTES`/`WINDOW_PALETTES` in `houseGenerator.ts` doubled from 5 `StylePreset`s to 10 (added coastal, sunny, burgundy, lavender, charcoal), × 4 style variations each = 40-style pool (`HOUSE_STYLE_POOL`).
  - **Short apartments (`ab0669b`):** 5 wall/roof/trim palettes (`SHORT_APARTMENT_PALETTES`), original red brick kept as index 0.
  - **Tall apartments (`3e97594`):** 5 wall/roof/trim palettes (`TALL_APARTMENT_PALETTES`), original blue-grey kept as index 0 (a terracotta option was swapped for teal after user review before landing).
  - **Food banks (`988c881`):** 3 palettes (`FOOD_BANK_PALETTES`: original tan, industrial grey-blue, produce green).
  - **Restaurants (`988c881`):** 8 brick/chimney color palettes (`RESTAURANT_PALETTES`); the red awning + yellow rooftop sign stay fixed across every variant per explicit user request ("that can stay red") — only brick + matching chimney shade vary.
  - **Schools (`6dd219c`, structurally different from the rest):** not a single wall color — each school splits its 50-wide front/back into 4 vertical color panels (`SCHOOL_PALETTES`, 3 schemes: original tan/blue/gold now paneled, primary colors, warm modern terracotta/mustard/teal/charcoal), roof/foundation/trim drawn from the same palette entry so each scheme reads as one coordinated design.
  - **Small hospital:** deliberately *not* given palette variety in this round — the hollow-conversion commit below explicitly notes it was out of scope ("this change is scoped to hollowing the structure, not adding variety that wasn't asked for").
- **Hollow-per-floor conversion, 2 commits, extending the pattern `generateRestaurant`/the 3-hospital-size shells already used to the remaining solid-fill types** (flagged as a known design-character gap back in the 2026-08-11 "hand-authored designs replace Phase 1 procedural shapes" entry — this closes that gap). All use `ishell`/`islab`/`iwinRow` for a visible ring-wall + interior floor slab per floor instead of a filled solid box; footprints are unchanged in every case (so the already-generated `cityLayout.ts` placements stay valid), heights grow instead, and every generator's exact `totalBlocks` target is preserved and was verified by actually running the generator, not just by arithmetic.
  - **Short/tall apartments (`38c1693`):** short_apartment 20×10 footprint, height 20→32 (4 floors→5 hollow floors), still exactly 4,000. tall_apartment 16×10 footprint, height 50→77 (10→15 hollow floors), still exactly 8,000.
  - **School, food bank, small hospital (`a482a0a`):** school and food_bank's footprint areas (500 and 400) don't divide evenly into uniform hollow floors + a thin 1-voxel cap (verified by exhaustive search), so their foundation/roof caps were thickened instead (school: 3+3 layers; food_bank: 4+3 layers) to shrink the leftover remainder to something a small architectural extra can absorb exactly: school gets a 104-voxel entrance canopy (slab + 2 support columns, protruding into z<0 space the shell never occupies), food_bank gets a 32-voxel awning over its loading-dock doors (same z<0 technique `generateRestaurant`'s awning already used). small_hospital is geometrically identical to short_apartment (same 20×10 footprint/4,000 target) so it reuses that exact solved structure verbatim with hospital colors + the existing red-cross overlay swapped in — no palette added, as noted above. `ishell` itself gained an overload accepting a per-cell color function (not just a flat color), needed so `generateSchool`'s hollow ring could still vary color by x-section.
- **Verified via `/dev/city` and `/dev/preview` (same-day follow-up check):** `/dev/city` top-down/oblique screenshots confirm the grass/beach textures render as a visible fine speckle pattern (not flat fill) on both `central_park` and the `lake_east` beach rect, and the whole-city view shows genuinely varied building colors across houses/apartments/food-banks/restaurants side by side (sage, tan, grey, brick, blue-purple, etc. all visible in one screenshot) rather than one repeated color. `/dev/preview` at full `totalBlocks` for each converted variant confirms exact counts (school 5,000/5,000, food_bank 4,000/4,000, short_apartment 4,000/4,000, tall_apartment 8,000/8,000, restaurant 5,000/5,000) and real hollow structure — distinct floor trim bands + window rows visible per floor (5 for short_apartment/small_hospital, 15 for tall_apartment), school's entrance canopy and central glass arch rendering correctly, food_bank's dock-door/awning geometry intact, small_hospital's red-cross overlay still correctly painted over the now-hollow shell, restaurant's red awning + yellow sign staying fixed while its brick color varies. `/kiosk` still loads cleanly to the code-entry screen with no console errors — confirms no regression from the `blueprintForVariant`/`HAND_AUTHORED_DESIGNS` signature changes. No console errors on any of the three routes.

## Status: lake_east reworked into a full-block beach + big rotated-then-unrotated lake; a second park/road inset bug fixed (2026-08-12, same day follow-up)

Committed as `0913fae`, `7048a40`, `f09b4b1`, `8a109ac` (all pushed).

- **Second park/road inset bug, park_north's west edge (`0913fae`):** the previous fix (drop-if-reversed, see the entry below) only caught a grid cell whose inset *overshot* into a reversed rect — a second case remained where an unrelated edge (this tile's south side) introduced an extra cut point a fraction of a unit inside the true west boundary, splitting it into two bands; only the first band touched the tile's literal original corner and got inset, the second silently rendered at the raw, un-inset boundary. Root fix this time (not a band-aid): `insetParkTiles`'s per-cell loop now computes an *effective* inset line straight from the tile's own true edges (`tile.x0 + inset`, etc.) and clips every band against that line, regardless of how many internal cut points fragment the true edge — replaces the "does this band's raw edge exactly match the tile's original corner" test entirely. User confirmed via the west border's exact expected position (`x ≈ -83.3`, matching `-86.2 + PARK_ROAD_INSET(3)`), verified against the actual regenerated tile data, not just a screenshot.
- **lake_east: trees, lamps, first-pass circular beach (already covered by the "fountain + lamps" entry above, chronologically first this session)** — then reworked further per a reference image showing a hand-drawn lake filling nearly the whole block with a beach that fills whatever's left, not a uniform ring:
  - **Beach is now a rect** (`BeachShape` changed from `{x,z,radius}` circle to `{x0,z0,x1,z1}`, same shape as a park tile), filling lake_east's BSP leaf minus `LAKE_EAST_ROAD_CLEARANCE` (5 units) on every side — rendered in `decor.ts` the same way as a park tile (merged `PlaneGeometry`, not `CircleGeometry`).
  - **Lake is now elliptical + rotatable**, not a wobbly circle — new `sampleEllipticalBlob(rng, cx, cz, radiusX, radiusZ, rotation)` in `generateCityLayout.ts` (independent x/z radii, wobble toned down to ±18%/10% from the original ±22%/12% since the ellipse+rotation combo has less margin to work with). `sampleBlobPolygon` (used by `central_lake`) is untouched.
  - **Sizing/position iterated live with the user, each step verified against the ACTUAL generated `LAKES`/`BEACHES` polygon bounds** (not the theoretical worst-case wobble formula, which turned out far more pessimistic than what the seeded RNG actually produces — e.g. one run's real margins were 5.8–16.4 units where the formula predicted as low as ~3.9): widened (`radiusX` 30→34), rotated 24° "toward east" + both radii scaled 1.125x (34→38.25, 20→22.5) + nudged south — hit a real bug here, where **rotation *sign* matters independently of the visual "which way it's tilted"**: +24° pushed one of the wobble's seeded bumps 1.5 units past the beach's south edge, while -24° (same apparent tilt) cleared every side. User then asked to test rotation=0 (preferred it — reads more upright/axis-aligned) and to nudge the lake "a tiny bit north," which in practice meant reducing the existing south offset (`LAKE_EAST_SOUTH_OFFSET`) from 4 to 2, not literally moving past center.
  - **Final state:** `LAKE_EAST_ELLIPSE = { radiusX: 34*1.125, radiusZ: 20*1.125, rotation: 0 }`, `LAKE_EAST_SOUTH_OFFSET = 2`. Margins against the beach rect: west ~10.6, east ~6.7, south ~7.6, north ~8.0 — all comfortably positive.
- **Verified each step via `/dev/city`** (live 3D, top-down + oblique screenshots) and by reading `LAKES`/`BEACHES`/`PARKS` straight out of the regenerated `cityDecor.ts` (a quick inline Node one-liner each time, not the scratchpad SVG map tool from earlier sessions — faster for a single numeric check like "does the lake fit inside the beach"). No console errors on `/dev/city`; `pnpm`-equivalent build (`node node_modules/vite/bin/vite.js build`) clean throughout.
- **Building/road layout untouched throughout** — `git diff` on `cityLayout.ts`/`cityRoads.ts` empty after every change; only `cityDecor.ts`, `types.ts`, `decor.ts`, and `generateCityLayout.ts` changed (this round didn't touch `buildingGenerators.ts`/`DevCityPreview.tsx` — those were the earlier fountain-landmark entry, not this one).

## Status: fountain + street lamps added near the church, plus a real park/road inset bug fixed (2026-08-12, same day follow-up)

User asked for a second hand-placed landmark (a 3D voxel fountain, same non-QR pattern as the church) near the church, plus street lamps along that block's road-lining trees. Then two more follow-ups: a spotted park/road overlap on the opposite side of the same block, and a reposition + spacing tweak for the new fountain/lamps. `/dev/city`'s building count is now **160** (158 real QR buildings + church + fountain) — expected, not a bug; only the 158 are ever buildable-by-users.

- **Fountain** — `generateFountain()` added to `buildingGenerators.ts` (hand-authored voxel shape: circular stone-rim basin + blue water, raised two-tier pedestal, gold finial — same `makeBlocks`/`islab`/`ishell` toolkit as `generateChurch`), registered in `HAND_AUTHORED_DESIGNS` as `"fountain"`. Hand-placed in `DevCityPreview.tsx` via a new `FOUNTAIN_POSITION` constant, same pattern as `CHURCH_POSITION` — not part of `cityLayout.ts`/the 158 real buildings.
- **Fountain position, corrected twice:** first placed at (-6,118) west of the church (a reasonable read of the user's first reference image); user then asked for a reposition, gave `x=110,z=25` which turned out to be a mis-swapped axis pair landing in an unrelated residential block near `house_28` — caught via a quick nearest-building/nearest-road distance check before moving anything, asked the user to confirm, they clarified they meant `x=25,z=110` (mirrored to the church's east side, axes swapped by mistake). Final position: **(25, 110)**, verified clear of the church footprint, all four of the church leaf's real road edges, and CHURCH_TREE_CLEARANCE via the calibrated SVG map (see below).
- **Street lamps along the church leaf's roads** — new `generateChurchLeafLamps()` in `generateCityLayout.ts`: walks the real road segments bordering the church's own BSP leaf (found via `segmentBordersLeaf`, matching segments lying exactly on the leaf's boundary — the leaf itself is zone-excluded and contributes none of its own edges, so every bordering road here comes from a real, non-excluded neighbor) at the same `TREE_SPACING` interval `generateRoadTrees` already uses, but emits a lamp only once per group instead of a tree every interval. Group size started at 3 trees/lamp per the user's first ask, changed to **2 trees/lamp** per a follow-up ("one lamp between every two trees") — `CHURCH_LAMP_GROUP_SIZE` constant, now 5 lamps along that block. Merged into the existing `LAMP_POSTS`/`lampPosts` output alongside the block-interior lamps `generateLampPosts` already places elsewhere — no new decor field needed.
- **Real bug found and fixed: a park tile was rendering on top of a real road**, on `park_north`'s west cluster (the opposite edge from the church-leaf work above) — user spotted it by eye via a debug 2D map (see below) they'd opened themselves (`localhost:8974/map.html` at the time; scratchpad-only, not part of the repo, not guaranteed to still be running). Root cause: `insetParkTiles`'s per-cell inset math (`classifyEdgeIntervals`/the grid-slicing loop, from the "per-edge park/road inset fix" status entry, 2026-08-11) assumed a sliced grid cell was always at least `PARK_ROAD_INSET` (3 units) wide — a real BSP leaf here has a neighbor whose edge doesn't align exactly with the leaf's own boundary, introducing a genuinely tiny (0.53-unit) slice; insetting its west edge by the full 3 units overshot past its own (un-inset) east edge, producing a reversed rect (`x0 > x1`) that — rather than disappearing — rendered back at the raw, un-inset boundary, i.e. right on top of the road. Fixed by dropping any grid cell whose inset computation overshoots past its opposite edge, instead of keeping the degenerate result. `park_north` went from 5 tiles → 4 (the bad sliver is just gone, not replaced). Worth checking other park edges for the same class of bug if one shows up again — this fix is general (applies to all parks' insetting), not church-specific.
- **Verified via `/dev/city`** (live 3D, both top-down and oblique-angle screenshots) and the same throwaway calibrated 2D SVG map technique from the previous entry (`scratchpad/render_map.mjs`, updated this session to also plot a labeled fountain marker and to stop silently mis-rendering tiles whose `x0/x1` or `z0/z1` come back unsorted — a real bug in the *debug script itself*, not the app; it was hiding the road-overlap tile behind a negative-width SVG rect until fixed, so it's worth re-checking any earlier "looks fine" verification in this map tool with that caveat in mind). No console errors on `/dev/city`.
- **Building/road layout untouched throughout** — `git diff` on `cityLayout.ts`/`cityRoads.ts` empty after every change this session; only `cityDecor.ts`, `cityFurniture.ts`, `buildingGenerators.ts`, `DevCityPreview.tsx`, and `generateCityLayout.ts` changed.

## Status: church kept clear of park_north (west cluster restored, east extension dropped) and its buffer trees (2026-08-12)

User sent three follow-up reference images (screenshots with hand-drawn annotations) refining park_north's shape around the hand-placed church landmark, then a fourth asking to clear its scattered trees. All committed and pushed (`2d2250e`).

- **Root cause:** `parkTilesFor`'s flood-fill (see the "real git repo added" status entry, 2026-08-11) had absorbed the BSP leaf the hand-placed church sits in — plus a further leaf chain extending east past it — since all of that was already zone-excluded territory (over-claimed by `park_north`'s circle, same permissive `rectIntersectsAnyZone` pattern used throughout `generateCityLayout.ts`), even though the church itself sits well outside `park_north`'s actual circle. Net effect: the park rendered green right up to and around the church.
- **First pass (image 1, circled the church "inside" the park):** clipped just the church's own leaf back to a line east of its real footprint (nave half-width 10, +3 buffer), keeping the rest of that leaf's tile as park.
- **Second pass (image 2, labeled "west"/"east" halves):** user's initial read of which side to keep was reversed from the first pass's labels — dropped the whole west cluster, kept only the (bigger) east portion.
- **Third pass (final correction):** user clarified the labels were backwards — restored the original west cluster exactly as it was (never touched, since it wasn't west of any cut to begin with) and dropped park_north's east extension entirely (the leaf chain including the church's own leaf), rather than clipping it. Implemented as `dropParkNorthEastExtension` in `generateCityLayout.ts`: keeps only tiles fully west of the exact BSP leaf boundary (`x ≈ -26.64`) the two clusters share — no partial clipping needed. `park_north` went from 7 tiles (post-flood-fill) → 5 (west cluster only, matching its original pre-church-fix shape).
- **Fourth pass (image 4, red rectangle around the church's block):** `generateZoneBufferTrees` (the "fills bare zone-excluded gaps with trees" generator, 2026-08-11) was still scattering trees across the church's leaf regardless of whether that leaf was ever rendered as park — independent code path, so the earlier tile-dropping didn't touch it. Added `leafContainsChurch(r)` and excluded any leaf containing `CHURCH_POSITION` from `bufferLeaves`. Road-lining trees (`generateRoadTrees`, a separate generator over real road segments) were never affected and stay.
- **Verified each pass via `/dev/city`** (zoomed top-down screenshots) plus a throwaway calibrated 2D SVG map script (`scratchpad/render_map.mjs`, reads `cityLayout.ts`/`cityDecor.ts`/`cityRoads.ts` directly, plots world coordinates with a known scale/offset — built this session to get pixel-exact answers instead of eyeballing the 3D perspective view, since the live camera's azimuth/tilt made naive pixel-measurement against reference screenshots unreliable this time). Not part of the repo — scratchpad-only, regenerate if needed for a future decor request.
- **Building/road layout untouched throughout** — `git diff` on `cityLayout.ts`/`cityRoads.ts` was empty after every one of the 4 passes; only `cityDecor.ts` (park tiles/trees), `cityFurniture.ts` (a few park bench positions shifted since `generateParkBenches` samples within the now-smaller `park_north` tile set), and `generateCityLayout.ts` itself changed.

## Status: parks/lakes reshaped to block-grid polygons + organic blobs, fountains, more park benches, less church-area tree clutter (2026-08-11)

User sent a reference image: parks as angular polygons following the block grid (not circles), lakes as organic pond blobs (not circles), one lake nested inside a park rather than sitting next to it as a separate zone. Plus: too many trees around the church, wants more benches per park, and a fountain roughly in the middle of every park.

- **Parks are no longer circles** — a park's shape is now the union of whichever BSP block tiles its `RESERVED_ZONES` circle overlaps (`parkTilesFor`, reusing the same tile set already excluded from buildings/roads — no new geometry needed). `RESERVED_ZONES` collapsed from 5 entries to 4: `central_lake` was removed as its own zone and is now *derived* from `central_park`'s own tile union (`deriveNestedLake` picks the largest tile, centers a circle in it at 55% of the tile's shorter side) — matches the reference's "lake inside the park" framing instead of an adjacent pair.
- **Lakes are organic blobs, not circles** — `sampleBlobPolygon(rng, cx, cz, radius)`: a circle perturbed by two sine waves of different frequency/phase/amplitude (22%/12%), sampled at 20 points. Used for both the nested `central_lake` and the standalone `lake_east`.
- **Fountains** — one per park (`generateParkFountain`), centered in the park's largest tile; for `central_park` specifically (whose largest tile may be the one holding the nested lake), it's placed in the largest *non-lake* tile, or pushed to the tile corner farthest from the lake if there's only one tile to work with.
- **More park benches** — `generateParkBenches`: 4-8 per park (scales with the park's total tile area), rejection-sampled across its tiles avoiding the fountain and any nested lake, all facing the park's overall center. Merged into the same `BENCHES` export the block-interior benches already used (`cityFurniture.ts`) — no new `CityDecor` field needed.
- **Church-area tree reduction** — `generateZoneBufferTrees` now also skips any candidate point within `CHURCH_TREE_CLEARANCE` (24 units) of the hand-placed church position (`0, 165`), same constant duplicated between the generator script and `DevCityPreview.tsx` (documented cross-reference, not a shared import, to keep the generator DOM-free).
- **Types/rendering:** `CityDecor.parks`/`lakes` changed from `DecorZone[]` (circle) to new `ParkShape[]` (`{id, tiles: Rect[]}`) / `LakeShape[]` (`{id, points: Point[]}`); new optional `fountains?: DecorZone[]`. `decor.ts`: parks render as one merged-geometry mesh per park (one `PlaneGeometry` per tile, `mergeGeometries`, same pattern already used for roads); lakes render via `THREE.Shape`+`ShapeGeometry` traced from the blob's points (local Y negated before the shape is built, since `rotateX(-90°)`'s Y→Z mapping is otherwise mirrored — circles never exposed this because they're symmetric, an asymmetric blob does); fountains render as a small gray base ring + blue water disc. Y-stack extended: parks(0.15) → fountain base(0.17) → fountain water(0.19) → lakes(0.20→0.21, bumped up 0.01 to keep clearance from the new fountain-water layer).
- **This run:** 3 parks (`central_park`=5 tiles, `park_north`=3, `park_south`=2), 2 lakes, 3 fountains, 24 dedicated park benches (on top of the existing 45 block-interior ones), 642 park trees (345 of which fill the park/lake zone-buffer gaps, now excluding the church clearing).
- **Verified via `/dev/city`:** top-down screenshots confirm parks render as irregular block-following polygons (a visible color-boundary against plain ground, following the road grid rather than a circular edge); both lakes render as clearly non-circular organic blobs; the nested lake sits inside `central_park`'s own green tiles rather than next to them; fountains (gray ring + blue disc) visible roughly centered in each park; benches present (small, thin from directly overhead, but confirmed via the generated data and visible as brownish dashes when zoomed). `pnpm build` clean (via `node node_modules/vite/bin/vite.js build` — `pnpm`/`npm` weren't on this shell's `PATH` this session, `vite`'s own `.bin` shim also needed `node` added to `PATH` explicitly). No console errors on `/dev/city` or `/kiosk`; `/kiosk` (which passes no `decor` prop) renders unaffected, confirming the new optional `fountains` field and the `parks`/`lakes` type change don't regress it.

## Status: park-tile over-claiming bug — two parks were visually fusing, church was inside a park (2026-08-11, same day follow-up)

User caught two real bugs by comparing against their reference image again: what should've been 3 visually distinct parks read as 2 (central_park and park_north were fusing into one shape), and the church — meant to sit *next to* a park, not inside one — had a park tile rendering right under it.

- **Root cause (one bug, two symptoms):** `parkTilesFor`'s tile-selection test (`rectIntersectsZone`, "does any part of this BSP tile touch the zone's circle") claims a tile's *entire* area as park the moment the circle clips even a corner of it. BSP tiles are frequently much bigger than a park's radius (a 45-radius `central_park` circle barely reaches into a ~70-90-unit-wide tile), so most of a claimed tile could sit well outside the actual circle. Two concrete instances: a `central_park` tile extended 48 units past the circle's edge, far enough to butt up against a `park_north` tile that itself only grazed *its* circle — two barely-overlapping tiles sharing a boundary, both fully colored green, reading as one fused park. Separately, the church's tile (deliberately placed just outside `park_north`'s circle when the church was positioned, per the earlier "hand-placed church landmark" status entry) got swallowed by this same over-claiming and rendered as park, with the church sitting on top of it.
- **Fix:** new `rectOverlapFraction(rect, zone)` — samples a 6×6 grid of points across the tile and returns the fraction that falls inside the zone's circle. `parkTilesFor` now only claims tiles at ≥35% overlap (`PARK_TILE_OVERLAP_THRESHOLD`); tiles below that stay plain ground (still excluded from buildings/roads via the unchanged, deliberately-permissive `rectIntersectsAnyZone`, and still eligible for zone-buffer trees) — this is exactly the "green (`#7ed957`-style) is reserved for the actual park polygon, not every tile a zone circle merely brushes" distinction from the user's reference image. Falls back to the single best-overlapping tile if nothing clears the threshold, so a small zone (`park_south`, `park_north`) can never end up with zero park tiles.
- **Result this run:** each of the 3 parks dropped from a multi-tile sprawl (5/3/2 tiles) to exactly 1 well-centered tile each — smaller, more accurate footprints instead of the old inflated/bleeding ones. `park_north`'s tile (`x:[-86.2,-26.6] z:[121.9,194.7]`) no longer contains the church (`x:0, z:165` — outside that tile's x-range), sitting ~27 units from its edge instead: next to the park, not in it. `central_park`'s single tile (`x:[-26.6,53.6] z:[-36.1,40.9]`) comfortably still holds the nested lake. Fountain/bench counts unchanged (3 fountains, 24 park benches) — those generators already worked off whatever tile set they were given.
- **Verified via `/dev/city`:** top-down screenshots show 3 clearly separated dark-green rectangles (visible gaps of plain ground + buildings between all three, no shared edges), the church sitting in plain ground next to — not on top of — its nearby park tile, and both lakes still rendering correctly inside their (now smaller, more accurate) park footprints. `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`, no regression.

## Status: central_park/central_lake un-merged (missing block restored), parks enlarged, fountains removed, lakes resized to reference (2026-08-11, same day, third follow-up)

User sent the actual pre-"not round" screenshot alongside the reference image and pointed out three more issues: merging `central_park`+`central_lake` into one zone had silently deleted a whole block of buildings+roads (visible by direct comparison to the old screenshot) and they wanted it back; the 1-tile-per-park fix from the previous entry made parks read as small squares, not the reference's bigger block-following shapes; and they wanted fountains gone entirely and both lakes resized to match what they'd drawn (shape doesn't need to match, just size).

- **Root cause of the missing block:** confirmed by direct comparison — merging `central_park`+`central_lake` into one `radius: 45` zone excluded more BSP tiles than the original two separate, smaller circles ever did (a single big circle balloons past both original circles' edges in most directions). Fixed by reverting to two independent `RESERVED_ZONES` entries again. Getting back the *exact* original layout required recovering the original `central_park`/`central_lake` position/radius precisely, which weren't recorded anywhere — recovered them by pixel-measuring circle centers in the pre-merge screenshot (color-based flood-fill connected-component detection, run via a Chrome-rendered `<canvas>` + `getImageData`, since no PNG-decoding library was available in Node) against the three zones that were *never* touched by any of these edits (`park_north`, `park_south`, `lake_east`), solving a 6-parameter affine pixel↔world transform from those 3 known correspondences, then inverting it onto the measured `central_park`/`central_lake` pixel positions. Result: `central_park` at `(-25, 0)` r28, `central_lake` at `(25, 0)` r23 — distance 50 apart, matching the "nearly touching" note from the original Phase 3 status entry almost exactly (cross-check, not just a coincidence — strong confidence in the recovery).
- **Lake sizes matched to the reference image:** same pixel-measurement technique applied to the reference image's hand-drawn blobs (same base screenshot, same camera framing, so the same affine transform applies directly) — computed each blob's pixel area, converted to world-space equivalent-circle radius. `lake_east` grew from r15 to r32 (the user's redrawn version is much bigger than the original), `central_lake` grew slightly from r20 to r23. Bigger lakes now exclude more BSP tiles than before — an expected, deliberate side effect of the resize, not a bug.
- **"Lake in the park" without deriving the lake from the park:** `deriveNestedLake` is gone — lakes are independent `RESERVED_ZONES` entries again, each rendered as its own `sampleBlobPolygon` blob at its own zone position. To keep the "lake sits inside the park's green" look without that coupling, added `zonesAreAdjacent(a, b)` (true when two zones' circles nearly touch or overlap, by a distance heuristic — not a hardcoded id pair) and changed `parkTilesFor` to pull tiles from *any* zone paired with the park this way, not just the park's own circle. Only `central_park`/`central_lake` end up paired (50 units apart, combined radius 51); the outskirts zones are all 100+ units from their nearest neighbor, so they're unaffected.
- **Parks enlarged (not 1-tile squares):** `PARK_TILE_OVERLAP_THRESHOLD` lowered from 0.35 to 0.15, and `park_north`/`park_south` radii bumped (20→30, 18→26) so more tiles actually clip their circles enough to qualify — tuned empirically by logging each zone's candidate overlap fractions and picking a threshold that pulls in a genuine second tile for `central_park` (0.19 frac) and `park_south` (0.17 frac) without ever coming close to the old fusion bug's territory (nearest other zone is still 100+ units away in every case). `park_north` only ever had one tile touching its circle at all in this BSP layout, so it stays a single (now larger, r30) tile — reference image shows it as a single tall block too, so this isn't a mismatch.
- **Fountains removed entirely:** `generateParkFountain`/`pickFountainPosition`/`FOUNTAIN_RADIUS` deleted from `generateCityLayout.ts`, the `FOUNTAINS` export dropped from `cityFurniture.ts`'s output, the fountain rendering block removed from `decor.ts` (lake y restored to 0.20, no more 0.17/0.19 fountain layers), the optional `fountains?` field removed from `CityDecor` (`types.ts`), and the import/usage removed from `DevCityPreview.tsx`. Park benches stay (`generateParkBenches`, 24 this run) — only fountains were asked to go.
- **This run:** `central_park`=2 tiles, `park_north`=1 tile (r30), `park_south`=2 tiles (r26), `central_lake` r23, `lake_east` r32, 158/158 buildings placed with 0 overlaps, 42 buildable blocks (up from the previous over-exclusion), 24 park benches, 574 park trees.
- **Verified via `/dev/city`:** top-down screenshot shows all 3 parks as clearly separated, appropriately larger green polygons (2 of 3 now genuinely multi-tile); `lake_east` visibly bigger than the nested `central_lake`, matching their relative sizes in the reference; the area around the central park+lake pair is now populated with buildings again (the previously-missing block); church still sits next to, not inside, its neighboring park tile; no fountains anywhere. `pnpm build` clean (`node node_modules/vite/bin/vite.js build`), no console errors on `/dev/city` or `/kiosk`, no regression.

## Status: park/lake enlargements reverted — building layout restored to exact original (2026-08-11, same day, fourth follow-up)

User reported the previous entry's `park_north`/`park_south`/`lake_east` radius increases (done to make parks feel bigger and match the reference lake sizes) excluded even more buildings than before — "even more areas with buildings are missing." Asked to restore the exact original building layout first (per the pre-merge screenshot) and defer park/lake aesthetics to a later pass.

- **Fix:** reverted all 3 radius bumps from the previous entry back to the exact pixel-measured original values — `park_north` 30→20, `park_south` 26→18, `lake_east` 32→15, `central_lake` 23→20 (only `central_park` at r28 was already original and untouched). `RESERVED_ZONES` is now byte-for-byte the recovered original 5-zone config, so `buildableLeaves`/`leavesToRoads` exclude exactly the same tiles they did in the pre-merge screenshot, and the deterministic seeded placement reproduces the same building layout.
- **Result:** 46 buildable blocks (up from 42 with the enlarged zones), 158/158 buildings placed, `central_park`=2 tiles, `park_north`=1, `park_south`=1 (back to single-tile — the multi-tile look from the previous entry came from the now-reverted bigger radii, so it's gone too; explicitly deferred along with the rest of the park/lake work).
- **Verified via `/dev/city`:** top-down screenshot at the same camera framing as the original pre-merge screenshot is now near pixel-identical in building positions, road network, and plaza/tree placement — direct visual comparison confirms the restoration. Parks/lakes still render as block-tile polygons/blobs rather than the original circles, left as-is per the user's "we'll deal with lakes and parks later." `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`, no regression.

## Status: parks fixed park-by-park — road clearance, park_north extended, central_lake resized (2026-08-11, same day, fifth follow-up)

User explicitly asked to leave the building layout alone from now on ("don't change the building layout unless i tell you to explicitly" — saved as a standing rule in memory, `feedback_building_layout_stability.md`), then asked for 3 concrete fixes, park by park: (1) `park_south` (smallest, no lake, not near church) — fill its whole block, but stop covering the road; (2) `park_north` (next to church) — also stop covering the road, and make it *longer* (not wider) to fill an empty gap above it; (3) `central_park`+`central_lake` — same road-clearance fix, plus make the lake slightly bigger and slightly closer to the park's center.

- **Road-overlap root cause:** roads render centered on every BSP leaf boundary (`ROAD_WIDTH`=4, so 2 units either side), but a park tile was rendered at its *raw* leaf bounds — always painting `ROAD_WIDTH/2` of green over the road on every edge. Fixed with `insetParkTiles` (new, in `generateCityLayout.ts`): insets each tile's edges by `PARK_ROAD_INSET` (3), *except* an edge shared with another tile in the same park (checked via a substantial-overlap match, `MIN_SHARED_EDGE`=5 — not just any contact) — those stay at the raw boundary so merged/adjacent tiles (central_park's 2-tile L, park_north's new 2-tile column) meet with no seam/gap down the middle. Applied uniformly to all 3 parks.
- **park_north made longer, not wider:** investigated rather than guessed at "the empty space between two building blocks above it" — added temporary debug logging (removed after) cross-referencing `keptLeaves` against `placed` building positions, and found the BSP leaf directly north of `park_north`'s tile (`x:[-80,-27] z:[195,280]`) has **zero buildings placed in it this run** — genuinely empty, sandwiched between two real building blocks (one to its west with 5 buildings, one further east). New `findBorderingLeaf(tile, side, keptLeaves)` finds this leaf generically (biggest-overlap neighbor on a given side), and `park_north`'s tile set now includes it. Crucially this is **rendering-only** — it's a plain lookup against `keptLeaves` (unaffected by `RESERVED_ZONES`), never touches `buildableLeaves`/`blocks`/`assignAndPlace`, so it can't shift any of the 158 buildings (that leaf remains a normal, technically-buildable leaf for any future regeneration — it just happens to be empty right now, which is exactly why painting park over it is safe today).
- **central_lake bigger + closer to center, without touching the building layout:** bumping the lake's real `RESERVED_ZONES` radius/position would exclude different BSP tiles and shift buildings — exactly what the user just asked not to do. Instead added `LAKE_VISUAL_OVERRIDES` (new, `generateCityLayout.ts`): a lake's *rendered* blob size/center can differ from its `RESERVED_ZONES` circle, as long as the bigger/shifted blob stays inside already-park-excluded territory (verified by hand: worst-case blob extent from `sampleBlobPolygon`'s wobble, ~1.34× the nominal radius, checked against the inset tile bounds). `central_lake`: radius 20→22, center `(25,0)`→`(20,1)` (nudged toward its containing tile's centroid). `lake_east` unchanged (not part of this request). `generateParkBenches`/`generateParkTreesInTiles`'s lake-avoidance circles (`pairedLakes`) now go through the same `visualLakeCircle()` helper, so benches/trees avoid where the lake is actually rendered, not its smaller real zone circle.
- **Verified building layout unchanged:** none of the above touches `RESERVED_ZONES`, and everything runs *after* `buildableLeaves`/`blocks`/`placed` are already computed — confirmed by the log line staying identical (46 buildable blocks, 158/158 placed, same extent) to the prior "exact original restore" entry.
- **This run:** `central_park`=2 tiles, `park_north`=2 tiles (was 1), `park_south`=1 tile, all inset off their roads; `central_lake` r22 visual (r20 real zone), `lake_east` r15 (unchanged); 22 park benches (down slightly from 24 — fewer valid non-road spots after insetting).
- **Verified via `/dev/city`:** zoomed screenshots of all 3 parks show a clear gray road strip between every green edge and its neighboring road/building (no more green-over-road); `park_north` reads as one seamless tall shape (no gap at the join between its original tile and the newly-absorbed empty leaf); `central_lake` visibly bigger and shifted toward the park's interior compared to before. `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`, no regression — building count/extent identical to the previous entry.

## Status: real git repo added; parks properly enlarged via connected-component flood-fill (2026-08-11, same day, sixth follow-up)

Two things this round. First, **`git init` + an initial commit** (142 files) — `.gitignore` excludes `node_modules/`, `dist/`, `.env` (kept `.env.example`), `server/qr-exports/` (generated), `.claude/settings.local.json`. No more reconstructing lost state from screenshots; `git diff`/`git status` are now the authoritative way to confirm "did the building layout change" instead of comparing log numbers or pixel-measuring old images.

Second: the previous entry's "extend `park_south` east / `central_park` south" fix used `findBorderingLeaf` to grab a *single* adjacent leaf per direction — but the specific leaves in the direction the user had marked turned out to have real buildings on them (verified by cross-referencing `placed`), which contradicted the "don't touch the building layout" rule. Reported this back with options; user pushed back that the area they'd scribbled had **no** buildings, and reframed the ask more simply: "fill the space between the roads that circle it."

- **Root cause of the mismatch:** turned out the user was right, and the bug was in `parkTilesFor` itself, not in "which direction to expand." A park's own zone circle only claims BSP leaves via `rectOverlapFraction`'s threshold — but `rectIntersectsAnyZone` (the *actual* building-exclusion test, deliberately more permissive) was already excluding several *more* neighboring leaves from `buildableLeaves` that `parkTilesFor` just wasn't rendering as green, because their overlap fraction with the zone's own circle happened to be lower than the "winning" seed tile's. Confirmed by dumping every kept leaf near both parks with its `rectIntersectsAnyZone` status and real building occupancy: `park_south` had a whole second zone-excluded, zero-building leaf sitting right next to it (`x:[-141,-74] z:[-185,-122]`) that was never being rendered; `central_park` had one too (`x:[-92,-76] z:[-76,-16]`, directly south of its west tile). Both are *permanently* building-free (that's what `rectIntersectsAnyZone` guarantees), not just empty by chance this run — painting them green has zero risk to the building layout.
- **Fix:** rewrote `parkTilesFor` from "single best tile, or every tile clearing a fraction threshold" to a proper connected-component flood-fill: seed with the zone's single best-overlapping tile (unchanged — this still stops a park's circle from reaching across a real gap into an unrelated zone's territory, the original fusion bug from two entries back), then repeatedly absorb any *other* kept leaf that's zone-excluded (`rectIntersectsAnyZone`) AND directly adjacent (new shared `tilesAreAdjacent` helper, extracted from `insetParkTiles`'s edge-matching logic) to a tile already in the set, until nothing new joins. This naturally picks up every leaf in a zone's connected reserved-territory island — exactly "fill the space between the roads that circle it," since a zone-excluded leaf never contributes its own road edges either (`leavesToRoads` skips them too), so an unclaimed leaf was always just invisible dead space, never a road.
- **park_north's manual absorption removed:** the previous entry's `findBorderingLeaf`-based special case for park_north (absorbing what was thought to be "a coincidentally empty but otherwise normal buildable leaf") turned out to *also* be genuinely zone-excluded territory — the new flood-fill picks it up on its own, so the manual step became redundant and was producing a duplicate tile in the output. Removed it, and removed `findBorderingLeaf` itself (no longer called anywhere).
- **Result:** `central_park` 2→3 tiles, `park_north` still 3 tiles (now via flood-fill instead of the manual special-case, no behavior change there), `park_south` 1→2 tiles. `git diff` on `cityLayout.ts`/`cityRoads.ts` after regenerating: **zero diff** — confirms the building layout and road network are byte-identical to the committed baseline, not just "same counts."
- **Verified via `/dev/city`:** `central_park` and `park_south` both read as substantially bigger, block-filling green polygons now (matching the user's scribbled reference much more closely), still clearing the road on every true outer edge, no seams at internal joins. `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`.

## Status: ground recolored grey; per-interval park/road inset fix (2026-08-11, same day, seventh follow-up)

Two more things. First, swapped the ground plane from green (`0x6b8f5e`) to a light grey (`0xb3b5b6`, `cityScene.ts`) — kept clearly lighter than `ROAD_COLOR` (`0x555a5e`, `decor.ts`) so roads still read as their own darker strip.

That recolor immediately made a pre-existing park/road bug obvious (parks and roads used to be close enough in tone to mask it): some park edges visibly overlapped their road, others had an unwanted gap before it.

- **Root cause:** `insetParkTiles`'s edge decision was whole-edge-only — "does *some* other tile in this same park share this edge (>5 units)? inset or don't, for the *entire* edge." BSP leaves aren't uniformly sized, so a single park tile edge can legitimately border more than one neighboring leaf: part of it seamlessly joins another tile in the same park (correctly no inset), while the rest borders a real road (needs inset) — a whole-edge decision can only get one of those right. Confirmed by cross-referencing every park tile's 4 edges against real `roadSegments` coverage: e.g. `central_park`'s tile2 had a 60-unit edge where 40 units genuinely bordered a road but was being rendered with zero inset (visible overlap); `park_north`'s tile2 had a 54-unit edge with *no* road at all (it borders the outer city boundary) that was still being inset for no reason (visible gap into nothing).
- **Fix:** replaced the whole-edge test with `classifyEdgeIntervals` — for each edge, walks every real bordering `keptLeaves` entry (not just same-park ones) and produces exact sub-intervals along that edge, each tagged inset (bordering a leaf outside this park — a real road) or not (bordering a same-park tile, or bordering nothing at all past the city edge, where there's no road either way). `insetParkTiles` then slices each tile at every interval boundary on both axes (so a tile with mixed intervals on two different sides still comes out right) and computes each resulting piece's inset independently; a piece's cut edges that aren't part of the tile's *original* boundary (introduced purely by this slicing) never get inset, since they're not real edges.
- **Result:** tile counts went up (`central_park` 3→5, `park_north` 3→7, `park_south` 2→4) since mixed edges now split into multiple pieces — expected, purely a rendering-geometry detail, not a design change. `git diff` on `cityLayout.ts`/`cityRoads.ts`: zero diff, building layout/roads untouched.
- **Verified via `/dev/city`:** zoomed screenshots of the previously-broken edges (central_park's east side, park_north's outer edges) now show a clean, consistent road gap with no overlap and no stray gaps into open field. `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`.

## Status: Phase 3 — parks, lakes, roads, trees (2026-08-11)

User's explicit request: one park+lake pair near the middle (decently sized, not too big), two smaller parks on the outskirts kept apart from each other, one lake on the outskirts (smaller than the central one) by itself. Plus an organic road network — "roads to make this look even more like a city."

- **Reserved zones carved out of the Phase 2 layout** — `src/scripts/generateCityLayout.ts` gained a `RESERVED_ZONES` const (5 hand-placed circles: `central_park` r28, `central_lake` r20 — distance 50 apart, nearly touching; `park_north`, `park_south` — angularly ~90-150° apart from each other and from center; `lake_east` r15, alone) and a rectangle-vs-circle avoidance check (same `ROAD_GAP` clearance used between buildings) wired into both the primary and fallback placement loops. Re-verified: 158/158 placed, zero overlaps with buildings *or* zones.
- **Real bug found and fixed while adding this:** the placement candidate was validated as a float, then `Math.round()`'d before being stored — a candidate accepted a fraction of a unit inside a zone's clearance boundary could round to just outside it, and did (`house_51` failed the zone-overlap check on the first run). Fixed by rounding immediately in `sampleInDisc()`, before any validation — every check now validates the exact integer coordinates that end up stored. This also hardens the building-vs-building check the same way (same class of bug existed there too, just hadn't been hit yet).
- **`src/data/cityDecor.ts`** (parks/lakes/park-interior trees) written by `generateCityLayout.ts` alongside `cityLayout.ts`. Park-interior trees: uniform-in-disc scatter, ~1 per 150 sq units, kept off the outer 15% of each park's radius for a clear grassy border.
- **`src/scripts/generateCityRoads.ts`** (new, run *after* `generateCityLayout.ts` since it reads `CITY_LAYOUT` + the zone centers) — organic (non-grid) road network: minimum spanning tree (Prim's, O(n²), trivial at 163 nodes = 158 buildings + 5 zone centers) guarantees full connectivity with no redundant long detours, then a bounded nearest-neighbor augmentation pass (2 extra edges/node, capped at 2.5× the average nearest-neighbor spacing) adds short edges back in so the network has loops/blocks instead of reading as a bare branching tree. Deliberately not a full Delaunay triangulation — simpler to get right, comparable result for this purpose. Produced 483 road segments; roadside trees generated by walking each segment at 20-unit intervals, offset to both sides, skipping the first/last 6 units near each intersection. Output: `src/data/cityRoads.ts` (`ROADS`, `ROAD_WIDTH` = 4, well inside the 8-unit `ROAD_GAP` so roads mostly clear building footprints, `ROAD_TREES`).
- **Rendering:** new `src/components/city/decor.ts` — `buildDecorGroup(decor: CityDecor)` builds parks/lakes as flat circle meshes (green Lambert / blue semi-transparent Phong), roads as a single merged-geometry mesh (one draw call for all 483 segments, via `three/addons/utils/BufferGeometryUtils.js`), trees as two InstancedMeshes (trunk cylinder + foliage cone) shared across park + roadside trees (~1,800+ instances, cheap). Static, built once, no per-frame cost. Wired in as an optional `decor?: CityDecor` param threaded through `createCityScene` → `CityRenderer` → `City` (captured via ref like `initialBuildings`, so a later re-render with a new decor reference doesn't tear down the scene) → `DevCityPreview.tsx`.
- **Verified via `/dev/city`:** central park+lake pair renders adjacent near the middle with roads radiating out and trees around both; top-down view (drag to orbit + synthetic large-delta wheel events to get past the manual-scroll plateau — `controls.maxDistance` is 1200, comfortably enough, manual scroll just needed a lot of ticks) shows the full road network as a connected mesh with visible loops, not a bare tree; spotted at least 2 additional denser-tree park patches at the expected outskirts locations. Didn't get a clean isolated screenshot of the single small outskirts lake (`lake_east`, r15) specifically — the generation script's own zero-overlap verification and logged zone list (`Parks: 3, lakes: 2`) is the authoritative check there, same rendering code path as the confirmed central lake. No console errors on `/dev/city` or `/kiosk` (kiosk doesn't pass a `decor` prop — confirms the optional-decor plumbing doesn't regress the real flow when omitted). `pnpm build` clean.
- **Known limitations, not fixed:**
  - `ROAD_GAP` (8) and `ROAD_WIDTH` (4) are first-pass values, not tuned by feel against a wider variety of camera distances yet.

## Status: Phase 3 revision — roads replaced with independent curved avenues, not a building graph (2026-08-11)

User feedback on the MST-based road network above: "no need for the roads to connect every building like a network, it's a bit too much... roads between buildings filling the space, straight and slightly curved," with a reference image (a hand-drawn "whimsical city map" — a handful of winding ribbon-like streets crossing the whole scene, buildings sitting in the gaps between them, not wired to a connectivity graph).

- **Roads are now independent** — `generateRoadCurves()` in `generateCityLayout.ts` creates `NUM_ROADS = 6` Catmull-Rom splines (start/end points near opposite sides of the city disc, 2 interior waypoints nudged perpendicular to the start-end line for gentle S-curves), with zero reference to any building position. Old approach (MST + nearest-neighbor augmentation over all 158 building positions, 483 segments) removed entirely, along with the `generateCityRoads.ts` script it lived in — deleted, folded into `generateCityLayout.ts`.
- **Order flipped:** since roads no longer depend on final building positions, they're generated *before* buildings now. Buildings avoid road corridors the same way they avoid park/lake zones (`overlapsAnyRoad`, reusing the same rect-vs-circle `overlapsCircle` check against each sampled curve point).
- **Real problem hit and fixed:** giving roads the same `ROAD_GAP` (8) clearance used between buildings made 6 city-spanning corridors eat a disproportionate amount of area (a 20-unit-wide no-build corridor × 6 roads × ~470 length each), pushing the required city radius out to ~280-290 — past the 512×512 ground plane's ±256 edge, so some buildings would've rendered off the visible terrain. Fixed two ways: (1) added a separate, smaller `ROAD_CLEARANCE` (5, vs `ROAD_GAP`'s 8) specifically for building-to-road spacing — reasoned that `ROAD_GAP`'s job is spacing out discrete obstacles under rejection sampling, which doesn't really apply to a few deterministic corridors; (2) the upfront city-radius formula now solves for road corridor area properly instead of ignoring it — generates roads once at a provisional radius, measures their *exact* total sampled length, then solves a closed-form final radius that accounts for that measured corridor area (no "curviness factor" guessing), and regenerates the roads at the final radius from the same seeded RNG stream (so still fully deterministic, same curve shapes just correctly scaled). (3) Also enlarged the ground plane + grid in `cityScene.ts` from 512×512 to 800×800 as a safety margin, since the RSA placement fallback can still push a hard-to-place building a bit past the theoretical radius estimate.
- **Rendering unaffected** — `src/components/city/decor.ts` didn't need to change at all. Each curve is emitted as a chain of short straight segments following its sampled polyline (297-323 segments total across 6 curves, depending on curvature), and the existing "one merged mesh from many `RoadSegment`s" renderer already handles that as a smooth-looking curve at this resolution — the output *format* (`ROADS: RoadSegment[]`) didn't change, only how it's computed.
- **Verified via `/dev/city`:** top-down view shows 6 gently curving, tree-lined roads sweeping across the whole city (some crossing near the central park+lake pair), buildings genuinely scattered in the gaps between them rather than wired to every road — matches the reference's spirit. Re-confirmed the whole city now sits comfortably inside the enlarged ground plane (extent `x:[-264,267] z:[-260,264]`, well under the new ±400 edge). No console errors on `/dev/city` or `/kiosk`, `pnpm build` clean.

## Status: roads temporarily hidden — user sketching their own reference (2026-08-11)

User wants to hand-draw where roads should go (over a screenshot of the blank layout) rather than iterate on the curve-generation algorithm further. `src/app/pages/DevCityPreview.tsx` now passes empty `roads`/`roadTrees` arrays to `<City decor>` instead of importing `ROADS`/`ROAD_TREES` from `cityRoads.ts` — **buildings/parks/lakes are untouched, not regenerated** (regenerating would have shifted building positions, since they were placed avoiding the old road corridors — deliberately left `cityLayout.ts`/`cityDecor.ts` alone and only changed what the dev preview renders). `src/data/cityRoads.ts` and `src/scripts/generateCityLayout.ts`'s road-generation code both still exist untouched, in case the curve-based approach is wanted again later — this is a rendering-only toggle in the dev preview, not a deletion.

**Next step:** once the user has a hand-drawn road reference, translate it into new `RESERVED_ZONES`-style waypoints/curves (or a different representation entirely, e.g. explicit polylines instead of generated Catmull-Rom splines) and re-wire `DevCityPreview.tsx` back to render them — building positions may need to shift again at that point since they'll need to avoid whatever the new road paths are.

## Status: Phase 3 revision #3 — real block-grid roads, buildings + roads generated together (2026-08-11)

User supplied a hand-drawn reference (screenshot of the blank layout with roads sketched on top): a proper interconnected block network — nearly every building fronting a road, city divided into cells, rounded corners — much denser than the 6-avenue approach. Asked whether it was doable; explained the real tradeoff (retrofitting a dense grid around already-placed buildings would look awkward; the clean way is generating blocks and buildings together, which means building positions shift again). User chose that option explicitly.

**New approach — recursive block subdivision (BSP), replacing the curve-avenue system entirely:**
- `bspSplit()` recursively splits the city area (a 280-half-size bounding square) into two rectangles along the longer axis, at a randomized 38-62% fraction, until each leaf is block-sized (`MIN_BLOCK_AREA` / `MAX_SPLIT_DEPTH`) — same idea as a treemap/slice-and-dice layout. A leaf's center within `CITY_RADIUS` (280) is "kept," giving the city its organic (not square) outline, same technique as the earlier disc-based passes.
- **Roads = every kept leaf's own 4 edges** (`leavesToRoads`) — edges shared between two kept leaves collide on the same dedup key and become one road; edges bordering a discarded leaf are still added once (by the kept side), which is what forms the outer boundary road with zero extra bookkeeping.
- Blocks that a park/lake zone overlaps are excluded from the buildable pool entirely (`rectIntersectsAnyZone`) — that space belongs to the zone, not shelf-packed around it.
- **Buildings are assigned to blocks and placed in one pass, not two** — `assignAndPlace`/`tryShelfPlace` simulates real shelf packing (sort tallest-first within a block, lay left-to-right, start a new row when full) *at assignment time*, committing a job to a block only once it's been proven to actually fit there. An earlier two-phase version (greedy area-based assignment, then a separate packing pass) kept producing blocks that looked fine by total area but couldn't actually be packed (e.g. a 40-wide food_bank forcing its own near-empty row an area budget hadn't accounted for) — simulating the real operation during assignment removed that whole class of failure.
- **Real rounding bug found and fixed:** two buildings placed exactly `ROAD_GAP` apart in continuous math ended up ~0.5 units *closer* than that after each independently rounded to an integer coordinate — confirmed via a real failure (`restaurant_0`/`house_35`, 19 units apart in z against a required 19.5). Fixed with a dedicated `PLACEMENT_GAP = ROAD_GAP + 2` used only for spacing *decisions* (row/column gaps, block insets), while `verifyNoOverlaps` still checks the final rounded output against the true `ROAD_GAP` — the 2-unit margin absorbs up to ~1 unit of combined rounding error with room to spare.
- Also fixed a block-sizing bug along the way: forcing extra splits to fix an elongated block's aspect ratio (`MAX_ASPECT_RATIO`) could produce a leaf *smaller* than `MIN_BLOCK_AREA` if the split fraction was unbalanced — fixed by rejecting any split whose smaller child would fall under `ABSOLUTE_MIN_LEAF_AREA` (2000), computed from the actual child rectangles rather than an area-ratio heuristic.
- `MAX_BUILDINGS_PER_BLOCK` (6) caps per-block density independent of the area math, since shelf packing wastes real space once a block mixes very different building widths.
- **Result:** 66 BSP leaves → 55 kept (within radius) → 46 buildable (9 excluded for park/lake overlap) → all 158 buildings placed, 0 overlaps with each other or any reserved zone, extent `x:[-268,262] z:[-270,248]` (comfortably inside the 800×800 ground plane). 195 road segments, 1192 roadside trees.
- **Verified via `/dev/city`** (roads re-enabled in `DevCityPreview.tsx`, reverting the previous "blank canvas" change): a real interconnected block grid, buildings organized in tidy rows within each cell, roads running throughout with trees lining them, park/lake zones correctly excluded from the buildable pool. No console errors on `/dev/city` or `/kiosk`, `pnpm build` clean.
- **`generateCityRoads.ts` stays deleted** — roads are generated inline in `generateCityLayout.ts` again (this time because building placement genuinely depends on knowing where they are, not just the other way around).
- **Known simplification, not done:** road corners are sharp, not rounded like the reference sketch — filleting BSP-grid corners is extra geometry work not attempted in this pass.

## Status: Phase 3 revision #3 follow-up — two real bugs fixed same day (2026-08-11)

User reported, after reviewing revision #3: buildings clustered by category again ("all tall buildings on one side along with restaurants, most hospitals very close by, schools way too close and clustered together"), and roads cutting through the park/lake zones (explicitly unwanted).

- **Category clustering — real bug, same root shape as the original Phase 2 bug, different mechanism.** `assignAndPlace` sorts jobs by footprint area (`width*depth`) descending for placement-robustness reasons (oversized buildings need first pick of blocks). But every instance of a variant shares the *exact same* width/depth, so that sort collapses all of them (e.g. all 13 schools) into one contiguous run — and "prefer whichever eligible block currently has the fewest buildings" then fills nearby blocks in array order for that whole run, clustering it geographically. Fixed by tracking, per variant, the centers of blocks already used for it, and ranking candidate blocks primarily by distance to the *nearest* existing same-variant block (farthest wins), with "fewest jobs so far" demoted to a tiebreaker — direct analogue of the `sameVariantMinDist` soft constraint the original disc-based placement used, expressed as a ranking instead of a threshold-with-fallback since block counts are small enough that an exhaustive rank is cheap.
- **Roads through parks — real bug.** `leavesToRoads` decided whether a leaf contributes its 4 edges as roads based only on `keepBlock` (within the city radius) — it never checked whether a park/lake zone overlapped that leaf, even though building placement already excludes those leaves from the buildable pool (`rectIntersectsAnyZone`). Fixed by applying the same exclusion to road generation: a zone-overlapping leaf contributes no edges of its own, but its non-overlapping *neighbors* still contribute theirs — including the edge they share with the excluded leaf — so the zone ends up bounded by a road on every side that borders a real block, with nothing crossing its interior. Same mechanism that already forms the outer city-boundary road (leaves outside `CITY_RADIUS` were already excluded the same way), just applied to a second exclusion reason.
- **Re-verified via `/dev/city`:** 46 buildable blocks (down slightly to 167 road segments from 195, since zone-overlapping blocks no longer contribute edges), hospitals/apartments/schools visibly spread across the whole city instead of clustered, roads clearly routing around the central park+lake pair and the other zones with no segments crossing through them. `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`.

## Status: z-fighting fixed, block-interior street furniture added (2026-08-11)

User's two follow-ups: (1) parks/lakes/roads got "glitchy" the more the camera zoomed out — z-fighting; (2) fill the empty space left inside blocks after shelf-packing, without rearranging buildings, to make the city feel more alive. Planned in plan mode first (`C:\Users\mm\.claude\plans\two-questions-the-parks-rustling-quiche.md`), user picked **more trees & bushes, street lamps, benches, small plazas/courtyards** from a shortlist before implementation.

**Z-fighting root cause:** linear (non-log) depth buffer with `near=0.5, far=4000` (8000:1 ratio) wastes nearly all depth precision on close-up range, and on top of that the ground/grid/road/park/lake all sat within a 0.01-0.04 unit y-band of each other (roads and the grid were literally both at `y=0.02`) — smaller than the depth buffer's precision error once zoomed out toward `maxDistance=1200`. Fixed in `cityScene.ts`: `logarithmicDepthBuffer: true` on the renderer (the real fix), near plane `0.5→1` (harmless — nothing renders closer than `minDistance=1.5`), grid `y: 0.02→0.05`. Fixed in `decor.ts`: road `y: 0.02→0.10`, park `y: 0.03→0.15`, lake `y: 0.04→0.20` (plaza, added same session, sits at `y=0.12`). Re-screenshotted at max zoom-out — clean, no striping/moiré artifacts.

**Block-interior filler decor** — all computed from each block's final shelf-packing cursor state (`BlockState.rowZ + rowDepth` marks the top of the last packed row; everything above that, up to the block's inset top edge, is `blockLeftoverRect`, a new helper in `generateCityLayout.ts`) — purely additive, buildings never move:
- **Bushes** (`generateBushes`): uniform scatter in each leftover rect, ~1 per 60 sq units, capped at 8/block. Rendered as a single flattened-sphere `InstancedMesh` (reuses `buildTreesMesh`'s instancing pattern). Written to `cityDecor.ts`'s `BUSHES` export (same foliage family as `PARK_TREES`).
- **Lamp posts** (`generateLampPosts`): one row along the leftover rect's long axis, 15-unit spacing, random rotation per lamp for visual variety.
- **Benches** (`generateBenches`): up to 2 per qualifying leftover rect, angled to face toward the block's own center (toward its buildings) so they read as intentionally placed.
- **Plazas** (`generatePlazas`): only the 6 largest-leftover-area blocks (area ≥ 900, and the block must actually hold ≥1 building) become a plaza — kept rare on purpose, same reasoning as the 5 hand-placed `RESERVED_ZONES`, so they read as deliberate gathering spaces rather than filler on every block. 6 generated on this run.
- Lamp posts/benches share a new `OrientedMarker {x,z,angle}` type (added to `CityDecor` as 4 new **optional** fields — `bushes`/`lampPosts`/`benches`/`plazas` — so `Kiosk.tsx`, which passes no `decor` prop at all, is completely unaffected). Written to a new `src/data/cityFurniture.ts` (kept separate from `cityDecor.ts` — architectural "street furniture," not park/foliage family, same split rationale that already gave roads their own file).
- Rendered in `decor.ts`: `buildBushesMesh`, `buildLampPostsMesh` (pole + head, two-`InstancedMesh` pattern), `buildBenchesMesh` (seat + backrest, same pattern), and a plaza loop matching the existing parks loop (paved stone-gray `CircleGeometry`).
- This run generated: 205 bushes, 114 lamp posts, 47 benches, 6 plazas.

**Verified via `/dev/city`:** bushes filling block gaps, lamp posts visible as small pole+warm-head instances in rows, multiple tan plaza circles scattered across the city distinct from parks(green)/lakes(blue)/roads(gray). `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`, no regression.

## Status: zone-buffer greenery + hand-placed church landmark (2026-08-11)

User sent a screenshot of the blank-canvas layout with hand-drawn marks: white squiggles on several large bare-green patches (asking to fill them), and a purple circle near a small standalone park (asking for a purely-aesthetic church there, explicitly not one of the 158 real/buildable QR buildings).

- **Root cause of the bare patches:** every BSP leaf that a park/lake zone overlaps is excluded from both the buildable pool *and* road generation (`buildableLeaves`/`leavesToRoads`'s `rectIntersectsAnyZone` filter) — correct for keeping buildings/roads out of parks, but it also meant the leftover space in that leaf *outside* the zone's own (usually smaller) circle got nothing at all, reading as genuinely empty. All 4 marked patches matched this pattern exactly (each sits right next to a park or lake).
- **Fix:** new `generateZoneBufferTrees(rng, keptLeaves)` in `generateCityLayout.ts` — scatters trees into every zone-overlapping leaf's rectangle (1 per ~100 sq units), skipping any point inside a zone's own circle (+3 margin). Merged directly into the existing `PARK_TREES` output (no new `CityDecor` field needed) — added 333 trees this run (364 total park-family trees, up from 31).
- **Church:** new `generateChurch()` in `buildingGenerators.ts` — 20×36 stone nave with a stepped-gable roof, an 8×8×26 bell tower protruding from the front with a pyramidal spire and a gold cross on top, arched-look window bands down both sides, a door in the tower face. 8,101 blocks. **Not** added to the 158-building `CITY_LAYOUT`/QR system — added to `HAND_AUTHORED_DESIGNS` (so `blueprintForVariant("church", ...)` works through the normal dispatch) but only ever instantiated as one extra hand-placed `CityBuilding` appended directly in `DevCityPreview.tsx` (position `{x:0, z:165}`, `totalBlocks`/`completedBlocks` read from the blueprint's own `voxelCount` rather than hardcoded, so it can never drift out of sync with the generator).
- **Placement derivation:** couldn't directly pixel-map the user's rotated screenshot to world coordinates, so cross-referenced 4 independent landmarks (the only zone *pair* = central_park+central_lake, the one standalone lake = lake_east, and the two standalone parks = park_north/park_south) against their known world positions to infer the screenshot's rotation, which consistently pointed to the purple circle being just east of **park_north** (`-40, 175, r20`). Added a temporary diagnostic to the generation script to print the actual buffer-leaf rectangles near park_north, picked `{x:0, z:165}` — inside the leaf `x:[-26.6,45.3] z:[92.8,195.3]`, ~41 units from the park's center (safely outside its r20), guaranteed clear of every building and road since the whole leaf is excluded from both.
- **Verified via `/dev/city`:** all 4 previously-bare patches now show scattered trees; church renders correctly (stone walls, dark roof, visible steeple + gold cross) right next to park_north's green circle, distinct from every other building. Building count confirmed 159 (158 + church). `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`, no regression.

## Status: tree variety — 5 spring-themed types replacing the single pine shape (2026-08-11)

User feedback: every tree (park, roadside, zone-buffer) used the same single trunk+cone InstancedMesh pair — reads as "so many pine trees." Asked for at least 5 types, mostly deciduous, spring colors.

- **`decor.ts` rewrite:** removed `buildTreesMesh` (one shared cone shape for every tree), replaced with `buildVariedTreesMesh` — 5 `TreeType` definitions (`buildTreeTypes()`): round deciduous spring green, flowering/blossom soft pink, fresh yellow-green deciduous, birch (pale trunk + narrow pale canopy), and the original conifer cone kept as a deliberate minority. Weighted `[0.30, 0.20, 0.20, 0.15, 0.15]` (85% deciduous/flowering, 15% conifer).
- **Deterministic per-tree assignment** — `hash2D(x, z, salt)` (a standard sine-based position hash) picks each tree's type *and* a 0.85-1.15x scale variation, both purely from the tree's own `(x,z)` — no RNG state to store, no change to the generated data files, same layout always looks the same on reload. Still only ~2 draw calls per type actually present (trunk + foliage InstancedMesh pair, bucketed), not one per tree — 10 meshes total at 5 types, same performance class as before.
- **Verified via `/dev/city`:** pink blossom trees, multiple green shades, pale birches all clearly visible and mixed together across parks/roadsides/zone-buffer areas; conifers still present but visibly a minority. `pnpm build` clean, no console errors on `/dev/city` or `/kiosk`, no regression. No generation script changes — this was a rendering-only change, `cityDecor.ts`/`cityRoads.ts`'s tree position data is untouched.

## Status: Phase 2 — real placement layout generated (2026-08-11)

User's zoning decision: **organic (not grid-snapped), mixed categories (not zoned/clustered by category)**.

- **`src/scripts/generateCityLayout.ts`** — one-off codegen script (run via `node --experimental-strip-types src/scripts/generateCityLayout.ts` from repo root, not part of the app bundle). Mirrors `server/src/scripts/seed.ts`'s `BUILDINGS` array (same 9 variants, counts, `totalBlocks`, insertion order) plus each variant's real footprint size — width/depth read off `buildingGenerators.ts`'s own local consts for the 8 hand-authored variants, and measured directly off `house-blueprint.json`'s x/z extent (14×12) for house. Deliberately does **not** import anything from `src/components/city/` — `utils.ts` (which `buildingGenerators.ts` depends on for `GLASS_HEX`) computes a canvas texture at module-load time via `document.createElement`, which crashes under plain Node; footprint dimensions are small enough to just duplicate as constants in the script instead of pulling in the whole rendering stack.
- **Placement algorithm:** random sequential adsorption — largest footprint first (packs more reliably; doesn't cause visible category/size clustering in the result since each building's `(x,z)` is independently randomly sampled across the whole terrain regardless of processing order), reject-and-retry against all previously placed buildings' padded AABBs, self-adjusting search radius that grows geometrically on repeated failure (same "solve it automatically rather than hand-tune a bound" pattern as `proceduralBuildingGenerator.ts`'s `computeFloors`). Seeded (xorshift32, seed `20260811`) — deterministic, rerunning reproduces the exact same layout.
- **`ROAD_GAP = 8`** — minimum clearance reserved around every building's footprint (not just between category clusters, since the layout is organic/mixed rather than zoned) for Phase 3's road network to run through. Not tuned by feel yet against actual road/decor meshes — revisit once Phase 3 exists.
- **Output: `src/data/cityLayout.ts`** — literal `CITY_LAYOUT: CityLayoutEntry[]` (158 entries: `buildingId` — stable `${variant}_${i}`, `i` 0-indexed within variant, matching `seed.ts`'s `(variant, order_index)` natural key — plus `variant`, `category`, `totalBlocks`, `x`, `z`). Static data file per the plan (`position is a fixed design decision, not runtime state`), not wired into the live kiosk yet (see "Not done yet" below).
- **Verified:** script's own `verifyNoOverlaps` check (pairwise AABB check across all 158, same padding as placement) passed; placed count matched expected count (158) exactly; extent came out to x/z both `[-183, 183]`, comfortably inside the existing 512×512 ground plane. Visually reviewed via a new dev-only `/dev/city` route (`src/app/pages/DevCityPreview.tsx`, registered in `main.tsx` behind the same `import.meta.env.DEV` guard as `/dev/preview`, confirmed stripped from the production bundle the same way) — renders all 158 buildings fully built at their generated positions via `<City>`. Screenshotted at two zoom levels: categories read as genuinely mixed (houses next to hospitals next to apartment towers, no visible category zoning), no overlaps visible, no console errors on load.
- **Not done yet, worth knowing before relying on this:**
  - **Not wired into the live `/kiosk`** — the real kiosk still only fetches+renders one active building per category (4 total) via `GET /api/buildings`, at the 4 hardcoded `PLOTS` positions in `Kiosk.tsx`. Making the real city show all 158 (completed + in-progress + queued, at their real positions) is separate future work, not yet scoped.
  - **Boundary shape:** placement samples uniformly within a disc, so the city's overall silhouette is circular rather than an irregular hand-drawn-feeling town boundary. Reads fine in the preview but flagged here in case it matters once roads/decor are laid over it in Phase 3.
  - **No rotation:** every building stays axis-aligned (footprint orientation fixed, matching how `buildingGenerators.ts`/`processBlueprint` define voxel coordinates — rotating would mean rotating voxel positions, out of scope here). All buildings currently face the same way.

**Revised same day after user feedback on the first pass:** "schools clustered in the middle, houses all pushed to the outside, 4 food banks bunched on one side." Root cause — the original algorithm placed largest-footprint-first (schools are the single largest footprint, houses the near-smallest) with a search radius that only *grew* as buildings got placed. Same-variant buildings were also processed back-to-back (grouped in the job list), so they all landed within whatever small/stable radius was current at that moment in the run — schools (placed first, 13 in a row) clustered near the center; food banks (placed right after, 11 in a row) clustered wherever the frontier happened to be at that point; houses (smallest, placed last, 75 of them) only ever saw the final, much-larger, mostly-already-full disc, so they got pushed to the outer ring almost by construction. Fixed:
- **Interleaved placement order** (`buildInterleavedJobs`) — round-robins across all 9 variants (higher-count variants like house keep reappearing after lower-count ones run out) instead of grouping every building of one variant together, so consecutive placements are almost always different variants.
- **Fixed full-size radius from the start**, computed upfront from total footprint+gap area (`totalArea / 0.5 / π`) rather than grown organically per-item — every building samples from the same disc regardless of processing order, which is what actually fixes the houses-pushed-to-the-edge effect.
- **Soft same-variant minimum-spacing preference** — each building prefers a candidate position at least `0.6 × sqrt(π·R²/count)` (that variant's "fair share" radius if evenly spread) away from other buildings of the same variant, self-relaxing (falls back to just the hard no-overlap check) if it can't be satisfied late in a variant's run.
- Re-verified: `verifyNoOverlaps` passed, 158/158 placed, extent `x:[-232,230] z:[-233,223]` (comfortably inside the 512×512 ground). Re-reviewed via `/dev/city`: hospitals/schools/food banks now visibly spread across the whole disc rather than clustered, houses appear throughout (including near the center), apartments unaffected (user confirmed those were already good). No console errors.

## Status: hand-authored designs replace Phase 1 procedural shapes (2026-08-11)

User provided a folder ("Updated voxel designs" at repo root) — a separate Figma Make prototype containing hand-authored geometry for the 8 non-house building types (`Updated voxel designs/src/buildingGenerators.ts`), iterated on visually outside this repo. Ported into `src/components/city/buildingGenerators.ts` and wired into `blueprintForVariant.ts`, fully replacing the Phase 1 `PROCEDURAL_CONFIGS`/`generateProceduralBuilding` config-driven approach for those 8 variants (`proceduralBuildingGenerator.ts` itself left in place, unused, same precedent as `generatePlaceholderBlueprint`). House is unchanged — the source folder only restyles the existing house blueprint (recolors), doesn't provide new house geometry.

- **Format adaptation:** source generators output `{x,y,z,type,color?:number}` (color sometimes a bare number, sometimes omitted entirely for window blocks relying on a `TYPE_COLORS` fallback); this project's `BlueprintVoxel` requires `color: string` (hex string like `"0xc05030"`) always set. Adapter converts every color to a hex string at the source, and explicitly resolves omitted window colors to this project's `GLASS_HEX` (`0x90c8d8`) — necessary because 3 of the 8 generators (restaurant, hospital_medium, hospital_large) used a different glass hex (`0x9fe4f5`/`0x5aa8cc`) internally that would NOT have matched this project's exact-color-match glass detection (`resolveColor(b) === GLASS_HEX` in `utils.ts`) if copied as-is — those blocks would have rendered as opaque walls instead of transparent windows.
- **Exact block counts verified before porting** (ran the source generators standalone via `node --experimental-strip-types`): all 8 land exactly on this project's real `totalBlocks` per variant (short_apartment 4,000, tall_apartment 8,000, food_bank 4,000, restaurant 5,000, school 5,000, hospital_small 4,000, hospital_medium 6,000, hospital_large 8,000) — no padding/truncation logic needed, unlike the old procedural generator.
- **Design character note, not fixed, just flagging:** 5 of the 8 (short/tall apartment, food_bank, school, hospital_small) are solid-fill boxes — every interior voxel is a real (invisible, occluded) block, not a hollow shell. This is the exact anti-pattern Phase 1 deliberately moved away from ("a solid fill wastes almost all its budget on invisible interior blocks... a hollow shell with the same budget can be a proper multi-floor footprint" — see the retuned food_bank note below). The other 3 (restaurant, hospital_medium, hospital_large) do use a hollow shell (multi-floor via `ishell`) with more bespoke detail (awnings, chimney, ambulance canopy, helipad, towers). Net effect: the 5 solid-fill designs are visually smaller/more compact per voxel budget than Phase 1's versions were, but have real per-floor window bands; the 3 shell-based ones are comparably tall/detailed to Phase 1. User explicitly asked for this replacement, so treated as their call — not blocking, just noting the tradeoff in case it wasn't obvious from a quick look in Figma Make preview.
- **Verified via `/dev/preview`:** all 8 variants at full `totalBlocks` — each showed the exact `N/totalBlocks` count with no padding, correct palette/silhouette per type (short apartment: brick box with window grid; tall apartment: dramatically taller/denser same palette; food_bank: low wide warehouse with 2 loading-dock doors + clerestory windows; restaurant: brick building with red awning, chimney, yellow rooftop sign, glass rendering correctly despite the different source hex; school: long/low with central entrance; small/medium/large hospital: red cross facade+roof, medium adds a central tower + ambulance canopy + antenna, large adds two staggered towers + helipad, visibly larger in sequence). `pnpm build` clean, no console errors on `/dev/preview` or `/kiosk` (confirms no regression to the real kiosk flow from the `blueprintForVariant.ts` change).

**Phase 3 (not started):** environment decor (trees, roads, lakes) — lightweight non-voxel meshes filling the `ROAD_GAP` space Phase 2 reserves around every building. Purely visual, no gameplay/DB tie-in (confirmed with user).

**Other open items, not yet done (carried over from earlier in the project):**
- Decide the `order_index` sequencing rule for real (still arbitrary/seed order).
- Decide rate-limiting strategy for the shared kiosk (still a per-IP placeholder).
- Deployment to Hetzner (Docker Compose for Postgres there too, `/api` reverse-proxy rule, real prod build).
- No automated tests written yet — only manual curl/browser passes all session.
- Multi-client/multi-tab live sync not built (known gap — mitigation: reload `/kiosk` after any out-of-band DB change, e.g. after running a reset script).
- Several tuning constants (`FOCUS_OFFSET`, `MONTAGE_BLOCK_COUNT`/`MONTAGE_STAGGER_MS`, `HIGHLIGHT_DUR`) were only verified against very low real block counts (a handful of blocks per building so far) — revisit by feel once buildings have real depth.
- QR code images (`server/qr-exports/images/...`) currently encode this machine's LAN IP (`192.168.1.163` as of 2026-08-10) — will break if the network/IP changes, and need regenerating again for the real production domain once deployed. Regenerate via `pnpm exec tsx src/scripts/generate-qr-images.ts "" <new-base-url>` from `server/`.
- Only residential (1,080 codes) and hospital (4,000 codes) have fresh QR codes right now — generate more for food/school via `pnpm exec tsx src/scripts/reset-and-generate-qr.ts <category:count>...` when needed (**this wipes existing blocks/codes** — see the "fresh-start reset" status section below for exact behavior before running it).

## Status: real building designs — Phase 1 started, proof of concept validated (2026-08-10)

Roadmap for the next big chunk of work (real designs for all 9 building types, real placement for all 158 buildings, environment decor) is written up as a 3-phase plan — confirmed with the user: procedural generation (not hand-authored/externally-modeled), one design per building *type* (9 total, not 158 unique), decor purely visual (no gameplay tie-in). Full phase breakdown lives in the approved plan; this note covers what's actually been built so far.

**New `src/components/city/proceduralBuildingGenerator.ts`** — generates a real hollow-shell building (foundation slab, perimeter walls with windows, roof cap) instead of `generatePlaceholderBlueprint`'s solid box. This matters a lot for visual size: the current placeholder box for 4,000 voxels is a cramped solid 16×16×16 cube, since a solid fill wastes almost all its budget on invisible interior blocks; a hollow shell with the same budget can be a proper multi-floor footprint. Floor count is auto-solved from a config's proportions (footprint, floor height) against the target `totalBlocks` (reserves a roof budget, then divides the remainder by perimeter × floor height) — so adding a new variant is just a proportions config, not per-variant trial-and-error to hit an exact count. Voxel count is capped/padded to match `totalBlocks` exactly, same constraint `generatePlaceholderBlueprint` already satisfied.

**Wired up as a proof of concept:** `food_bank` (`blueprintForVariant.ts` — new `PROCEDURAL_CONFIGS` map, checked before falling through to the placeholder box; the other 7 non-house variants still use the placeholder for now). Verified via `/dev/preview`: exact count match (4,000/4,000 shown correctly), real windows (glass-colored, correctly picked up by the renderer's existing glass-vs-solid split), roof cap, gray foundation, and a mid-construction check (1,800/4,000) confirmed the shell is genuinely hollow — interior floor visible through the rising walls, which is exactly the "building going up" look placeholder boxes can't give. No console errors, `pnpm build` clean.

**Retuned per user request (finish one reference example before replicating the pattern).** Original config (`width:24, depth:18, floorHeight:6`) auto-solved to 6 floors / 36 voxels tall on a 24-wide footprint — tower-like, not the warehouse look intended. New config: `width:48, depth:36, floorHeight:6, windowEvery:6` (much larger footprint, sparser/more industrial window spacing) → auto-solved to exactly 1 floor, 6 voxels tall on a 48×36 footprint. Verified via `/dev/preview`: at 4,000/4,000 it reads as a proper low, wide, single-story distribution-center warehouse — big flat roof, one row of windows around the base, correct orange/brick palette. Mid-construction (2,000/4,000) shows the wide foundation slab down first with walls just starting to rise, a sensible "building going up" progression. No console errors, `pnpm build` clean. **`food_bank` is now the finished reference example** — same generator/config pattern (large footprint, low floor count, `windowEvery` tuned per building character) should be replicated across the remaining 7 variants (short/tall apartment, restaurant, school, 3 hospital sizes) next, each needing appropriately different proportions (e.g. apartments should be genuinely tall/multi-floor, hospitals wider/blockier, school long and low).

## Status: Phase 1 complete — all 9 building types now have real procedural designs (2026-08-10)

Added configs for the remaining 7 variants to `blueprintForVariant.ts`'s `PROCEDURAL_CONFIGS` — every real building type now generates through `proceduralBuildingGenerator.ts` instead of falling through to the flat placeholder box. `generatePlaceholderBlueprint` itself is untouched/still available as a fallback, just unused now that all 9 variants have configs.

- **`short_apartment` / `tall_apartment`** (residential, brick + dark-red roof, `windowEvery:3` for a dense residential look): share the same footprint proportions (18×15 vs 22×18) and floor height (5) on purpose — `tall_apartment`'s ~2x voxel budget (8,000 vs 4,000) naturally solves to ~1.7x the floors (17 vs 10) without hand-tuning, so the "short vs tall" contrast falls straight out of the shared math.
- **`restaurant`** (food, same palette as `food_bank`): smaller/more storefront-scaled than the food_bank warehouse (2 floors vs 1) despite being in the same category — reads as a distinct, modest commercial building next to it.
- **`school`** (school, blue walls / dark-gray roof, `windowEvery:3`): deliberately elongated footprint (56×24, not square) — reads as a row of classrooms, not a tower.
- **`hospital_small` / `_medium` / `_large`** (hospital, light gray-blue walls / subtle blue-gray roof accent, sparser `windowEvery:5` for a more clinical wall-heavy look): footprint *and* floor count both scale up together across the three sizes (32×24/4 floors → 36×28/5 → 42×32/6), so they read as a coherent size family rather than the same box just stretched taller.

**Verified all 7 visually via `/dev/preview`, each at its full totalBlocks:** short apartment (mid-rise, window grid), tall apartment (dramatically taller, same palette — contrast confirmed), restaurant (modest 2-story, distinct from food_bank), school (long/low/institutional), and all 3 hospital sizes (visibly bigger than each other in sequence, clinical light-gray look). Every one showed its exact target count in the `X / totalBlocks` readout (4,000/4,000, 8,000/8,000, etc.) — the generator's pad/truncate-to-exact-count mechanism holds across every config. No console errors on `/dev/preview` or `/kiosk` (confirms the shared `blueprintForVariant` change didn't regress the real kiosk flow — its currently in-progress hospital_small building now renders with the real design instead of the old placeholder box, as intended). `pnpm build` clean.

**Phase 1 of the 3-phase roadmap (approved plan) is now done.** Next up is Phase 2: real placement positions for all 158 buildings across the terrain — blocked on a zoning/layout style decision from the user (organic vs. grid, clustered-by-category vs. mixed) before generating real position data.

## Status: corner-block camera framing — found and fixed a second real bug (2026-08-10)

User reported (house, block 337): camera pointed at "the wrong wall" when placing the next block. `src/components/city/cityScene.ts`.

**Root cause:** block 337 is a genuine corner — debug-logged its neighbor occupancy and confirmed **both** `+X` and `+Z` were exposed simultaneously (a wall meeting another wall at 90°). `findExposedFaceNormal`'s `LATERAL_DIRECTIONS` was a fixed compass-order list (`-Z, -X, +X, +Z`), so it always picked whichever exposed direction came first in that arbitrary order — for this block, `+X`, which turned out to be the *short* corner nub, not the long wall the recent construction (and the montage replay, shown from this same fixed camera position) was actually on.

**Fix:** when a block has multiple exposed lateral faces, added a tie-break — new `wallSpanLength()` walks outward along the perpendicular horizontal axis in both directions, counting how many consecutive blocks share the same exposure, i.e. measuring how long each candidate wall actually is. `findExposedFaceNormal` now collects *all* exposed lateral directions and picks whichever has the longer span (falls straight through with no extra work when there's only one exposed lateral direction — the common case).

**Verified:** re-ran block 337 before/after via the same debug-logging approach used for the earlier ceiling-clamp bug — before: `normal=[1,0,0]` (+X, the short nub); after: `normal=[0,0,1]` (+Z). Screenshotted both: the old framing showed a short flat wall with barely any windows in frame; the new framing shows a longer wall with two visible windows — clearly the more substantial, "actually being built" wall. Re-confirmed no regression on the earlier-fixed floor/ceiling case (index 500, house) — unaffected since vertical directions are still checked first, before any lateral tie-break logic runs. No console errors on `/dev/preview` or `/kiosk`. `pnpm build` clean, debug instrumentation removed afterward.

## Status: floor/ceiling camera framing — found and fixed a real bug (2026-08-10)

User (testing via `/dev/preview`'s simulate button) reported the camera framing looked "odd" for some blocks. Two changes, in `src/components/city/cityScene.ts`:

1. **Priority: vertical (floor/ceiling) now wins over lateral (wall) whenever both are exposed.** `findExposedFaceNormal` previously checked lateral directions (front/left/right/back) before vertical (top/bottom) — so a block completing a floor/ceiling layer that *also* happened to sit on a wall perimeter got framed at the wall instead of from above. Split into `VERTICAL_DIRECTIONS` (checked first) and `LATERAL_DIRECTIONS` (fallback) — matches the user's ask: floor/ceiling blocks framed from above so "the building animation is visible," wall blocks framed at the wall.

2. **Real bug: the "ceiling" (bottom-exposed) case was broken by `controls.maxPolarAngle`.** Debugged via direct instrumentation (logged `findExposedFaceNormal`'s output + actual post-`controls.update()` camera position for a real block) and found: for a block with `normal = [0,-1,0]` (nothing below — a "ceiling"), the code positioned the camera *below* the target looking up. But `maxPolarAngle = π/2 - 0.02` forbids the camera from ever going below the target's horizontal plane — so `controls.update()` silently clamped it into a useless, neither-fish-nor-fowl in-between position (confirmed: intended camera Y was -15.5, actual clamped Y came out ~6.94, barely above the target). This — not the priority-order issue — was almost certainly the specific "odd" framing the user saw. **Fix:** both vertical cases (floor *and* ceiling) now always offset the camera *upward* (`[0,1,0]`) rather than literally along the detected normal — matches the user's explicit ask ("camera to point towards them from upwards") for both cases, and stays safely within the existing polar-angle bounds (phi=0, straight overhead, is the *minimum* of the allowed range, never clamped).

**Verified via `/dev/preview`'s simulate button across multiple real cases** (house variant, several counts): index 500 — before the fix, confirmed via logged camera position that it landed in the broken clamped state; after the fix, camera correctly lands at `(target.x, target.y+22, target.z)`, dead overhead, and the screenshot shows a clean bird's-eye view of the floor plan (windows, chimney, brick perimeter all visible from above) — exactly the intended shot. Indices 50 and 250 (both still within the house's ground-floor footprint) also correctly show top-down framing. Switched to Small Hospital (a uniform box, easier to reason about geometrically) at count 2000 — a genuine mid-height interior wall block — correctly shows a flat, straight-on wall framing, confirming the lateral branch still works. No console errors on `/dev/preview` or `/kiosk` (same shared camera code, confirmed unaffected). `pnpm build` clean — output hash matched exactly before/after debug-instrumentation cleanup, confirming no leftover debug code.

## Status: "Simulate placing next block" button added to /dev/preview (2026-08-10)

The dev-only block-count preview page now has a button that replays the exact same sequence a real placement triggers in `Kiosk.tsx` — camera focus, the up-to-100-block construction montage, the pause, then the new block's highlighted reveal — on demand, at whatever count the slider is currently set to.

- **`src/components/city/constructionMontageConfig.ts`** — new shared module: `MONTAGE_BLOCK_COUNT`, `MONTAGE_STAGGER_MS`, `MONTAGE_FINAL_BLOCK_DELAY_MS`, extracted from `Kiosk.tsx` (previously local consts) so the preview's simulate button uses *identical* timing to the real app, not an approximation. `Kiosk.tsx` now imports from here — behavior-preserving refactor.
- **`City.tsx`** — new `addBlockToBuilding(buildingId: string)` on `CityHandle`. The existing `addBlock(category)` resolves a building via the queue and gates on the *React-reducer's* `completedBlocks` — which the preview page's slider deliberately bypasses (it drives the mesh directly via `setVisibleCount`, not dispatch), so that gate would use stale data. `addBlockToBuilding` skips straight to `rendererRef.current?.addBlock(buildingId)` — same underlying call, no queue/category resolution, no stale-state gate — while still firing `onBlockAdded` so React state stays in sync regardless.
- **`DevPreview.tsx`** — new "Simulate placing next block" button, disabled while a simulation is in-flight or once `count >= totalBlocks`. On click: `focusOnBlock(variant.key, count)` → `playConstructionMontage(...)` → (after the shared delay) `addBlockToBuilding(variant.key)` + `count + 1` — the exact same call sequence as `Kiosk.tsx`'s `handleConfirm`, just targeting the synthetic preview building instead of a real one from a `/place-block` response.

**Verified in-browser:** set the house to 300/1080 via the input, clicked simulate — camera snapped close to the wall (matching the real placement framing), and after the full sequence completed the count correctly landed on 301/1080 with the button re-enabled. Confirmed the button correctly disables at 1080/1080 (full house rendered — brick, windows, roof trim all correct). No console errors. `pnpm build` clean, and reconfirmed the new button text is still absent from the production `dist/assets/*.js` (dead-code elimination still holds after this addition).

## Status: dev-only block-count preview tool added (2026-08-10)

New route `/dev/preview` (dev server only — see below) lets you pick any of the 9 real building variants and any block count from 0 to its total, and see it rendered instantly. Purely client-side, never touches the DB/backend.

- **`src/components/city/blueprintForVariant.ts`** — new shared module, extracted from what used to be inline logic in `Kiosk.tsx` (`CATEGORY_COLOR`, house blueprint source/style, the house-vs-placeholder branch). Both `Kiosk.tsx` and `DevPreview.tsx` now import `blueprintForVariant(variant, category, totalBlocks)` from here instead of duplicating it — behavior-preserving refactor, verified `/kiosk` still works identically after the change.
- **`cityScene.ts` → `CityRenderer.tsx` → `City.tsx`** — new `setVisibleCount(buildingId, count)`, threaded through the same pass-through chain as the other imperative methods. Implementation is a one-line wrapper around the existing internal `rebuildNode(node, count)` (already used by `syncBuilding`) — instant, no animation/highlight, cheap for any jump size. This is the right primitive for a slider being dragged live; looping `addBlock()` thousands of times would have been the wrong tool.
- **`src/app/pages/DevPreview.tsx`** — new page. A `VARIANTS` table (variant/category/totalBlocks for all 9 real building types) drives a `Select` + `Slider`+`Input` pair in a corner `Card` over a full-bleed `<City>`. Variant switching remounts `<City key={variant.key}>` (infrequent, fine to be heavier); the slider/input call `setVisibleCount` directly on every change (frequent, needs to be cheap — no React re-render round-trip for the mesh itself).
- **`src/main.tsx`** — route registered as `{import.meta.env.DEV && <Route path="/dev/preview" .../>}`. Verified this actually works, not just in theory: after `pnpm build`, grepped the production `dist/assets/*.js` for `"dev/preview"`, `"Block count preview"`, and the card's description text — zero matches for all three, confirming Rollup dead-code-eliminates the whole branch (and the now-unreferenced `DevPreview` import) out of the production bundle entirely.

**Verified in-browser:** house variant at 1080/1080 renders the full real house (matches `/` demo's shape). Switched to Small Hospital, set count via the numeric input (2000/4000) — correctly showed a half-built placeholder box; dragged the slider directly to 3570/4000 — box grew taller, input field stayed in sync. Both control paths (typed number, dragged slider) correctly drive the mesh via `setVisibleCount`. No console errors on `/dev/preview` (fresh load) or `/kiosk` (confirming the refactor didn't regress the real flow). `pnpm build` clean.

## Status: "under construction" montage on fresh placement (2026-08-10)

When a user *first* places a block (not on return/existing-view scans), the last up-to-100 already-placed blocks on that building now visually re-reveal with a stagger right before the new block appears — purely cosmetic, doesn't touch `placed_at` or any DB state, just makes the moment feel more dynamic. Also fixed lowercase code entry the same session: `input-otp` tests raw keystrokes against `pattern` *before* `onChange` runs, so `CODE_PATTERN` in `Kiosk.tsx` now includes lowercase in its character class (relies on the existing `.toUpperCase()` in `onChange` to normalize).

- **`src/components/city/cityScene.ts`** — extracted the single-block reveal logic out of `addBlock` into a shared `revealBlockAt(node, i, opts?: {highlight?: boolean})` helper (`highlight` defaults to `true`). New `CitySceneHandle.playConstructionMontage(buildingId, count, staggerMs, onComplete?)`: computes `rewindTo = max(0, currentVisibleCount - count)`, instantly hides that range via the existing `rebuildNode`, then replays `revealBlockAt(node, i, { highlight: false })` one block at a time on a `setTimeout` chain `staggerMs` apart — **no yellow highlight** on these replayed blocks, they pop straight to final color; only the genuinely new block (revealed separately via `addBlock`, unchanged, still highlighted) gets the highlight-fade. Doesn't call any `onProgress`/state-sync callback — these blocks were already accounted for in `completedBlocks`. Tracks the pending timer (`montageTimeoutId`) so a new montage or `dispose()` can cancel an in-flight one.
- **`CityRenderer.tsx` / `City.tsx`** — plain pass-through, same pattern as `focusOnBlock`/`resetCamera`.
- **`Kiosk.tsx`** — new `"constructing"` phase (between school-confirm and `"placed"`) with no modal/overlay. `handleConfirm`, on successful `placeBlock`: sets phase to `"constructing"`, calls `focusOnBlock(buildingId, blockIndex)` **first** (camera snaps close-up before the montage starts — see fix below), then `playConstructionMontage(...)`; only once that completes does it call `addBlock()` (reveals the genuinely new block, highlighted) → `setState({phase:"placed"})`. The `"existing"`/return-scan path is untouched — no montage there.

**Two real bugs found and fixed after initial user testing:**
1. **Replayed blocks were getting the yellow highlight too** ("the 100 blocks before... don't need to be highlighted") — root cause was `revealBlockAt` being unconditionally highlighted; fixed via the `opts.highlight` parameter above.
2. **Stagger wasn't visible** ("they all show up at the same time") — root cause was `focusOnBlock` being called *after* the montage instead of before, so the whole staggered replay played out at the wide establishing shot where individual voxels are sub-pixel (same invisibility problem debugged earlier in this doc). Fixed by moving the `focusOnBlock` call to before `playConstructionMontage` starts.

**Verified after both fixes:** real scan→enter→school→confirm cycle — confirmed via an instrumented `highlights.set` patch that exactly one highlight call fired (for the new block only, `origIdx` matching `completedBlocks - 1`), zero for any replayed block. Confirmed the staggering mechanism itself works correctly via a direct diagnostic call to `playConstructionMontage` with a deliberately slow 300ms stagger while polling `solidMesh.count` every 50ms — count grew cleanly 1→2→3→4→5→6 in ~300ms steps, exactly as designed. Note: with real data still only ~7 blocks per building, the actual production stagger (100 blocks/20ms ≈ 2s max) is only a few blocks × 20ms ≈ under 150ms right now — genuinely too fast for a human to perceive at these small counts; this will become properly visible once a building has real depth (dozens+ blocks placed). `pnpm build` clean, no console errors, all debug instrumentation removed afterward.

**Follow-up per user feedback:** added `MONTAGE_FINAL_BLOCK_DELAY_MS = 400` in `Kiosk.tsx` — a pause between the montage's `onComplete` and the genuinely-new block's reveal (wraps the existing `addBlock()` + `setState("placed")` call in a `setTimeout`), so the finale reads as a deliberate beat rather than blending into the last replayed block. Not visually verified against real depth (same "too few real blocks yet" caveat as above) — a mechanical, low-risk change (single `setTimeout` wrap around already-tested calls), `pnpm build` clean. Bump the constant if the pause feels too short/long once tried.

**Return-scan highlight "stopped working" — root cause was reaction-time, not a code bug (2026-08-10).** User reported that checking on an already-placed block via re-scan no longer highlights it. Verified via an instrumented `highlights.set` patch that `focusOnBlock(..., {highlight:true})` in `handleEnter`'s `existing` branch was still firing correctly every time — no regression in the code path. The real cause: `HIGHLIGHT_DUR` (`src/components/city/utils.ts`) was still `2.0` seconds, and the return-scan highlight fires immediately on the camera cut with no lead-up (unlike a fresh placement, where the ~2s montage already has the user's attention on the right spot before the highlight starts). By the time a real person types a 6-char code, the API round-trip completes, and they visually locate the right spot on a wall that may now show many blocks in frame (`FOCUS_OFFSET = 22`), the 2-second fade had often already finished — so it reads as "never highlighted" even though it technically ran every time. **Fix:** bumped `HIGHLIGHT_DUR` to `5.0` seconds (affects both the fresh-placement and return-scan highlight equally — a longer fade on a fresh placement is harmless/arguably nicer too). Verified visually with the same natural tool-call latency a real user's typing+reading would incur: the correct block (rightmost, matching `blockIndex`) showed up clearly yellow against the other 9 gray blocks in the screenshot. `pnpm build` clean, debug instrumentation removed afterward.

**Not tuned by feel yet:** `MONTAGE_BLOCK_COUNT = 100` and `MONTAGE_STAGGER_MS = 20` were picked as reasonable defaults, not iterated on visually against real depth (see above) — revisit once a building has enough real progress to judge the pacing properly.

## Status: close-up camera framing on placement/return (2026-08-10)

Added a "camera focuses on your block" feature: after a fresh placement, and again on any return scan of an already-placed token, the camera snaps in close to and facing the specific block (instead of staying on the wide city overview), and the same yellow highlight-fade used for newly-revealed blocks re-triggers so the user can pick their block out.

- **`src/components/city/cityScene.ts`** — new `findExposedFaceNormal(blocks, index)`: since voxels carry no face/normal data, this derives "which way does this block face outward" purely from occupancy — checks the 6 neighbor cells in a fixed priority order (front/left/right/back/top/bottom) and returns the first direction with no neighboring voxel. New `CitySceneHandle.focusOnBlock(buildingId, blockIndex, opts?: {highlight})`: sets `camera.position`/`controls.target` directly (no tween — an instant cut) to `FOCUS_OFFSET` (2.2 units) along that normal from the block's center, then optionally re-registers the block in `node.highlights` (the exact same Map the existing `addBlock()` reveal-highlight animation already drains every frame — no new animation code needed, just a new way to populate an entry for a block that's already visible instead of newly revealed). New `resetCamera()` snaps back to the wide establishing shot (extracted as `DEFAULT_CAMERA_POS`/`DEFAULT_CAMERA_TARGET` constants, same values as before). `controls.minDistance` lowered from 4 to 1.5 — OrbitControls clamps camera distance to this floor on every `update()` call, so the close-up framing was getting silently pushed back out until this was lowered below `FOCUS_OFFSET`.
- **`CityRenderer.tsx` / `City.tsx`** — plain pass-through of `focusOnBlock`/`resetCamera` through the existing imperative-handle chain (`CityRendererHandle` → `CityHandle`), no new logic.
- **`Kiosk.tsx`** — `handleConfirm` (fresh placement) calls `focusOnBlock(...)` (no `highlight` — `addBlock()` already highlights the newly-revealed block itself) right after `addBlock()`. `handleEnter`'s `existing` branch calls `focusOnBlock(..., { highlight: true })` (this block wasn't just revealed, so needs the explicit re-trigger). The "Scan next block" button now calls `resetCamera()` before returning to `entry` phase, so each new scan starts from the same wide shot.
- **Determinism, by construction, no DB changes needed**: `buildingId` + `blockIndex` (both already returned by `/enter` and `/place-block`) are the only inputs, and `blockIndex` indexes directly into the same sorted `Block[]` array (`processBlueprint` output) both the reveal-order logic and this new framing logic already share — same inputs always produce the same camera position.

**Verified in-browser** (fresh hospital placement via a real scan→enter→school→confirm cycle): camera snapped close to and facing the placed block's wall (gray hospital-wall texture filling most of the frame). Re-scanning the same token showed visually identical framing (confirming determinism) with "Already placed" state. Confirmed via temporary instrumentation that `focusOnBlock(..., {highlight:true})` actually registers a highlight entry on return (`highlights.set` fired with the correct `origIdx`) — the 2s fade completes fast enough that grabbing a screenshot mid-fade proved unreliable, so this was the more rigorous check. "Scan next block" confirmed to reset the camera back to the wide shot. `pnpm build` clean, no console errors, debug instrumentation removed afterward.

**`FOCUS_OFFSET` tuned twice after user feedback (2026-08-10):** initial `2.2` made the target block fill most of the frame — user wanted it smaller so neighboring blocks on the same wall are visible too. Bumped to `6` (verified: 2 adjacent voxels visible side by side), then bumped again to `15` per further feedback (verified: 3 adjacent voxels visible with clear space around them). Still just a reasonable-looking value, not derived from a target "N blocks visible" — revisit if it feels off once a wall has real depth (dozens/hundreds of blocks) rather than just a handful.

**Not tuned yet, worth revisiting by feel:** the face-priority tie-break (front/left/right/back/top/bottom) for corner blocks with multiple exposed faces is an arbitrary but deterministic choice, never compared against alternatives. Adjust if the framing feels off for particular block types (e.g. chimney/window blocks might want a different priority than plain wall blocks).

## Status: fresh-start reset + QR batch generation, found/fixed an order_index tie-break bug (2026-08-10)

Wiped all dev/test data and generated a real first batch of QR codes: **1080 residential** (enough to fully complete the currently-active `house` building) and **4000 hospital** (enough to fully complete the currently-active `hospital_small` building). New script: `server/src/scripts/reset-and-generate-qr.ts` (`pnpm run reset-and-generate-qr -- <category:count> ...`, run directly via `pnpm exec tsx` — `pnpm run ... --` double-passes a literal `--` to the script on this pnpm version, don't use that form). It wipes `blocks` → `desktop_codes` → `qr_codes` in FK order, resets every building to `completed_blocks = 0` / `status = 'queued'`, re-activates one building per category, generates the requested QR codes via `unnest()` batch insert, and writes a CSV (`server/qr-exports/qr-codes-<timestamp>.csv`, columns `category,public_token,scan_path`) so the tokens aren't only sitting in the DB — that CSV is what eventually needs turning into printable QR images.

**Found and fixed a real bug while doing this:** `order_index` resets to 0 for *every variant* within a category (see `BUILDINGS` in `seed.ts` — it's `i` within each variant's own loop, not a running counter across the whole category). Any query doing `WHERE category = $1 AND status = 'queued' ORDER BY order_index ASC LIMIT 1` is therefore ambiguous whenever a category has more than one variant, and Postgres doesn't reliably tie-break by insertion order once the table has real data and the planner picks a different scan strategy. This first bit on the very first run of the new reset script: it activated `short_apartment` instead of `house` for residential, `hospital_medium` instead of `hospital_small` for hospital, and `restaurant` instead of `food_bank` for food (school was fine — it's the only variant in that category, no tie). Fixed by adding `, id ASC` as a secondary sort key (id order = insertion order = the intended variant priority) in all three places this pattern appears: `seed.ts`, `reset-and-generate-qr.ts`, and — importantly — `server/src/routes/placeBlock.ts`'s building-rollover logic (line ~112, fires when a building completes and needs to activate the next `queued` one in its category). That last one would have silently mis-selected the next building on every future category completion, not just during this reset. Verified the live DB and `GET /api/buildings` after the fix: residential→`house`, hospital→`hospital_small`, food→`food_bank`, school→`school`, all at `completedBlocks: 0`. `pnpm run build` (server) clean.

**Current DB state:** 1 block placed (1 residential, from a manual test after the reset), 5080 QR codes (1080 residential + 4000 hospital), 0 desktop codes. The house and small hospital are exactly resourced to be completable end-to-end with this batch; food/school have no fresh QR codes yet (their active buildings are still `food_bank`/`school` from before, just zeroed out) — generate more via the same script when needed.

**Known gap surfaced by manual testing:** the kiosk only fetches server state once on mount and otherwise manages its Three.js scene incrementally (`addBlock()` calls) — it does **not** re-sync with the server mid-session. If the DB changes out-of-band (e.g. this reset script runs) while a `/kiosk` tab is already open, that tab's local block count silently diverges from the server's (confirmed: a tab left open across the reset showed 6 blocks after placing 1 new one, while the DB correctly had only 1 — the other 5 were stale renders from before the reset). Not a new bug, this is the already-deferred "multi-client/multi-tab live sync" item — just re-confirmed as the explanation next time this comes up. Mitigation until it's built: reload `/kiosk` after any out-of-band DB change.

**QR code images generated (2026-08-10):** added `server/src/scripts/generate-qr-images.ts` (`pnpm exec tsx src/scripts/generate-qr-images.ts [csvPath] [baseUrl]`, new `qrcode`/`@types/qrcode` deps) — turns a `qr-exports/qr-codes-*.csv` into actual scannable PNG QR images (400px, encoding `${baseUrl}${scan_path}`), organized as `qr-exports/images/<csv-stem>/<category>/<token>.png`. Defaults to the most recently generated CSV and `$QR_BASE_URL` or `http://localhost:5173`. Ran it against the 5080-code batch above → `server/qr-exports/images/qr-codes-2026-08-10T07-19-31-955Z/{residential,hospital}/*.png`, verified one visually (well-formed QR, scans to the right URL shape). **Caveat: these currently encode `localhost:5173`, which only a browser/device on this same machine can reach — not scannable by a real phone yet.** Regenerate with a LAN IP (for on-site testing) or the real prod domain (once Hetzner deployment exists, see Next steps) via the `baseUrl` arg/`QR_BASE_URL` env var before actually printing/using these for real.

**Regenerated for real phone testing, LAN IP (2026-08-10):** re-ran `generate-qr-images.ts` against `http://192.168.1.163:5173` (this machine's current LAN IPv4 — will change if the network changes, re-check via `ipconfig` before reusing) — same output directory, images overwritten with the new encoding. Two things had to be fixed to make this actually work end-to-end:
1. **The frontend dev server wasn't reachable from the LAN at all** — `pnpm dev` (plain `vite`) binds to `[::1]` (IPv6 loopback) only by default. Restarted it as `pnpm exec vite --host` (binds all interfaces) — confirmed via `netstat`/`curl` before and after. Note: `pnpm run dev -- --host` does **not** work, same double-`--` pnpm quirk noted elsewhere in this doc for the reset script — go through `pnpm exec vite --host` directly instead.
2. **`generate-qr-images.ts` had its own bug**: `process.argv[2] ?? (await latestCsv())` doesn't fall back on an empty string (only `null`/`undefined`), so the documented "pass `\"\"` for csvPath to use the default" usage actually crashed with `ENOENT`. Changed both `??` to `||` in `main()`. Fixed and verified: `pnpm exec tsx src/scripts/generate-qr-images.ts "" http://192.168.1.163:5173` now works as documented.

Verified via curl: `http://192.168.1.163:5173` (frontend + `/api` proxy) and `http://192.168.1.163:5173/s/<token>` (scan landing page) both return 200 from outside the loopback interface. One image re-viewed visually — correctly re-encoded (different pattern than the localhost version, as expected). **Remember: the dev server needs to be started with `pnpm exec vite --host` (not plain `pnpm dev`) for LAN/phone testing to work** — if it gets restarted later via the normal `pnpm dev` path, it'll silently go back to loopback-only and phones will fail to connect even though the QR codes still look fine.

## Environment notes
- Node lives at `C:\Tools\node-v24.19.0-win-x64` (not on PATH — call `node.exe`/`pnpm.cmd` with the full path, or `export PATH="/c/Tools/node-v24.19.0-win-x64:$PATH"` in Bash first).
- Docker Desktop is installed at `C:\Users\mm\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe` but was not running. It was just launched — **check whether it's actually up** (`docker ps`) before doing anything Docker-related.
- No native PostgreSQL install — decided to run Postgres via Docker, both locally and later on the Hetzner work server (which supports Docker).
- Repo has no backend yet — pure Vite/React frontend (`pnpm dev` / `pnpm build`).

## Decisions locked in
- **Backend stack:** Node/Express + PostgreSQL (via Docker).
- **QR category granularity:** coarse only — `residential | hospital | food | school`. A QR just says "this is a hospital block"; the app assigns it to whichever specific hospital (small/medium/large) is currently `in_progress`.
- **School attribution:** chosen once, at first block placement, and stored on `blocks.school_id`. Repeat/view-only scans skip the school picker.
- **Building order within a category:** not decided yet — schema just has a placeholder `order_index` column on `buildings`. Revisit the actual sequencing rule later.
- **Building counts finalized** (158 buildings, 500,000 blocks total):

| Building | Count | Blocks Each | Total |
|---|---|---|---|
| Houses | 75 | 1,080 | 81,000 |
| Short Apartments | 23 | 4,000 | 92,000 |
| Tall Apartments | 12 | 8,000 | 96,000 |
| Food Banks | 11 | 4,000 | 44,000 |
| Restaurants | 14 | 5,000 | 70,000 |
| Schools | 13 | 5,000 | 65,000 |
| Small Hospitals | 5 | 4,000 | 20,000 |
| Medium Hospitals | 4 | 6,000 | 24,000 |
| Large Hospitals | 1 | 8,000 | 8,000 |

- `src/imports/house-blueprint.json` was already trimmed from 1,084 → 1,080 voxels (removed one chimney shaft layer, gap closed) to make the Houses row consistent. `App.tsx` / `houseGenerator.ts` comments and UI text updated. Build verified clean.

## Finalized DB schema (not yet created anywhere)

```sql
CREATE TYPE building_category AS ENUM ('residential', 'hospital', 'food', 'school');
CREATE TYPE building_status   AS ENUM ('queued', 'in_progress', 'completed');

CREATE TABLE schools (
  id   SERIAL PRIMARY KEY,
  name TEXT NOT NULL
);

CREATE TABLE qr_codes (
  id            BIGSERIAL PRIMARY KEY,
  public_token  TEXT NOT NULL UNIQUE,        -- unguessable, embedded in the QR/URL, never the id
  category      building_category NOT NULL
);

CREATE TABLE desktop_codes (
  id         BIGSERIAL PRIMARY KEY,
  code       TEXT NOT NULL,                  -- short, human-typed
  qr_id      BIGINT NOT NULL REFERENCES qr_codes(id),
  expires_at TIMESTAMPTZ NOT NULL,
  used_at    TIMESTAMPTZ,                    -- null = still valid/unused
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE UNIQUE INDEX desktop_codes_active_code_uq
  ON desktop_codes (code) WHERE used_at IS NULL;

CREATE TABLE buildings (
  id               BIGSERIAL PRIMARY KEY,
  category         building_category NOT NULL,
  variant          TEXT NOT NULL,             -- 'house' | 'short_apartment' | 'hospital_small' | 'school_design_1' | ...
  order_index      INT NOT NULL,              -- placeholder for sequencing; rule TBD later
  total_blocks     INT NOT NULL,
  completed_blocks INT NOT NULL DEFAULT 0,
  status           building_status NOT NULL DEFAULT 'queued'
);
CREATE UNIQUE INDEX buildings_one_active_per_category
  ON buildings (category) WHERE status = 'in_progress';

CREATE TABLE blocks (
  id          BIGSERIAL PRIMARY KEY,
  building_id BIGINT NOT NULL REFERENCES buildings(id),
  block_index INT NOT NULL,
  qr_id       BIGINT NOT NULL UNIQUE REFERENCES qr_codes(id),
  school_id   BIGINT NOT NULL REFERENCES schools(id),
  placed_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (building_id, block_index)
);
```

## Status: backend scaffolded and smoke-tested (2026-08-07)

All 7 steps below are done. Summary of what exists now:

- `docker-compose.yml` (repo root) — Postgres 16-alpine, `caritascity-postgres` container, db `caritascity` / user `caritas` / pass `caritas_dev_pw`, port 5432, named volume `postgres_data`. `docker compose up -d` to start.
- `server/` — new pnpm workspace package (added to `pnpm-workspace.yaml`). Express 4 + TypeScript, run via `tsx`.
  - `src/config.ts`, `src/db.ts` — env config (`server/.env`, copy of `.env.example`) and a `pg` Pool.
  - `src/migrations/001_init.sql` — exactly the schema from this file. `pnpm run migrate` applies it (tracked via a `schema_migrations` table, safe to re-run).
  - `src/scripts/seed.ts` — `pnpm run seed`. Inserts all 158 `buildings` rows (order_index = position within category, per the table above), one dev `schools` row ("Dev Test School", id=1), and 5 dev `qr_codes` (residential x2, hospital, food, school). Also flips one `queued` building per category to `in_progress` so `/place-block` has somewhere to write immediately.
  - `src/routes/scan.ts`, `enter.ts`, `placeBlock.ts` — the three endpoints, wired in `src/index.ts`. `pnpm run dev` starts the server on `:3001` (`/health` for a liveness check).
- Manually verified end-to-end: scan → enter (needs_school) → place-block → re-scan same QR → enter (existing/view-only), plus unknown-QR 404 and expired/reused-code 400. All correct.

### Decisions made while implementing (were previously open/unresolved — revisit if they don't fit)
- **Desktop code flow, precisely:** `/scan/:public_token` mints a `desktop_codes` row and returns `{ code, expiresAt }` to the phone — the phone is expected to *display* this code for the user to type into the desktop kiosk. The code is **not** marked used at `/enter` time if the QR still needs a school picker; it stays live so `/place-block` can redeem it with the same `code` (no separate session token, no qr_id ever sent to the client). It's only marked `used_at` once the flow actually terminates (either the view-only `/enter` response, or a successful `/place-block`).
- **Desktop code shape:** 6 characters, alphabet `ABCDEFGHJKMNPQRSTUVWXYZ23456789` (no 0/O/1/I/L — avoids handwriting/typing ambiguity). TTL: 5 minutes. Both are env-configurable (`DESKTOP_CODE_LENGTH`, `DESKTOP_CODE_TTL_MINUTES` in `server/.env`) — easy to tune later.
- **Rate limiting:** `express-rate-limit`, per-IP, 20 requests / 15 min on both `/enter` and `/place-block`. Not tuned against real usage patterns yet — the shared kiosk likely wants a per-device or per-session limit instead of per-IP eventually.
- **`order_index` sequencing:** still arbitrary (seed order, i.e. array order above) — the actual sequencing rule is still undecided, only the placeholder column is populated.
- **Building rollover:** implemented eagerly, in the same transaction that completes a building — when a building hits `total_blocks`, the next `queued` building in that category (by `order_index`) is immediately flipped to `in_progress` before the transaction commits, so the one-`in_progress`-per-category invariant never has a gap.
- `public_token` uses `nanoid()` default alphabet/length (21 chars) — unguessable, URL-safe.

## Status: scan + kiosk frontend built and verified (2026-08-07)

Phone-side scan landing page and desktop kiosk UI exist and were verified end-to-end in a real browser against the real backend (not just curl). Scope was deliberately limited to these two pages — wiring live placements into the full 158-building 3D city view is still a separate, later step (see Next steps).

- **Backend routes moved under `/api`** (`server/src/index.ts`) — `POST /api/scan/:publicToken`, `POST /api/enter`, `POST /api/place-block`, and a new `GET /api/schools` (`server/src/routes/schools.ts`, returns `[{id, name}]`, powers the kiosk's school picker — didn't exist before). One clean prefix for the Vite dev proxy and any future reverse-proxy rule; doesn't affect QR codes already minted since those encode the frontend URL (`/s/:token`), not this backend path.
- **`enter.ts` / `placeBlock.ts` responses enriched** — both now include `totalBlocks`, `completedBlocks`, and `category` on the returned block, so the kiosk can render a progress bar without a second request.
- **Frontend routing added** — `react-router` (already an installed-but-unused dependency) is now wired up in `src/main.tsx` via `BrowserRouter`/`Routes`/`Route`:
  - `/` — unchanged, still the original 3D demo (`App.tsx`)
  - `/s/:token` — new `src/app/pages/ScanLanding.tsx`
  - `/kiosk` — new `src/app/pages/Kiosk.tsx`
- **`vite.config.ts`** — added `server.proxy` for `/api` → `http://localhost:3001`, so `pnpm dev` can call the Express server with no CORS setup needed (relative `fetch('/api/...')` calls work the same in dev and in a same-origin prod deploy later).
- **`src/lib/api.ts`** — typed fetch client (`scanQr`, `enterCode`, `placeBlock`, `getSchools`) with an `ApiError` carrying the backend's `error` code so pages can branch on it (`unknown_qr_code`, `invalid_or_expired_code`, `unknown_school`, `category_complete`, `block_already_placed`, etc.).
- **`ScanLanding.tsx`** — mobile-first, standalone (no 3D canvas). Calls `scanQr` on mount, shows the code with a live mm:ss countdown to expiry, and handles unknown-QR (404) and retryable-error states.
- **`Kiosk.tsx`** — desktop-facing state machine: code entry (shadcn `InputOTP`, restricted to the desktop-code alphabet, auto-submits on the 6th character) → `needs_school` (shadcn `Select` populated from `GET /api/schools`) → `placed` (building variant + progress bar) or `existing` (view-only, same progress bar, for already-scanned blocks). Reuses existing shadcn primitives throughout (`Card`, `Button`, `Progress`, `Label`, `Select`, `InputOTP`) — no new UI primitives written.
- Verified in an actual browser via `claude-in-chrome`: fresh scan → code display/countdown renders → kiosk entry auto-submits → school picker populates from the live DB (seeded "Dev Test School") → placement confirms with a correct 1/5000-style progress bar. Also verified via curl through the Vite proxy: unknown-token 404, and the `existing`/view-only response shape for an already-placed block.

## Status: connecting the kiosk to the real 3D game — rendering bug FIXED (2026-08-10)

**Root cause found and fixed.** The city was rendering correctly the whole time — the actual bug was suspect #1 from the list below: in `Kiosk.tsx`, the full-screen `showsModal` overlay (shown by default, since the kiosk boots straight into the `entry` phase) had no `pointer-events: none`, so it captured every wheel/scroll event across the *entire* viewport, not just over the card. That made it impossible to zoom in far enough to see anything, which looked exactly like "no buildings rendering."

Confirmed via direct instrumentation (temporary `window.__debugScene` / NDC projection of a known block position) that the mesh data, camera framing, and Three.js pipeline were all correct all along — a real block was rendering at the right screen position, just as a sub-pixel speck at the original camera distance (residential/food/school each had only 1-2 of thousands of blocks placed; hospital had 0). Suspect #3 (data/registration) is ruled out — `getBuildings()`, `buildCityBuilding()`, and `syncBuilding()` all checked out with correct real data.

**Fix applied** (`src/app/pages/Kiosk.tsx`): added `pointer-events-none` to the full-viewport `showsModal` wrapper div, and `pointer-events-auto` to the `EntryStep`/`SchoolStep` `Card` components so the card itself stays clickable/typeable while scroll/pointer events pass through to the canvas everywhere else. Verified: scroll now moves `OrbitControls` (camera distance changed on scroll, confirmed via `camera.position.distanceTo(controls.target)`), and the code-entry flow still works end-to-end (OTP auto-submit, error states) with the new pointer-events split. `pnpm build` clean.

**Not fixed, not a bug, just noted:** at the default boot camera distance (`position(0,90,-140)/target(0,10,0)`), a single freshly-placed block is only a few pixels on screen — technically correct but not very legible for a kiosk meant to show "here's what's been built." Worth considering pulling the camera in closer by default, or adding a smarter auto-frame (e.g. frame the actual current bounding box of placed blocks rather than the eventual full-building footprint) as a later polish pass. Not blocking further work.

### Full end-to-end verification pass (2026-08-10) — found and fixed a second bug

Ran the complete flow for real against the running stack (Postgres seed data + backend `:3001` + frontend `:5173`), driven through the actual browser UI via `claude-in-chrome`, not just curl:

1. **Fresh scan → kiosk → school picker → confirm → block reveal.** `POST /api/scan/:token` on the unused seeded hospital QR (`wuoW5L1ckxrgPfXTf615j`) → entered the returned code in the kiosk OTP field → correctly landed on `needs_school` labeled "Placing a Hospital block". **Found a second real bug here:** the school combobox button (`SchoolStep`'s `PopoverTrigger asChild` wrapping the shadcn `Button`) didn't open visibly. Root cause: `src/app/components/ui/button.tsx`'s `Button` was a plain function component, not wrapped in `React.forwardRef` — Radix's `Popper` positioning needs a real DOM ref from the trigger, and without one it rendered the popover content off-screen (confirmed via DOM inspection: `top: -156px`, `transform: translate(0px, -200%)`) even though `aria-expanded` correctly flipped to `true`. This was never caught before because the earlier session got blocked by the city-rendering bug before ever reaching this newly-rewritten combobox.
   - **Fix:** wrapped `Button` in `React.forwardRef<HTMLButtonElement, ...>` and passed `ref` through to the underlying `Comp` (`Slot` or `"button"`). Standard shadcn/ui pattern — this component had just been missing it.
   - Re-verified after the fix: combobox opens, shows "Dev Test School", selecting it enables "Enter the game", confirming placement shows "Block placed! Hospital — hospital_small, 1/4000 placed" with a progress bar, and the previously-invisible React ref warning is gone from the console.
   - `GET /api/buildings` confirmed the backend persisted it: hospital `completedBlocks` went from 0 → 1.
2. **Re-scan an already-placed token → existing/view-only.** `POST /api/scan/:token` again on the already-placed residential QR (`w3VnN76N-QsGjMwtBVBwA`) → entering that code in the kiosk skipped the school picker entirely and went straight to "Already placed — Residential — house, 2/1080 placed". Correct.
3. **Refresh → boot-reveal.** Reloading `/kiosk` re-triggers the boot-reveal effect (`addBlock` fired once per category with `completedBlocks > 0`, confirmed via earlier instrumentation and code review of `Kiosk.tsx`'s `revealedOnLoad` effect); visually consistent with the known "tiny speck at default zoom" legibility caveat above, no regression.

All debug instrumentation removed. `pnpm build` clean. No console errors on load. Dev stack (Postgres, backend, frontend) left running.

## Status: connecting the kiosk to the real 3D game — IN PROGRESS, blocked on a rendering bug (2026-08-07) — historical, see fix above

Full plan for this pass is saved at `C:\Users\mm\.claude\plans\serene-sprouting-axolotl.md` (title: "Connect the QR scanner + school picker to the real 3D game") — read that first, it has the full context/reasoning. Summary of what's been decided and built:

**Decisions locked in for this pass:**
- `/kiosk` embeds the live 3D game directly (code-entry/school-picker overlay on top of the real city canvas) — no redirect to `/`. `/` (`App.tsx`) stays the old untouched 5-house demo.
- Exactly 4 buildings shown — one per category (residential/hospital/food/school), whichever is `in_progress` in the DB.
- Only houses have real 3D art. Hospital/food/school get flat-colored placeholder box blueprints, sized exactly to each building's real `total_blocks` (verified via a standalone Node script — exact-count math is correct for 1/2/4000/5000/6000/8000/4000000).
- School picker is now a searchable Popover+Command combobox (shadcn, cmdk-based, client-side filtered) instead of a plain `Select` — scales to the ~1,500 real schools coming later.
- `houseGenerator.ts` untouched; palette/window-size UI removal is a separate later task, not this one.

**Built so far (backend + frontend both build/typecheck clean):**
- `server/src/routes/buildings.ts` — new `GET /api/buildings`, returns the in-progress building per category. Verified via curl through the Vite proxy — returns correct real data.
- `src/lib/api.ts` — added `getBuildings()` / `BuildingState` type.
- `src/components/city/placeholderGenerator.ts` — new, `generatePlaceholderBlueprint(totalBlocks, color)`, exact-N box packer.
- `src/components/city/cityScene.ts` — camera constants changed from `position(0,55,-80)/target(0,8,10)` to `position(0,90,-140)/target(0,10,0)` to (attempt to) frame a 2×2 layout instead of the old 5-houses-in-a-row layout. **This framing is unverified and is a prime suspect for the bug below.**
- `src/app/pages/Kiosk.tsx` — rewritten: fetches `getBuildings()` on mount, builds `CityBuilding[]`/`ConstructionQueue` (real house blueprint for `variant==="house"`, placeholder box otherwise), renders `<City>` full-bleed with the state-machine `Card` as an overlay (`bg-background/40 backdrop-blur-sm`, `absolute inset-0`, centered) for `entry`/`needs_school`, and a bottom-bar confirmation (not a full overlay) for `placed`/`existing` so the city stays visible. Plot layout: residential(-30,-20), hospital(30,-20), food(-30,20), school(30,20). `cityRef.current.addBlock(category)` fires both on successful placement and once per category on load (for any category with real progress > 0) to play the reveal-highlight animation and catch local state up to the server.

**BUG — not yet root-caused, blocking verification:** On `/kiosk`, the city canvas renders (sky + ground, confirmed via screenshot) but **no buildings are visible at all** — not even as tiny specks after scrolling to zoom out. Debugging was cut short (user rejected a `javascript_tool` call mid-investigation, then asked to save state instead). What's confirmed so far:
- The Three.js rendering pipeline itself is fine — on `/` (old demo), clicking "Auto Build" for a few seconds visibly renders a real house (screenshotted, looked correct — brick/roof/window colors, highlight animation).
- On `/kiosk`, scrolling to zoom out over the canvas had **zero visible effect** (identical screenshot before/after) — meaning the scroll either isn't reaching `OrbitControls`, or nothing changed because there's nothing off-frame either. Leading suspects, in order to check next:
  1. **The full-screen overlay div may be eating all pointer/scroll events.** In `Kiosk.tsx`, the `showsModal` block wraps the entry/school-picker `Card` in a div with `absolute inset-0` — that covers the *entire* viewport (not just around the card), so it likely captures the wheel event before it reaches the canvas underneath. This would explain why scrolling did nothing, independent of whether buildings are actually there. Needs fixing regardless (should only intercept clicks over the card itself, or at least not eat scroll everywhere) — but doesn't by itself prove buildings are missing.
  2. **Camera framing math may just be wrong** — the new `position(0,90,-140)/target(0,10,0)` in `cityScene.ts` was chosen by rough proportion, never actually verified to frame plots at `±30,±20`. Needs either careful recalculation or empirical iteration (temporarily revert to a known-working camera, confirm buildings appear, then adjust incrementally).
  3. **Possible data/registration issue** — never got to confirm client-side whether `getBuildings()` actually resolved, whether `buildCityBuilding()` produced sane `CityBuilding` objects, or whether `<City initialBuildings>` actually received them, before the debugging session was cut short. Current real data (confirmed via curl, so this part is *not* in question) as of last check: residential/house 2/1080, hospital/hospital_small 0/4000, food/food_bank 1/4000, school/school 1/5000. Note hospital is at 0 — expect literally nothing to render for it (correct behavior), but the other three should show at least 1–2 tiny voxels minimum even before fixing framing.
- **Dev servers were left running** for whoever resumes: Postgres (`caritascity-postgres`), backend (`cd server && pnpm run dev`, `:3001`), frontend (root `pnpm dev`, `:5173`) — all confirmed up as of this note.

*(This section is historical — see "Next steps & resume prompt" at the top of this file for the current, accurate list.)*
